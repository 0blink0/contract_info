from __future__ import annotations

from backend.app.extract.field_catalog import (
    CHAPTER_FIELDS,
    MANUAL_ONLY_PRODUCT,
    SKIP_PRODUCT_FIELDS,
)
from backend.app.extract.text_limits import text_for_llm_prompt

_ENUM_HINTS: dict[str, str] = {
    "风险等级": "R1/R2/R3/R4/R5",
    "基金面值": (
        "⚠️只输出阿拉伯数字本身：1 或 100 或 1000。"
        "禁止输出任何包含「元」「人民币」「/份」「认购」等文字的内容。\n"
        "合同写「人民币1.000元，认购价格为1.000元/份」→输出 1；"
        "「初始面值为100元/份」→输出 100；「1000元/份」→输出 1000。"
    ),
    "币种": (
        "从以下选项选填：人民币现钞/美元现汇/多币种/欧元现汇/港元现汇/澳元现汇/日元现汇/其他/英镑现汇。\n"
        "⚠️合同记账本位币写「人民币」（不含「现钞」二字）→ 必须填「人民币现钞」；禁止直接输出「人民币」。"
    ),
    "产品类型（协会）": (
        "【重要】禁止直接使用合同中「基金的类型：XX类」或「基金类型：XX类」标注值，"
        "须从投资范围/投资限制章节的资产占比约束独立判断，仅填以下5类之一：\n"
        "权益类：合同明确规定≥80%【总资产】投于股票等权益类资产；\n"
        "固定收益类：合同明确规定≥80%【总资产】投于债券/固定收益类资产；\n"
        "期货和衍生品类：合同明确规定≥80%【总资产】投于期货/衍生品；\n"
        "私募证券投资母基金：合同规定主要投于其他私募基金/资管产品；\n"
        "混合类：以下任一情形→混合类：①80%约束分母为「已投资产」而非总资产"
        "（如「不低于本基金已投资产的80%」）；②同时列举多类资产；③无单类≥80%总资产约束。\n"
        "⚠️「已投资产」≠「总资产」：「不低于已投资产80%」属混合类，不属于权益类/固收类。\n"
        "⚠️禁止在分类名称后追加「基金」二字：合同写「混合类基金」→输出「混合类」。\n"
        "若投资范围/限制文本不在本窗口，留空。"
    ),
    "是否封闭": (
        "封闭/不封闭；"
        "封闭 = 合同设有封闭期，在封闭期内不开放申购赎回"
        "（含「满足XX条件后方可申请赎回」等条件性开放，有封闭期即为封闭）；"
        "不封闭 = 无封闭期约定，开放式运作"
    ),
    "海外基金": "否/是",
    "结构类型": (
        "仅填「普通」或「母子结构」，禁止输出其他描述。\n"
        "合同「基金的运作方式：契约型【开放式】」「合伙型」「公司型」等是组织形式描述，与此字段无关；\n"
        "无母子结构（子基金/母基金关系）时一律填「普通」。"
    ),
    "份额结构": (
        "仅填「不分级」或「分级结构」，禁止输出合同原文描述句子。\n"
        "合同基本情况或申购赎回章节中描述有A/B/C/D等多类份额→分级结构；"
        "无分类份额描述，或仅有单一份额→不分级。"
        "不得留空，必须从合同中判断后输出其中一项。"
    ),
    "开放日规则": (
        "输出格式固定为：「申购、赎回｜每周X开放、非交易日向后递推」，其中X为开放日名称。\n"
        "多个开放日用「/」分隔，例如：「申购、赎回｜每周周一/周三/周五开放、非交易日向后递推」。\n"
        "若合同写的是「如遇节假日顺延」「如遇非交易日向后递推」，统一输出为「非交易日向后递推」。\n"
        "不填入成立日期或开始日期，该信息由系统自动补充。\n"
        "若合同约定封闭期内不开放，则前缀改为「赎回｜」并注明封闭期约定。"
    ),
    "外包机构": "仅输出机构名称，如「广发证券股份有限公司」；不输出描述性句子",
    "首次申购起点": (
        "⚠️只填金额数字+「万」，如「100万」；禁止输出合同原文句子。\n"
        "合同写「首次申购金额应不低于100万元人民币」→输出「100万」。"
    ),
    "追加起点": (
        "⚠️只填金额数字+「万」，如「1.0万」；禁止输出合同原文句子。\n"
        "合同写「每次追加申购的金额应不低于【1】万元人民币」→输出「1.0万」；"
        "合同无追加限制→填「无追加起点限制」。"
    ),
    "最低持有类型": "仅填「金额」或留空；合同有「赎回后持有资产净值不低于X万元」→填「金额」；合同无此限制→留空",
    "最小变动单位": (
        "⚠️只填金额数字+「万」，如「1.0万」；禁止输出原文句子。\n"
        "仅当合同明确写「以X万元为单位递增/追加」「超出起点部分以X万元整数倍追加」等递增单位表述时才填写；\n"
        "「每次追加申购金额不低于X万元」描述的是追加起点，不是最小变动单位，不能作为本字段的依据；\n"
        "合同未明确约定递增单位→留空。"
    ),
    "最低持有数量": (
        "⚠️只填金额数字+「万」，如「100万」；禁止输出合同原文句子。\n"
        "合同写「赎回后持有的基金资产净值不得低于100万元」→输出「100万」；合同无明确最低持有限制→留空。"
    ),
    "业绩比较基准": (
        "⚠️合同含「不设置业绩比较基准」「不设置」「无业绩」等表述→输出「无」；"
        "禁止输出「本基金不设置业绩比较基准」等原文句子；否则摘录基准描述。"
    ),
    "投资经理信息": "格式：「姓名（实际）」，多人用「、」分隔；禁止填写任职时间或日期。",
    "投资收益分配": (
        "若合同无定期强制分配条款（仅描述分配方式选择权），固定填「不分配」。"
        "若合同有具体强制分配方案则摘录关键约定。不得留空。"
    ),
    "预警线": (
        "⚠️禁止输出「本基金设置预警线…」等合同原文句子，只填净值数值。三种情形：\n"
        "①不分级基金有设置：填「X元」如「0.85元」；合同写「预警线【0.75】元」或「净值为0.8500元设置为预警线」→「0.75元」/「0.85元」\n"
        "②分级结构基金有设置：即使合同按整体净值设置，也须按份额类别分列，"
        "格式「主份额/X元、A份额/X元、B份额/X元、C份额/X元」（有哪类写哪类，各类填同一数值），用「、」分隔；\n"
        "示例：合同写「预警线【0.75】元」且有A/B/C类份额→「主份额/0.75元、A份额/0.75元、B份额/0.75元、C份额/0.75元」\n"
        "③不设置预警线→「无」"
    ),
    "止损线": (
        "⚠️禁止输出「本基金设置止损线…」等合同原文句子，只填净值数值。三种情形：\n"
        "①不分级基金有设置：填「X元」如「0.80元」；合同写「止损线【0.70】元」→「0.70元」\n"
        "②分级结构基金有设置：格式同预警线，各份额填同一止损值；\n"
        "示例：合同写「止损线【0.70】元」且有A/B/C类份额→「主份额/0.70元、A份额/0.70元、B份额/0.70元、C份额/0.70元」\n"
        "③不设置→「无」"
    ),
    "计费频率": "按日/按月/按季/按年；「每日计提」「逐日计提」→按日；无明确则留空",
    "计费基准": "前一日资产净值/期间均值/其他；无明确说明则留空",
    "年计提天数": "实际天数/365/360；「当年实际天数」「÷N（N为当年天数）」→实际天数；无明确则留空",
    "费用计算方式": "系统计算/管理人计算；合同含「每日计提」「自动扣划」→系统计算；无明确说明则留空",
    "管理类型": (
        "仅输出以下之一，禁止输出合同描述性语句（如「按照恪尽职守…原则管理运用基金财产」）：\n"
        "自主管理型/顾问管理型/银行委外合作(信托通道)/券商委外合作(信托通道)/其他。\n"
        "无管理类型标签时推断：合同明确聘请「投资顾问」负责投资决策→顾问管理型；"
        "管理人自主投资决策（或无顾问条款）→自主管理型；无法确定则留空。"
    ),
    "冷静期回访": (
        "若合同有明确专项条款，摘录核心约定；"
        "若合同无明确专项约定，固定填：「合同无明确约定（普通投资者默认需要回访，专业投资者默认不需要回访）」。"
        "不得留空。"
    ),
    "双录": (
        "若合同有明确双录条款，摘录核心约定；"
        "若合同无明确双录条款，固定填：「普通投资者首次购买需智能自助双录」。"
        "不得留空。"
    ),
}


def build_messages(window_key: str, text: str) -> list[dict[str, str]]:
    fields = CHAPTER_FIELDS.get(window_key, [])
    field_list = "、".join(fields) if fields else "（无额外字段）"
    hints = []
    for name in fields:
        if name in _ENUM_HINTS:
            hints.append(f"{name}：{_ENUM_HINTS[name]}")
    hint_block = "\n".join(hints) if hints else "（无额外枚举提示）"

    system = (
        "你是私募基金合同要素抽取助手。仅根据给定合同片段输出一个 JSON 对象，"
        "键名为中文（与需抽取字段一致），值为字符串；无法确定则该键为空字符串。"
        "日期尽量规范为 yyyy/m/d。禁止输出解释、markdown 或多余键。"
        f"以下字段勿编造（合同无则留空）：{'、'.join(sorted(MANUAL_ONLY_PRODUCT | SKIP_PRODUCT_FIELDS))}。"
    )
    user = (
        f"【章节窗口】{window_key}\n"
        f"【需抽取字段】{field_list}\n"
        f"【枚举参考】\n{hint_block}\n\n"
        f"【合同片段】\n{text_for_llm_prompt(text)}\n\n"
        "请仅输出 JSON 对象。"
    )
    return [{"role": "system", "content": system}, {"role": "user", "content": user}]


def build_rag_history_block(rag_cases: list[dict[str, str]] | None) -> str:
    cases = rag_cases or []
    if not cases:
        return ""

    lines = ["【历史案例参考】"]
    for idx, case in enumerate(cases, start=1):
        field_name = str(case.get("field_name", "") or "").strip()
        field_value = str(case.get("field_value", "") or "").strip()
        snippet = str(case.get("snippet", "") or "").strip()
        lines.append(f"{idx}. 字段名：{field_name}")
        lines.append(f"   字段值：{field_value}")
        lines.append(f"   原文摘录：{text_for_llm_prompt(snippet)}")
    return "\n".join(lines)
