---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - contract_info/frontend/package.json
  - contract_info/frontend/vite.config.ts
  - contract_info/frontend/tsconfig.json
  - contract_info/frontend/tsconfig.node.json
  - contract_info/frontend/index.html
  - contract_info/frontend/src/main.ts
  - contract_info/frontend/src/App.vue
  - contract_info/frontend/src/api/client.ts
  - contract_info/frontend/src/api/types.ts
  - contract_info/frontend/src/constants/status.ts
  - contract_info/frontend/src/composables/useJobPoll.ts
  - contract_info/frontend/src/components/UploadPanel.vue
  - contract_info/frontend/src/components/JobList.vue
  - contract_info/frontend/src/components/ProcessStepper.vue
  - contract_info/frontend/src/components/WarningsList.vue
  - contract_info/frontend/src/components/JobDetail.vue
  - contract_info/frontend/.env.example
  - contract_info/.gitignore
  - contract_info/README.md
autonomous: true
requirements:
  - UI-01
  - DOC-01
  - DEV-01
---

<objective>
Phase 5 Wave 2：Vue 3 + Vite + TS 前端 — 上传、历史列表、步骤条、轮询、warnings、双下载、README。
</objective>

<threat_model>
<trust_boundary>浏览器 ↔ API（X-API-Key 在构建产物中可见，仅内网）。</trust_boundary>
<stride_register>
- threat_id: T-05-02
  category: I
  component: VITE_API_KEY
  disposition: mitigated
  mitigation_plan: 文档说明勿将带 Key 的 dist 暴露公网；开发环境无 Key。
</stride_register>
</threat_model>

<must_haves>
- `npm run dev` 在 5173，proxy `/api` 到 8000
- 上传 docx → 列表出现 pending →「开始处理」→ 步骤条更新 → exported 后两下载按钮
- 失败态显示重试
- ROADMAP 三条成功标准可手工验证
</must_haves>

<tasks>

<task id="05-02-01" type="execute">
  <read_first>
    - contract_info/.planning/phases/05-frontend/05-RESEARCH.md
    - ai_bid_management/bid_tool_agents/frontend/vite.config.ts
  </read_first>
  <files>
    contract_info/frontend/package.json
    contract_info/frontend/vite.config.ts
    contract_info/frontend/tsconfig.json
    contract_info/frontend/tsconfig.node.json
    contract_info/frontend/index.html
    contract_info/frontend/src/vite-env.d.ts
    contract_info/frontend/src/main.ts
    contract_info/frontend/.env.example
    contract_info/.gitignore
  </files>
  <action>
初始化 **Vue 3 + Vite + Vue-TS** 项目于 `contract_info/frontend/`：

- 依赖：`vue`, `element-plus`, `@element-plus/icons-vue`（可选）
- `vite.config.ts`：`server.port: 5173`，`proxy: { '/api': { target: 'http://127.0.0.1:8000', changeOrigin: true } }`
- `main.ts` 注册 Element Plus（中文 locale `zh-cn`）
- `.env.example`：`VITE_API_KEY=`
- `.gitignore` 增加 `frontend/node_modules`, `frontend/dist`

`src/vite-env.d.ts` 声明 `VITE_API_KEY`。
  </action>
  <verify>
    cd contract_info/frontend; npm install; npm run build
  </verify>
  <acceptance_criteria>
    - build 无 TS 错误
  </acceptance_criteria>
</task>

<task id="05-02-02" type="execute">
  <read_first>
    - contract_info/backend/app/api/schemas.py
  </read_first>
  <files>
    contract_info/frontend/src/api/types.ts
    contract_info/frontend/src/api/client.ts
    contract_info/frontend/src/constants/status.ts
    contract_info/frontend/src/composables/useJobPoll.ts
  </files>
  <action>
**types.ts** — 对齐后端 `JobListItem`, `JobDetailResponse`, `WarningItem`, `UploadResponse`。

**client.ts**：
- `apiFetch(path, options)` 自动加 `X-API-Key`（若 `import.meta.env.VITE_API_KEY`）
- `upload(file)`, `listJobs(limit)`, `getJob(id)`, `runJob(id)`
- `downloadUrl(jobId, kind: 'product-elements' | 'fee-rates')` 返回路径；`downloadBlob` 用于带 Key 的 fetch 下载

**status.ts** — `stepStates(status)` 返回三步 `{label, state: 'wait'|'process'|'finish'|'error'}`；`isInProgress(status)`；`statusLabelZh(status)`。

**useJobPoll.ts** — 接收 `jobId` ref + `status` ref；in-progress 时每 2.5s 调 `getJob`；onUnmounted clear。
  </action>
  <verify>
    npm run build
  </verify>
  <acceptance_criteria>
    - 类型与 API JSON 字段一致（job_id 等）
  </acceptance_criteria>
</task>

<task id="05-02-03" type="execute">
  <read_first>
    - contract_info/.planning/phases/05-frontend/05-CONTEXT.md § 交互流程
  </read_first>
  <files>
    contract_info/frontend/src/components/UploadPanel.vue
    contract_info/frontend/src/components/JobList.vue
    contract_info/frontend/src/components/ProcessStepper.vue
    contract_info/frontend/src/components/WarningsList.vue
    contract_info/frontend/src/components/JobDetail.vue
    contract_info/frontend/src/App.vue
  </files>
  <action>
**布局（App.vue）：** 顶栏标题「合同要素抽取」；左栏 `JobList` + `UploadPanel`；右栏 `JobDetail`。

**UploadPanel：** `el-upload` 仅 `.docx`，`auto-upload=false` 或手动调 `upload()`；成功后 emit `uploaded(jobId)`。

**JobList：** 挂载时 `listJobs(20)`；点击选中；显示 filename + `statusLabelZh` + 时间。

**JobDetail：**
- pending：主按钮「开始处理」→ `runJob`
- `ProcessStepper` 绑定当前 status
- in-progress 时启用 `useJobPoll`
- `failed`/`extraction_failed`/`export_failed`：显示 `error_message` +「重试」
- `exported`：两按钮下载（product / fee），用 `downloadBlob` 若需 Key
- `WarningsList`：`el-collapse` 展示 warnings（count>0）

上传/运行后刷新列表并选中当前 job。
  </action>
  <verify>
    手测：backend :8000 + `npm run dev`，上传 example docx → 开始处理 → 完成下载
  </verify>
  <acceptance_criteria>
    - 三步步骤条随 status 变化
    - warnings 可展开
  </acceptance_criteria>
</task>

<task id="05-02-04" type="execute">
  <read_first>
    - contract_info/README.md
  </read_first>
  <files>
    contract_info/README.md
  </files>
  <action>
README 新增 **前端** 节：

```powershell
cd frontend
npm install
npm run dev   # http://localhost:5173
```

并行启动 backend `uvicorn ... --port 8000`。

生产：`npm run build`，`dist/` + Nginx 反代 `/api`；`VITE_API_KEY` 构建注入说明。

满足 DEV-01 中 Web 运行说明。
  </action>
  <verify>
    Select-String contract_info/README.md -Pattern "npm run dev"
  </verify>
  <acceptance_criteria>
    - 文档含双进程启动步骤
  </acceptance_criteria>
</task>

</tasks>
