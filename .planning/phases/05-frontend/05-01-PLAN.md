---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contract_info/backend/app/api/schemas.py
  - contract_info/backend/app/api/routes/jobs.py
  - contract_info/backend/tests/test_api_jobs_list.py
  - contract_info/backend/tests/test_api_job_detail.py
autonomous: true
requirements:
  - API-02
  - UI-01
---

<objective>
Phase 5 Wave 1：扩展后端 API — 任务列表与 job 详情中的 warnings 数组，供前端历史列表与可展开 warnings。
</objective>

<threat_model>
<trust_boundary>列表接口暴露近期文件名与状态（内网运营工具）。</trust_boundary>
<stride_register>
- threat_id: T-05-01
  category: I
  component: GET /jobs
  disposition: mitigated
  mitigation_plan: 与现有路由相同 X-API-Key；limit 上限 50。
</stride_register>
</threat_model>

<must_haves>
- `GET /api/v1/jobs?limit=20` 返回 `{ items: JobListItem[] }`
- `GET /api/v1/jobs/{id}` 含 `extraction_warnings: [{field, code, message, suggestion?}]`
- 仍不返回 `parse_json` / `extraction_result`
- pytest 列表与 detail warnings（mock DB 或 integration skip）
</must_haves>

<tasks>

<task id="05-01-01" type="execute">
  <read_first>
    - contract_info/.planning/phases/05-frontend/05-CONTEXT.md
    - contract_info/backend/app/api/schemas.py
    - contract_info/backend/app/models/contract_file.py
  </read_first>
  <files>
    contract_info/backend/app/api/schemas.py
  </files>
  <action>
新增 Pydantic 模型：

- `WarningItem` — field, code, message, suggestion optional
- `JobListItem` — job_id, filename, status, created_at (ISO string)
- `JobListResponse` — items: list[JobListItem]

扩展 `JobDetailResponse`：
- `extraction_warnings: list[WarningItem] = []`
- 保留 `extraction_warnings_count`（可与 len(warnings) 一致）

辅助函数 `_warnings_from_record(record)`：将 JSONB list 转为 `WarningItem[]`，非法项跳过。
  </action>
  <verify>
    python -c "from backend.app.api.schemas import JobListResponse, WarningItem"
  </verify>
  <acceptance_criteria>
    - OpenAPI 中可见新 schema
  </acceptance_criteria>
</task>

<task id="05-01-02" type="execute">
  <read_first>
    - contract_info/backend/app/api/routes/jobs.py
  </read_first>
  <files>
    contract_info/backend/app/api/routes/jobs.py
  </files>
  <action>
**路由顺序：** 在 `@router.get("/{job_id}")` **之前** 添加：

```python
@router.get("", response_model=JobListResponse)
def list_jobs(limit: int = Query(20, ge=1, le=50)): ...
```

查询 `ContractFile` 按 `created_at.desc()`，limit 条，映射为 `JobListItem`。

更新 `_record_to_detail` 填充 `extraction_warnings`。
  </action>
  <verify>
    curl -s "http://localhost:8000/api/v1/jobs?limit=5" -H "X-API-Key: ..."
  </verify>
  <acceptance_criteria>
    - 空库返回 items: []
    - 有记录时含 filename、status
  </acceptance_criteria>
</task>

<task id="05-01-03" type="execute">
  <read_first>
    - contract_info/backend/tests/conftest.py
  </read_first>
  <files>
    contract_info/backend/tests/test_api_jobs_list.py
    contract_info/backend/tests/test_api_job_detail.py
  </files>
  <action>
**test_api_jobs_list：** patch SessionLocal/query 或 integration mark；验证 200 与 items 结构。

**test_api_job_detail：** mock record 含 extraction_warnings JSON，GET detail 返回 2 条 warnings。

不破坏既有 test_api_* 套件。
  </action>
  <verify>
    pytest contract_info/backend/tests/test_api_jobs_list.py contract_info/backend/tests/test_api_job_detail.py -q
  </verify>
  <acceptance_criteria>
    - 新测试通过；全 backend tests 仍 green
  </acceptance_criteria>
</task>

</tasks>
