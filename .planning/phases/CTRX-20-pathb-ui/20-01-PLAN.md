---
phase: 20-pathb-kb-entry-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - backend/tests/test_kb_service.py
  - backend/tests/test_api_kb.py
autonomous: true
requirements:
  - KB-BE-01
  - KB-BE-02
  - KB-BE-03
  - KB-BE-04
  - KB-BE-05

must_haves:
  truths:
    - "requirements.txt 中包含 lancedb>=0.33.0 和 sentence-transformers>=5.5.1"
    - "test_kb_service.py 中存在 test_init_creates_table 和 test_model_unavailable_soft_degrade 测试桩"
    - "test_api_kb.py 中存在 test_post_entries_success / test_post_entries_503_when_no_model / test_get_entries / test_get_entries_filter / test_delete_entry 测试桩"
    - "pytest 以 SKIP 或 XFAIL 方式通过（非 ERROR），等待 Wave 1 实现"
  artifacts:
    - path: "requirements.txt"
      provides: "新增两个 PyPI 包声明"
      contains: "lancedb"
    - path: "backend/tests/test_kb_service.py"
      provides: "KB service unit 测试骨架"
      exports: ["test_init_creates_table", "test_model_unavailable_soft_degrade"]
    - path: "backend/tests/test_api_kb.py"
      provides: "KB API integration 测试骨架"
      exports:
        - "test_post_entries_success"
        - "test_post_entries_503_when_no_model"
        - "test_get_entries"
        - "test_get_entries_filter"
        - "test_delete_entry"
  key_links:
    - from: "backend/tests/test_api_kb.py"
      to: "backend/app/main.py"
      via: "TestClient(app) — 复用 conftest.py 中已有的 api_client fixture"
      pattern: "api_client.*api_headers"
    - from: "backend/tests/test_kb_service.py"
      to: "backend/app/services/kb_service.py"
      via: "直接导入（Wave 1 实现后解除 ImportError）"
      pattern: "from backend.app.services.kb_service import"
---

<objective>
Wave 0：安装后端新依赖，创建测试骨架。

本计划为 Wave 1（后端实现）建立测试先行基础：
- 将 lancedb 和 sentence-transformers 添加至 requirements.txt
- 创建 test_kb_service.py（KB-BE-01 unit、KB-BE-02 soft degrade unit）
- 创建 test_api_kb.py（KB-BE-03~05 TestClient integration 测试）

测试骨架采用 pytest.mark.xfail(reason="Wave 1 not implemented") 标记，
Wave 1 实现完成后移除该标记即可转为正式测试。

Purpose: Nyquist 合规——测试先行，Wave 1 实现后直接运行，无需补写测试。
Output: requirements.txt（修改）、test_kb_service.py、test_api_kb.py（新增）。
</objective>

<execution_context>
@d:/bid_management/contract_info/.planning/phases/CTRX-20-pathb-ui/20-RESEARCH.md
@d:/bid_management/contract_info/.planning/phases/CTRX-20-pathb-ui/20-VALIDATION.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/phases/CTRX-20-pathb-ui/20-CONTEXT.md

<interfaces>
<!-- 现有 conftest.py fixtures（直接复用，无需修改） -->

来自 backend/tests/conftest.py:
```python
@pytest.fixture
def api_settings(monkeypatch):
    monkeypatch.setenv("API_KEY", "test-key")
    from backend.app.config import get_settings
    get_settings.cache_clear()
    yield
    get_settings.cache_clear()

@pytest.fixture
def api_client(api_settings):
    from fastapi.testclient import TestClient
    from backend.app.main import app
    return TestClient(app)

@pytest.fixture
def api_headers():
    return {"X-API-Key": "test-key"}
```

<!-- Wave 0 测试骨架只需知道最终接口契约（来自 RESEARCH.md Pattern 2） -->
<!-- POST /api/v1/kb/entries → body: KbEntriesRequest → 200 { ids: [...], count: N } -->
<!-- GET /api/v1/kb/entries?field_name=X → 200 { items: [...], total: N } -->
<!-- DELETE /api/v1/kb/entries/{id} → 204 -->
<!-- POST /api/v1/kb/entries 无模型 → 503 { detail: "知识库功能不可用（bge-m3 模型未加载）" } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>任务 1：添加 PyPI 依赖至 requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt（当前内容，确认 lancedb/sentence-transformers 不重复添加）
  </read_first>
  <action>
在 requirements.txt 末尾追加两行（保持现有行不变）：

```
lancedb>=0.33.0
sentence-transformers>=5.5.1
```

pyarrow 无需单独声明（lancedb 依赖自动引入）。
不添加 pandas（项目故意不使用，list_entries 用 to_arrow().to_pydict()）。
  </action>
  <verify>
    <automated>python -c "txt=open('requirements.txt').read(); assert 'lancedb' in txt and 'sentence-transformers' in txt, 'missing deps'"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt 包含行 `lancedb>=0.33.0`（版本约束精确）
    - requirements.txt 包含行 `sentence-transformers>=5.5.1`
    - 不包含 `pandas`
    - 现有 14 行依赖保持不变
  </acceptance_criteria>
  <done>requirements.txt 新增两行依赖，pip install -r requirements.txt 不报错（lancedb + sentence-transformers 安装成功）</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：创建 test_kb_service.py 测试骨架</name>
  <files>backend/tests/test_kb_service.py</files>
  <read_first>
    - backend/tests/conftest.py（tmp_path fixture 使用方式，pytest.fixture 结构）
    - .planning/phases/CTRX-20-pathb-ui/20-RESEARCH.md（Pattern 1 kb_service 完整结构，用于了解测试目标接口）
    - .planning/phases/CTRX-20-pathb-ui/20-VALIDATION.md（测试名称精确字符串）
  </read_first>
  <behavior>
    - test_init_creates_table：调用 init_kb_service()（mock lancedb.connect + monkeypatch CTRX_DATA_DIR 指向 tmp_path），断言 _kb 不为 None；断言 _kb.model_available 为 False（未提供 CTRX_MODELS_DIR）
    - test_model_unavailable_soft_degrade：init_kb_service() 在 CTRX_MODELS_DIR 指向不存在路径时不抛异常；_kb 不为 None；_kb.model_available 为 False
    - 两个测试均用 pytest.mark.xfail(reason="Wave 1: kb_service not yet implemented", strict=False) 标记，Wave 1 实现后移除该标记
    - 使用 tmp_path fixture（pytest 内置）提供临时 LanceDB 目录，不污染生产数据
  </behavior>
  <action>
新建 backend/tests/test_kb_service.py：

导入：pytest、os、unittest.mock（patch）。
测试函数精确名称（来自 VALIDATION.md）：
  - test_init_creates_table
  - test_model_unavailable_soft_degrade

test_init_creates_table 逻辑：
  1. monkeypatch.setenv("CTRX_DATA_DIR", str(tmp_path))
  2. monkeypatch.delenv("CTRX_MODELS_DIR", raising=False)
  3. 使用 `with patch("lancedb.connect") as mock_connect:` 避免真实 I/O
     - mock_connect 返回 mock_db；mock_db.open_table side_effect=Exception；mock_db.create_table 返回 mock_table
  4. 调用 `from backend.app.services import kb_service; kb_service._kb = None; kb_service.init_kb_service()`
  5. 断言 `kb_service._kb is not None`
  6. 断言 `kb_service._kb.model_available is False`

test_model_unavailable_soft_degrade 逻辑：
  1. monkeypatch.setenv("CTRX_DATA_DIR", str(tmp_path))
  2. monkeypatch.setenv("CTRX_MODELS_DIR", str(tmp_path / "nonexistent_models"))
  3. 同样 patch lancedb.connect
  4. 调用 init_kb_service()
  5. 断言无异常抛出
  6. 断言 kb_service._kb.model_available is False

两个测试函数均添加 @pytest.mark.xfail(reason="Wave 1: kb_service not yet implemented", strict=False)。
  </action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && python -m pytest backend/tests/test_kb_service.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在于 backend/tests/test_kb_service.py
    - 包含函数 test_init_creates_table 和 test_model_unavailable_soft_degrade（精确函数名）
    - pytest 运行结果为 XFAIL 或 XPASS，无 ERROR
    - 不导入 pandas
    - 使用 tmp_path fixture（不硬编码临时路径）
  </acceptance_criteria>
  <done>pytest backend/tests/test_kb_service.py -v 无 ERROR，两个测试为 XFAIL 状态</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：创建 test_api_kb.py 集成测试骨架</name>
  <files>backend/tests/test_api_kb.py</files>
  <read_first>
    - backend/tests/conftest.py（api_client、api_headers fixture 用法）
    - .planning/phases/CTRX-20-pathb-ui/20-RESEARCH.md（Pattern 2 kb.py 路由接口契约，含 request/response schema 章节）
    - .planning/phases/CTRX-20-pathb-ui/20-VALIDATION.md（精确测试函数名）
  </read_first>
  <behavior>
    - test_post_entries_success：mock KbService.add_entries 返回 ["uuid-1"]，POST /api/v1/kb/entries 返回 200，body { ids: ["uuid-1"], count: 1 }
    - test_post_entries_503_when_no_model：KbService.model_available=False，POST /api/v1/kb/entries 返回 503，detail 含 "知识库功能不可用"
    - test_get_entries：mock KbService.list_entries 返回 1 条，GET /api/v1/kb/entries 返回 200，items 长度 1，total=1
    - test_get_entries_filter：GET /api/v1/kb/entries?field_name=业绩报酬提取方式，断言 query param 传入 list_entries(field_name="业绩报酬提取方式")
    - test_delete_entry：mock KbService.delete_entry，DELETE /api/v1/kb/entries/{id}，返回 204
    - 所有测试均用 pytest.mark.xfail(reason="Wave 1: routes/kb not yet implemented", strict=False) 标记
  </behavior>
  <action>
新建 backend/tests/test_api_kb.py：

测试函数精确名（来自 VALIDATION.md）：
  - test_post_entries_success
  - test_post_entries_503_when_no_model
  - test_get_entries
  - test_get_entries_filter
  - test_delete_entry

所有函数接受 api_client、api_headers 两个 fixture 参数。

mock 策略（避免真实 LanceDB 和 bge-m3 调用）：
  - patch "backend.app.services.kb_service.get_kb_service" 返回 mock_svc
  - mock_svc.model_available = True（或 False，按测试需要）
  - mock_svc.add_entries 为 AsyncMock，返回 ["test-uuid-1"]
  - mock_svc.list_entries 返回 [{"id":"test-uuid-1","field_name":"业绩报酬提取方式","field_value":"高水位法","snippet":"","source_job_id":"job-1","source_filename":"test.docx","created_at":"2026-06-02T10:00:00+00:00"}]
  - mock_svc.delete_entry 为普通 Mock（同步 void）

请求体（KB_ENTRY_PAYLOAD）：
```python
KB_ENTRY_PAYLOAD = {
    "entries": [{
        "field_name": "业绩报酬提取方式",
        "field_value": "基金整体资产高水位法",
        "snippet": "",
        "source_job_id": "job-uuid-1",
        "source_filename": "test.docx"
    }]
}
```

端点前缀：/api/v1/kb（v1 prefix + kb router prefix，参见 RESEARCH.md Pattern 4）。

test_post_entries_503_when_no_model：设置 mock_svc.model_available = False。

所有函数添加 @pytest.mark.xfail(reason="Wave 1: routes/kb not yet implemented", strict=False)。
  </action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && python -m pytest backend/tests/test_api_kb.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在于 backend/tests/test_api_kb.py
    - 包含 5 个精确函数名（test_post_entries_success、test_post_entries_503_when_no_model、test_get_entries、test_get_entries_filter、test_delete_entry）
    - pytest 运行结果为 XFAIL 或 XPASS，无 ERROR
    - 不包含 import pandas
    - 使用 unittest.mock.patch / AsyncMock，不依赖真实 lancedb 文件 I/O
    - 测试覆盖端点前缀 /api/v1/kb/entries（含 v1 前缀）
  </acceptance_criteria>
  <done>pytest backend/tests/test_api_kb.py -v 无 ERROR，5 个测试全为 XFAIL 状态</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 说明 |
|------|------|
| CI/CD → PyPI | requirements.txt 变更触发 pip install，引入新依赖 |

## STRIDE 威胁清单

| 威胁 ID | 类别 | 组件 | 处置 | 缓解措施 |
|---------|------|------|------|---------|
| T-20-01-SC | Tampering | pip install lancedb / sentence-transformers | accept | RESEARCH.md Package Legitimacy Audit 已对三个包进行 PyPI 直接验证（Approved），slopcheck 假阳性已记录 |

本计划（Wave 0）仅修改 requirements.txt 并创建测试文件，无生产代码路由或服务实现，攻击面极小。
</threat_model>

<verification>
全阶段快速验证：

```bash
cd /d d:\bid_management\contract_info
python -m pytest backend/tests/test_kb_service.py backend/tests/test_api_kb.py -v -q
```

预期：7 个测试全部 XFAIL（无 ERROR）。
</verification>

<success_criteria>
- requirements.txt 含 lancedb>=0.33.0 和 sentence-transformers>=5.5.1
- backend/tests/test_kb_service.py 和 test_api_kb.py 均存在
- pytest 运行 7 个测试骨架，全部 XFAIL，无 ERROR
- Wave 1 可直接移除 xfail 标记后验证，无需补写测试
</success_criteria>

<output>
完成后在 .planning/phases/CTRX-20-pathb-ui/ 创建 20-01-SUMMARY.md
</output>
