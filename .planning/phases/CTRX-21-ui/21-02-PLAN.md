---
phase: 21-kb-config-ui
plan: "02"
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - frontend/src/api/kb.ts
  - frontend/src/composables/useKbConfigList.ts
  - frontend/src/views/KbConfigView.vue
  - frontend/src/layouts/AppLayout.vue
  - frontend/src/router/index.ts
autonomous: true
requirements:
  - KB-UI-01
  - KB-UI-02
  - KB-UI-03
  - KB-UI-04

must_haves:
  truths:
    - "左侧菜单新增“知识库配置”入口，位置在“文件列表”与“系统设置”之间（per D-01）"
    - "菜单高亮继续沿用 `default-active + router` 机制，且不改变“文件详情”子菜单展开逻辑（per D-02）"
    - "进入知识库配置页后可展示分页列表并按字段名防抖过滤（per D-03, D-04）"
    - "删除流程有二次确认，删除后保持当前筛选与页码并刷新当前结果（per D-05, D-06）"
  artifacts:
    - path: "frontend/src/layouts/AppLayout.vue"
      provides: "知识库配置菜单入口"
      contains: "index=\"/kb-config\""
    - path: "frontend/src/router/index.ts"
      provides: "知识库配置路由注册"
      exports: ["kb-config route"]
    - path: "frontend/src/views/KbConfigView.vue"
      provides: "列表展示/过滤/删除 UI"
      contains: "el-table + el-pagination"
    - path: "frontend/src/composables/useKbConfigList.ts"
      provides: "防抖过滤 + 分页状态 + 删除后重拉"
      exports: ["useKbConfigList"]
    - path: "frontend/src/api/kb.ts"
      provides: "分页参数化 API 客户端"
      exports: ["getKbEntries"]
  key_links:
    - from: "frontend/src/layouts/AppLayout.vue"
      to: "frontend/src/router/index.ts"
      via: "el-menu router + /kb-config route"
      pattern: "default-active.*router"
    - from: "frontend/src/views/KbConfigView.vue"
      to: "frontend/src/composables/useKbConfigList.ts"
      via: "页面状态与操作组合"
      pattern: "useKbConfigList"
    - from: "frontend/src/composables/useKbConfigList.ts"
      to: "frontend/src/api/kb.ts"
      via: "getKbEntries/deleteKbEntry"
      pattern: "getKbEntries\\(|deleteKbEntry\\("
---

<objective>
完成知识库配置页前端可用闭环：菜单导航、列表展示、字段名筛选、删除确认与上下文保持刷新。

Purpose: 交付 Phase 21 面向用户的主功能，并精确落实 D-01~D-06 决策。
Output: 新增 KB 配置页与状态管理，导航/路由接入完成。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/phases/CTRX-21-ui/21-CONTEXT.md
@d:/bid_management/contract_info/.planning/phases/CTRX-21-ui/21-UI-SPEC.md
@d:/bid_management/contract_info/frontend/src/layouts/AppLayout.vue
@d:/bid_management/contract_info/frontend/src/router/index.ts
@d:/bid_management/contract_info/frontend/src/views/FileListView.vue
</context>

<tasks>

<task type="auto">
  <name>任务 1：接入导航入口与路由页面</name>
  <files>frontend/src/layouts/AppLayout.vue, frontend/src/router/index.ts</files>
  <action>在 `AppLayout.vue` 增加 `/kb-config` 菜单项，插入位置严格按 D-01；继续复用 `default-active + router` 高亮机制，不改动文件详情子菜单展开逻辑（D-02）。在 `router/index.ts` 注册 `kb-config` 路由并设置中文 title。实现时显式对齐 `21-PATTERNS.md` 中的 analog：`frontend/src/layouts/AppLayout.vue`（菜单插入与 active 同步）和 `frontend/src/router/index.ts`（lazy route 注册与 meta 标题模式）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:router --prefix frontend</automated>
  </verify>
  <done>从左侧菜单可进入知识库配置页且高亮行为与现有菜单一致。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：实现列表状态与 API 查询契约</name>
  <files>frontend/src/api/kb.ts, frontend/src/composables/useKbConfigList.ts</files>
  <behavior>
    - Test 1: 关键词变化触发防抖请求并重置到第 1 页（per D-04）。
    - Test 2: 页码/页大小变化按服务端分页参数请求（per D-03）。
    - Test 3: 删除成功后沿用当前 keyword/page/pageSize 重拉（per D-05）。
  </behavior>
  <action>扩展 `getKbEntries` 支持 `field_name/page/page_size` 参数；新增 `useKbConfigList` 统一管理 loading、keyword、pagination、data、refresh、remove。实现防抖即时查询与过期请求保护，禁止本地全量筛选。实现时显式复用 `21-PATTERNS.md` 的 analog：`frontend/src/api/kb.ts`（query composition + delete wrapper）与 `frontend/src/composables/usePathB.ts`（composable 的 refresh/reset/error 结构）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend</automated>
  </verify>
  <done>查询、分页、过滤、删除后的上下文保持刷新可在 composable 层完整驱动。</done>
</task>

<task type="auto">
  <name>任务 3：实现知识库配置页交互与文案</name>
  <files>frontend/src/views/KbConfigView.vue</files>
  <action>创建页面：顶部标题+筛选输入+刷新；表格列包含字段名/字段值/原文摘录/来源合同/入库时间/操作；删除使用二次确认文案“删除后不可恢复”（D-06），成功失败均给出中文反馈并保留后端错误摘要。接入分页组件并绑定 composable 状态。页面编排和交互细节显式参考 `21-PATTERNS.md` 的 analog：`frontend/src/views/FileListView.vue`（页头+表格+删除动作）与 `frontend/src/composables/useConfirmDeleteJob.ts`（确认弹窗与错误反馈模式）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend</automated>
  </verify>
  <done>用户可在新页面完成查看、字段名筛选、确认删除、上下文不丢失刷新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input->frontend state | 字段名关键词来自不受信任输入 |
| frontend->api | 删除与查询请求跨越网络边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | 删除操作 | mitigate | 删除前强制二次确认（D-06）并仅按 ID 发起删除 |
| T-21-05 | Repudiation | 删除反馈 | mitigate | 成功/失败提示明确，失败透传后端 detail 便于审计 |
| T-21-06 | Denial of Service | 即时过滤请求 | mitigate | 防抖 + 过期请求清理，避免输入风暴请求 |
</threat_model>

<verification>
cd /d d:\bid_management\contract_info && npm run test:router --prefix frontend && npm run test:frontend --prefix frontend
</verification>

<success_criteria>
- 菜单出现“知识库配置”并可正确导航（KB-UI-01）
- 页面以服务端分页展示并支持字段名过滤（KB-UI-02/03）
- 删除具备二次确认，删除后在当前筛选上下文刷新（KB-UI-04 + D-05/D-06）
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-21-ui/21-02-SUMMARY.md`
</output>
