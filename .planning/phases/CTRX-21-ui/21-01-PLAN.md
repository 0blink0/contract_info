---
phase: 21-kb-config-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/kb_service.py
  - backend/app/api/routes/kb.py
  - backend/tests/test_api_kb.py
autonomous: true
requirements:
  - KB-UI-02
  - KB-UI-03

must_haves:
  truths:
    - "知识库列表请求支持服务端分页，不再依赖前端全量加载（per D-03）"
    - "字段名过滤在服务端执行且与分页组合可用（per D-04）"
    - "查询返回总数与当前页数据，前端可据此渲染分页器"
  artifacts:
    - path: "backend/app/services/kb_service.py"
      provides: "字段名过滤 + 分页切片能力"
      exports: ["KbService.list_entries"]
    - path: "backend/app/api/routes/kb.py"
      provides: "GET /kb/entries 的分页查询参数与响应"
      exports: ["get_kb_entries", "KbListResponse"]
    - path: "backend/tests/test_api_kb.py"
      provides: "分页与过滤契约回归测试"
      contains: "field_name + page + page_size"
  key_links:
    - from: "backend/app/api/routes/kb.py"
      to: "backend/app/services/kb_service.py"
      via: "get_kb_entries -> list_entries"
      pattern: "list_entries\\(field_name=.*page=.*page_size="
    - from: "backend/tests/test_api_kb.py"
      to: "backend/app/api/routes/kb.py"
      via: "TestClient 请求 /api/v1/kb/entries"
      pattern: "GET /api/v1/kb/entries\\?field_name="
---

<objective>
为 Phase 21 提供服务端分页与字段名过滤的后端契约，作为知识库配置页 UI 的数据基础。

Purpose: 落实 D-03（服务端分页）与 D-04（字段名即时过滤）并提供稳定可测接口。
Output: 可分页查询的 KB API、服务层分页过滤逻辑、对应 pytest 用例。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/REQUIREMENTS.md
@d:/bid_management/contract_info/.planning/phases/CTRX-21-ui/21-CONTEXT.md
@d:/bid_management/contract_info/.planning/phases/CTRX-21-ui/21-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：扩展 KB 服务层分页过滤契约</name>
  <files>backend/app/services/kb_service.py</files>
  <behavior>
    - Test 1: `field_name` 为空时返回全量总数 + 当前页切片。
    - Test 2: `field_name` 有值时执行包含匹配并返回过滤后总数与当前页切片（per D-04）。
    - Test 3: 非法页码输入由路由层兜底后仍返回稳定结构。
  </behavior>
  <action>将 `list_entries` 改为接收 `field_name/page/page_size`，返回 `{items, total}` 结构；字段名过滤使用包含匹配（非全等），再做分页切片。保持既有行字段不变，不修改入库与删除逻辑。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_api_kb.py::test_get_entries_filter -q</automated>
  </verify>
  <done>服务层可在单次调用中完成过滤+分页，并可返回过滤后总数。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：扩展 GET /kb/entries 分页参数与响应</name>
  <files>backend/app/api/routes/kb.py</files>
  <behavior>
    - Test 1: 传 `page/page_size` 时返回对应页 `items` 与 `total`。
    - Test 2: 传 `field_name` + 分页组合时仍返回正确 `total`。
  </behavior>
  <action>在 `get_kb_entries` 新增查询参数 `page`（>=1）和 `page_size`（1-100），调用服务层新契约并直接返回 `items + total`。当 service 不可用仍返回空列表结构，保持后向兼容。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_api_kb.py::test_get_entries -q</automated>
  </verify>
  <done>API 层支持服务端分页查询，响应结构稳定为 `items/total`。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：补齐后端分页过滤回归测试</name>
  <files>backend/tests/test_api_kb.py</files>
  <behavior>
    - Test 1: `GET /api/v1/kb/entries?page=2&page_size=10` 断言调用参数与响应结构。
    - Test 2: `field_name` 过滤词与分页并用时，断言服务调用包含 `field_name/page/page_size`。
  </behavior>
  <action>扩展现有 KB API 测试，覆盖分页默认值、过滤分页组合、参数透传断言；保留现有删除与创建测试，不降低覆盖面。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_api_kb.py -q</automated>
  </verify>
  <done>pytest 覆盖 GET 分页与过滤组合路径且通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend->api | 未受信任查询参数进入分页和过滤逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | `GET /kb/entries` query params | mitigate | 路由层约束 `page>=1`、`page_size<=100`，避免异常参数放大查询 |
| T-21-02 | Denial of Service | `KbService.list_entries` | mitigate | 限制 page_size 上限并在服务层先过滤后切片 |
| T-21-03 | Information Disclosure | 错误响应 | accept | 沿用现有错误模型，不额外回显敏感堆栈 |
</threat_model>

<verification>
cd /d d:\bid_management\contract_info && pytest backend/tests/test_api_kb.py -q
</verification>

<success_criteria>
- `GET /kb/entries` 支持 `field_name/page/page_size`，并返回 `items + total`
- 服务端执行过滤与分页（非前端全量拉取），满足 D-03 / D-04
- 后端测试覆盖分页与过滤组合并通过
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-21-ui/21-01-SUMMARY.md`
</output>
