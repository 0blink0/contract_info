## Deferred Items

- [out-of-scope] `npm run test:router --prefix frontend` fails at `frontend/tests/router/hub-pathb.test.mjs` because `JobHubView.vue` no longer contains `WarningsList` assertion target; unrelated to Phase 21 KB-config changes.
