---
phase: 18-hub-pathb
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - frontend/src/composables/usePathB.ts
  - frontend/src/components/pathb/PathBDetail.vue
  - frontend/src/components/PathBPanel.vue
  - frontend/src/views/JobFieldBView.vue
  - frontend/tests/router/hub-pathb.test.mjs
autonomous: true
requirements:
  - PB-01
  - PB-02

must_haves:
  truths:
    - "D-08/D-09: JobFieldBView 全页 PathBDetail 自动加载"
    - "D-10: PathBPanel 复用 PathBDetail"
    - "D-11: 页码列或全局说明「页码暂未解析」"
    - "D-12: 手录说明 alert"
    - "PB-02: copyJson + downloadJson"
    - "D-13/D-14: hub-pathb.test.mjs + phase gate"
  artifacts:
    - path: frontend/src/composables/usePathB.ts
    - path: frontend/src/components/pathb/PathBDetail.vue
    - path: frontend/src/views/JobFieldBView.vue
    - path: frontend/tests/router/hub-pathb.test.mjs
---

<objective>
Deliver full Field B page (PB-01, PB-02) via extracted PathB logic; add phase tests and build gate.

Output: usePathB, PathBDetail, JobFieldBView, refactored PathBPanel, tests.
</objective>

<context>
@contract_info/frontend/src/components/PathBPanel.vue
@contract_info/frontend/src/views/JobFieldBView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: usePathB composable (D-08)</name>
  <files>frontend/src/composables/usePathB.ts</files>
  <action>
    Extract from PathBPanel: data, loading, loaded, load, refresh, jsonText, copyJson, downloadJson, computed crmRows, rawSections, tierRows, filledCount.
    Accept `jobId: Ref<string | null>` or getter.
  </action>
</task>

<task type="auto">
  <name>Task 2: PathBDetail component (D-09, D-11, D-12, PB-01)</name>
  <files>frontend/src/components/pathb/PathBDetail.vue</files>
  <action>
    - Props: `jobId: string`, `available: boolean`
    - onMounted: if available, load()
    - Top el-alert: CRM 手录、不进 Excel
    - Secondary alert if no page metadata: 「合同摘录暂未标注页码，请结合原文块判断」
    - CRM table add column 「页码」 with cell 「页码暂未解析」 (or derive if future API adds page)
    - source_snippet_rows table: 标签 / 摘录 / 页码列
    - Actions: refresh, show/hide JSON, copy, download
    - raw_sections blocks + tier table (from PathBPanel)
  </action>
</task>

<task type="auto">
  <name>Task 3: JobFieldBView + PathBPanel refactor (D-10)</name>
  <files>frontend/src/views/JobFieldBView.vue, frontend/src/components/PathBPanel.vue</files>
  <action>
    JobFieldBView:
    - inject detail; show PathBDetail when jobId && path_b_available else el-empty
    - Remove Phase 17 placeholder alert

    PathBPanel:
    - el-collapse wrapper only; body = PathBDetail with lazy load on expand (keep onExpand load pattern or delegate to PathBDetail with `:auto-load="false"` prop)
  </action>
</task>

<task type="auto">
  <name>Task 4: Static tests + gate (D-13, D-14)</name>
  <files>frontend/tests/router/hub-pathb.test.mjs</files>
  <action>
    Assert:
    - JobHubView imports WarningsList and ValidationPanel
    - JobDetailLayout does not import WarningsList
    - JobFieldBView imports PathBDetail or usePathB
    - JobHubView imports useHubSummary
    Run test:router, typecheck, build.
  </action>
</task>

</tasks>

<verification>
```bash
cd contract_info/frontend && npm run test:router && npm run typecheck && npm run build
```
</verification>
