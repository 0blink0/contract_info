---
phase: 18-hub-pathb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/composables/useHubSummary.ts
  - frontend/src/components/hub/HubSectionCard.vue
  - frontend/src/views/JobHubView.vue
autonomous: true
requirements:
  - HUB-01

must_haves:
  truths:
    - "D-01/D-02: 五表并行分表 GET 仅用于行数摘要"
    - "D-03: 字段 B 摘要卡有一句 path-b 摘要"
    - "D-04: 可选展示 job 级 validation fail/warn 角标"
    - "Hub 卡片链到 job-table / job-field-b"
  artifacts:
    - path: frontend/src/composables/useHubSummary.ts
    - path: frontend/src/components/hub/HubSectionCard.vue
    - path: frontend/src/views/JobHubView.vue
---

<objective>
Replace Hub placeholder with summary cards showing row counts and field B teaser (HUB-01).

Output: useHubSummary, HubSectionCard, upgraded JobHubView (cards only — validation/warnings in 18-02).
</objective>

<context>
@contract_info/.planning/phases/CTRX-18-hub-pathb/18-CONTEXT.md
@contract_info/frontend/src/views/JobHubView.vue
@contract_info/frontend/src/constants/jobSections.ts
@contract_info/frontend/src/composables/useJobDetailContext.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useHubSummary composable (D-01, D-03)</name>
  <files>frontend/src/composables/useHubSummary.ts</files>
  <action>
    - Input: use `useJobDetailInject()` for jobId, status, detail.
    - `tableSummaries: Ref<Array<{ key: TableKey, label, rowCount: number | null, loading: boolean }>>`
    - When PREVIEW_PLUS status: `Promise.all(JOB_TABLE_SECTIONS.map(s => getJobPreviewSection))` then compute rowCount via product_rows.length or fee_rows.length etc. (reuse helpers from jobSections).
    - `pathBSummary: Ref<string | null>`, `pathBLoading` — if detail.path_b_available, fetch getPathB once, set summary like `CRM 已建议 ${filledCount}/6 项` or open_day line.
    - Expose `reload()` for refresh button on Hub optional.
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 2: HubSectionCard component (D-02, D-04)</name>
  <files>frontend/src/components/hub/HubSectionCard.vue</files>
  <action>
    Props: title, subtitle (row count or status), :to route object, optional failCount/warnCount badges (job-level, same for all cards or omit per-card).
    Visual: extend Phase 16 nav-card with metrics line.
  </action>
</task>

<task type="auto">
  <name>Task 3: JobHubView summary grid (D-07 partial)</name>
  <files>frontend/src/views/JobHubView.vue</files>
  <action>
    - Remove「后续版本」placeholder desc.
    - Render 5× HubSectionCard from useHubSummary + field B card with pathBSummary.
    - Show skeleton while loading; el-alert when not PREVIEW_PLUS.
    - Job-level validation tags near page title if fail/warn &gt; 0 from detail.
  </action>
  <verify>npm run typecheck</verify>
</task>

</tasks>

<verification>`cd frontend && npm run typecheck`</verification>
