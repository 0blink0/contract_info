---
phase: 18-hub-pathb
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - frontend/src/views/JobHubView.vue
  - frontend/src/layouts/JobDetailLayout.vue
autonomous: true
requirements:
  - HUB-02
  - HUB-03

must_haves:
  truths:
    - "D-05: WarningsList 仅在 JobHubView"
    - "D-06: ValidationPanel 仅在 JobHubView"
    - "D-07: JobDetailLayout 无 WarningsList/ValidationPanel/ExportPreview"
    - "表页不出现校验/警告全量面板"
  artifacts:
    - path: frontend/src/views/JobHubView.vue
      provides: warnings + validation on hub
    - path: frontend/src/layouts/JobDetailLayout.vue
      provides: slim chrome without hub-only panels
---

<objective>
Move job-level warnings and validation to Hub only (HUB-02, HUB-03); keep Layout as pipeline chrome.

Output: Hub hosts WarningsList + ValidationPanel; Layout stripped.
</objective>

<context>
@contract_info/frontend/src/layouts/JobDetailLayout.vue
@contract_info/frontend/src/components/WarningsList.vue
@contract_info/frontend/src/components/ValidationPanel.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add panels to JobHubView (D-05, D-06)</name>
  <files>frontend/src/views/JobHubView.vue</files>
  <action>
    Below summary cards:
    - `WarningsList` with `:warnings="detail.extraction_warnings"` `:count="detail.extraction_warnings_count"` when detail present.
    - `ValidationPanel` with jobId, visible=PREVIEW_PLUS, available/fail/warn from detail (same props as old JobDetail.vue).
    Order: cards → warnings → validation (match ARCHITECTURE).
  </action>
</task>

<task type="auto">
  <name>Task 2: Remove panels from JobDetailLayout (D-05, HUB-02)</name>
  <files>frontend/src/layouts/JobDetailLayout.vue</files>
  <action>
    - Remove WarningsList import and template block.
    - Confirm no ExportPreview, ValidationPanel, PathBPanel in Layout.
    - child-shell router-view unchanged.
  </action>
  <verify>grep ValidationPanel JobDetailLayout — no match</verify>
</task>

</tasks>

<verification>
Manual: Hub shows warnings; table route does not. typecheck pass.
</verification>
