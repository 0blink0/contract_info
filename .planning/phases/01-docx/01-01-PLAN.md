---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contract_info/README.md
  - contract_info/requirements.txt
  - contract_info/.env.example
  - contract_info/docker-compose.yml
  - contract_info/.gitignore
  - contract_info/backend/__init__.py
  - contract_info/backend/app/__init__.py
  - contract_info/backend/app/config.py
  - contract_info/backend/app/db/session.py
  - contract_info/backend/app/models/contract_file.py
  - contract_info/backend/app/models/__init__.py
  - contract_info/alembic.ini
  - contract_info/alembic/env.py
  - contract_info/alembic/versions/001_contract_files.py
  - contract_info/uploads/.gitkeep
autonomous: true
requirements:
  - DOC-03
  - DEV-01
---

<objective>
交付 **contract_info** 开发骨架：venv 依赖、独立 PostgreSQL（docker-compose）、`contract_files` 表（Alembic 迁移）、SQLAlchemy 模型与 README 运维说明。
</objective>

<threat_model>
<trust_boundary>本机开发者 ↔ Docker Postgres（误配 DATABASE_URL 导致连错库）。</trust_boundary>
<stride_register>
- threat_id: T-01-01
  category: I
  component: docker-compose / .env
  disposition: mitigated
  mitigation_plan: .env.example 与 compose POSTGRES_PASSWORD 一致；README 说明卷初始化。
</stride_register>
</threat_model>

<must_haves>
- `docker compose up -d` 可启动 Postgres
- `alembic upgrade head` 创建 `contract_files`
- README 含 venv、`pip install -r requirements.txt`、迁移与 compose 命令
</must_haves>

<tasks>

<task id="01-01-01" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
    - contract_info/.planning/phases/01-docx/01-RESEARCH.md
  </read_first>
  <files>contract_info/requirements.txt, contract_info/.env.example, contract_info/.gitignore, contract_info/uploads/.gitkeep</files>
  <action>
创建 `requirements.txt`，至少包含：
`python-docx>=1.1.0`, `sqlalchemy>=2.0`, `alembic>=1.13`, `psycopg2-binary>=2.9`, `pydantic-settings>=2.0`, `pytest>=8.0`, `click>=8.1`.

创建 `.env.example`：
```
DATABASE_URL=postgresql+psycopg2://postgres:contract_info_dev@localhost:5433/contract_info
```

创建 `.gitignore`：`.venv/`, `__pycache__/`, `.env`, `uploads/*/`（保留 `uploads/.gitkeep`）。

创建 `uploads/.gitkeep`。
  </action>
  <verify>
    rg "python-docx" contract_info/requirements.txt
    rg "DATABASE_URL" contract_info/.env.example
    test -f contract_info/uploads/.gitkeep
  </verify>
  <acceptance_criteria>
    - requirements.txt 含上述包名
    - .env.example 端口 5433（与 compose 一致，避免与 bid_tool 5432 冲突）
  </acceptance_criteria>
</task>

<task id="01-01-02" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
    - ai_bid_management/bid_tool_agents/docs/troubleshooting-postgres-docker-password.md
  </read_first>
  <files>contract_info/docker-compose.yml</files>
  <action>
创建 `docker-compose.yml` 单服务 `postgres`：
- image: `postgres:16`
- ports: `5433:5432`
- environment: `POSTGRES_USER=postgres`, `POSTGRES_PASSWORD=contract_info_dev`, `POSTGRES_DB=contract_info`
- volume: `contract_info_pg_data`

服务名 `postgres`，healthcheck: `pg_isready -U postgres`.
  </action>
  <verify>
    rg "5433:5432" contract_info/docker-compose.yml
    rg "contract_info_dev" contract_info/docker-compose.yml
    rg "contract_info_pg_data" contract_info/docker-compose.yml
  </verify>
  <acceptance_criteria>
    - compose 文件可 `docker compose config` 无语法错误
    - 口令与 .env.example 中 DATABASE_URL 一致
  </acceptance_criteria>
</task>

<task id="01-01-03" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-RESEARCH.md
    - contract_info/backend/app/config.py
  </read_first>
  <files>
    contract_info/backend/__init__.py
    contract_info/backend/app/__init__.py
    contract_info/backend/app/config.py
    contract_info/backend/app/db/session.py
    contract_info/backend/app/models/contract_file.py
    contract_info/backend/app/models/__init__.py
  </files>
  <action>
实现 `Settings`（pydantic-settings）：`database_url: str` 从环境变量 `DATABASE_URL` 读取。

`db/session.py`：`engine`, `SessionLocal`, `get_db()` 上下文。

`models/contract_file.py` 表名 `contract_files`，列与 RESEARCH 一致：
`id` UUID PK, `filename`, `status` (默认 pending), `storage_path`, `parse_json` JSONB, `outline_preview` JSONB, `error_message`, `created_at`, `updated_at`（server default now）。

导出 `Base` 供 Alembic 使用。
  </action>
  <verify>
    rg "class ContractFile" contract_info/backend/app/models/contract_file.py
    rg "contract_files" contract_info/backend/app/models/contract_file.py
    rg "parse_json" contract_info/backend/app/models/contract_file.py
  </verify>
  <acceptance_criteria>
    - ContractFile 含 status、storage_path、parse_json、outline_preview
    - get_db 可 import
  </acceptance_criteria>
</task>

<task id="01-01-04" type="execute">
  <read_first>
    - contract_info/backend/app/models/contract_file.py
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
  </read_first>
  <files>contract_info/alembic.ini, contract_info/alembic/env.py, contract_info/alembic/versions/001_contract_files.py</files>
  <action>
初始化 Alembic（`alembic init alembic` 或手写等价结构），`env.py` 从 `backend.app.config` 读取 `DATABASE_URL`。

迁移 `001_contract_files.py`：`upgrade` 创建 `contract_files` 表（列与模型一致）；`downgrade` drop table。

`alembic.ini` 中 `script_location = alembic`。
  </action>
  <verify>
    rg "contract_files" contract_info/alembic/versions/001_contract_files.py
    rg "outline_preview" contract_info/alembic/versions/001_contract_files.py
  </verify>
  <acceptance_criteria>
    - 存在 001 迁移且含 jsonb 列
  </acceptance_criteria>
</task>

<task id="01-01-04b" type="execute">
  <read_first>
    - contract_info/docker-compose.yml
    - contract_info/.env.example
    - contract_info/alembic/versions/001_contract_files.py
  </read_first>
  <files>N/A (runtime)</files>
  <action>
**[BLOCKING] Schema push：** 在 compose 运行且 `.env` 配置后执行：
`cd contract_info && alembic upgrade head`
验证：`\dt` 或 SQLAlchemy 反射可见 `contract_files`。
  </action>
  <verify>
    alembic current
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` 退出码 0
    - 数据库存在 `contract_files` 表
  </acceptance_criteria>
</task>

<task id="01-01-05" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
    - contract_info/requirements.txt
    - contract_info/docker-compose.yml
  </read_first>
  <files>contract_info/README.md</files>
  <action>
编写 README（中文）章节：
1. 项目说明（CTRX Phase 1）
2. 创建 venv：`python -m venv .venv` 与 activate
3. `pip install -r requirements.txt`
4. 复制 `.env.example` → `.env`
5. `docker compose up -d` 与 `alembic upgrade head`
6. 指向 Phase 2+ 的 CLI/pytest（占位「见 01-02 计划」）

链接 Postgres 排障：`../ai_bid_management/bid_tool_agents/docs/troubleshooting-postgres-docker-password.md`
  </action>
  <verify>
    rg "python -m venv" contract_info/README.md
    rg "alembic upgrade head" contract_info/README.md
    rg "docker compose" contract_info/README.md
  </verify>
  <acceptance_criteria>
    - DEV-01：新开发者可按 README 完成 venv + DB
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `docker compose -f contract_info/docker-compose.yml up -d`
2. `cd contract_info && alembic upgrade head`
3. 各 task `<verify>` rg 通过
</verification>

<success_criteria>
DOC-03 数据模型与迁移就绪；DEV-01 README 完整。
</success_criteria>

## PLANNING COMPLETE
