---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - contract_info/backend/app/parse/schemas.py
  - contract_info/backend/app/parse/outline.py
  - contract_info/backend/app/parse/docx_parser.py
  - contract_info/backend/app/parse/__init__.py
  - contract_info/backend/app/services/parse_service.py
  - contract_info/backend/app/services/__init__.py
  - contract_info/backend/cli.py
  - contract_info/backend/tests/conftest.py
  - contract_info/backend/tests/test_outline.py
  - contract_info/backend/tests/test_docx_parser.py
  - contract_info/backend/tests/test_cli_persist.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DEV-02
---

<objective>
实现 **docx → Document JSON**（outline + blocks）、**CLI `parse`**（可选 `--persist` 入库），并对 `example/*.docx` 提供 **pytest** 验证。
</objective>

<threat_model>
<trust_boundary>本地 CLI 读取用户指定路径 docx（路径遍历需限制在显式参数，不自动执行宏）。</trust_boundary>
<stride_register>
- threat_id: T-01-02
  category: T
  component: backend/cli.py
  disposition: mitigated
  mitigation_plan: 仅读取用户传入路径；不执行 docx 宏；复制到 uploads 时用 uuid 子目录。
</stride_register>
</threat_model>

<must_haves>
- `python -m backend.cli parse example/<sample>.docx` 输出 outline/blocks 统计
- `--persist` 后 DB 有 `status=parsed` 且 `parse_json` 非空
- pytest 在 example 合同上 `blocks` 非空、`outline` 至少 1 条
</must_haves>

<tasks>

<task id="01-02-01" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
    - contract_info/.planning/phases/01-docx/01-RESEARCH.md
  </read_first>
  <files>contract_info/backend/app/parse/schemas.py</files>
  <action>
定义 Document JSON 契约（TypedDict 或 Pydantic）：
- `Document`: source_file, format="docx", metadata={}, outline: list[OutlineItem], blocks: list[Block]
- `OutlineItem`: anchor_id (sec_001…), title, level (int, default 1)
- `Block`: id (b_001/t_001), type paragraph|table, section_id, text OR rows

提供 `document_to_dict(doc) -> dict` 用于 jsonb 序列化。
  </action>
  <verify>
    rg "class Document|TypedDict" contract_info/backend/app/parse/schemas.py
    rg '"format"' contract_info/backend/app/parse/schemas.py
  </verify>
  <acceptance_criteria>
    - schemas 模块可 import，含 outline 与 blocks 类型
  </acceptance_criteria>
</task>

<task id="01-02-02" type="execute">
  <read_first>
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
    - contract_info/contract_keywords.txt
  </read_first>
  <files>contract_info/backend/app/parse/outline.py, contract_info/backend/tests/test_outline.py</files>
  <action>
实现 `detect_outline(paragraph_text: str) -> OutlineItem | None`，正则（RESEARCH）：
- `^第[零一二三四五六七八九十百]+章[：:\s]?(.*)$`
- `^[一二三四五六七八九十]+[、．.](.*)$`
- `^\d+[、．.](.*)$`

`build_outline(paragraphs) -> list[OutlineItem]`，anchor_id 递增 `sec_{n:03d}`。

pytest：样例字符串「四、私募基金的基本情况」匹配；「1.1 一般」不匹配或按设计处理。
  </action>
  <verify>
    cd contract_info && pytest backend/tests/test_outline.py -q
  </verify>
  <acceptance_criteria>
    - test_outline.py 全部通过
    - 至少 3 条正则用例
  </acceptance_criteria>
</task>

<task id="01-02-03" type="execute">
  <read_first>
    - contract_info/backend/app/parse/schemas.py
    - contract_info/backend/app/parse/outline.py
  </read_first>
  <files>contract_info/backend/app/parse/docx_parser.py, contract_info/backend/app/parse/__init__.py, contract_info/backend/tests/test_docx_parser.py</files>
  <action>
实现 `parse_docx(file_path: str) -> Document`：
1. python-docx 加载
2. 遍历 paragraphs：先尝试 outline，再 append paragraph block（带 current section_id）
3. 遍历 tables：append table block，rows 为 cell text 二维数组
4. metadata: paragraph_count, table_count

`__init__.py` 导出 `parse_docx`.

pytest（`conftest.py` 提供 `example_docx_path` glob `example/*.docx` 第一个）：
- `len(document.blocks) > 0`
- `len(document.outline) >= 1`（样例基金合同）
- 至少一个 block 含 `section_id` 非空
  </action>
  <verify>
    cd contract_info && pytest backend/tests/test_docx_parser.py -q
  </verify>
  <acceptance_criteria>
    - DOC-02：example docx 解析满足上述断言
    - test 跳过条件：无 example docx 时 pytest.skip
  </acceptance_criteria>
</task>

<task id="01-02-04" type="execute">
  <read_first>
    - contract_info/backend/app/models/contract_file.py
    - contract_info/backend/app/parse/docx_parser.py
    - contract_info/.planning/phases/01-docx/01-CONTEXT.md
  </read_first>
  <files>contract_info/backend/app/services/parse_service.py, contract_info/backend/cli.py</files>
  <action>
`parse_service.persist_parse(file_path: str) -> UUID`：
1. uuid4 生成 id
2. `shutil.copy` 到 `uploads/{id}/` 保留原文件名
3. 插入 ContractFile(status=parsing, storage_path=相对路径, filename=basename)
4. 调用 parse_docx；写入 parse_json、outline_preview（outline 投影）、status=parsed
5. 异常 → status=failed, error_message=str(e)

`cli.py` Click 命令：
```
@click.command()
@click.argument("docx_path", type=click.Path(exists=True))
@click.option("--persist", is_flag=True)
@click.option("--out", type=click.Path(), default=None)
def parse(docx_path, persist, out):
```
无 persist 时打印 JSON 摘要；有 persist 时打印 file id。

包入口：`python -m backend.cli`（`backend/__main__.py` 或 cli 模块）。
  </action>
  <verify>
    rg "@click.command" contract_info/backend/cli.py
    rg "persist" contract_info/backend/cli.py
    rg "def persist_parse" contract_info/backend/app/services/parse_service.py
  </verify>
  <acceptance_criteria>
    - DOC-01：CLI 接受 docx 路径参数
    - DOC-03：--persist 写入 contract_files
  </acceptance_criteria>
</task>

<task id="01-02-05" type="tdd">
  <read_first>
    - contract_info/backend/cli.py
    - contract_info/backend/app/services/parse_service.py
    - contract_info/.planning/phases/01-docx/01-VALIDATION.md
  </read_first>
  <files>contract_info/backend/tests/test_cli_persist.py, contract_info/backend/tests/conftest.py</files>
  <action>
`conftest.py`：fixture `db_session`（测试库或 skip 若无 DATABASE_URL）；fixture 清理插入行。

`test_cli_persist.py`：
- 标记 `@pytest.mark.integration`
- 调用 `persist_parse(example_docx)` 
- 断言 DB 行 status==parsed, parse_json is not None, outline_preview is not None

README 补充：
```
python -m backend.cli parse example/<file>.docx --persist
pytest backend/tests -q
```
  </action>
  <verify>
    cd contract_info && pytest backend/tests/test_cli_persist.py -q -m integration
  </verify>
  <acceptance_criteria>
    - DEV-02：集成测试在 DB 可用时通过；无 DB 时 skip 并文档说明
    - 全量 `pytest backend/tests -q` 单元测试始终可跑
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `pytest backend/tests/test_outline.py backend/tests/test_docx_parser.py -q`
2. `docker compose up -d && alembic upgrade head`
3. `python -m backend.cli parse example/*.docx --persist`（替换为实际文件名）
4. DEV-02 satisfied
</verification>

<success_criteria>
Phase 1 成功标准（ROADMAP）全部满足：venv 文档、example 解析 JSON、contract_files 入库、pytest/CLI 通过。
</success_criteria>

## PLANNING COMPLETE
