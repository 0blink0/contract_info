---
phase: 22-rag-llm
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/types/ipc.ts
  - electron/store.ts
  - electron/main.ts
  - electron/tests/settings-rag-topk.test.mjs
autonomous: true
requirements:
  - KB-RAG-04
must_haves:
  truths:
    - "设置页保存 `RAG Top-K` 后，重启后端进程会携带新值生效（per D-07）"
    - "`RAG Top-K` 在配置层被限制为 1-10 的整数，非法值会被阻断（per D-08）"
  artifacts:
    - path: "electron/types/ipc.ts"
      provides: "AppSettings 新增 ragTopK 类型契约"
      exports: ["AppSettings"]
    - path: "electron/store.ts"
      provides: "ragTopK 默认值与范围校验持久化"
      exports: ["validateSettings", "loadSettings", "saveSettings"]
    - path: "electron/main.ts"
      provides: "重启后端进程时注入 RAG_TOP_K 环境变量"
      exports: ["backendChildEnv"]
    - path: "electron/tests/settings-rag-topk.test.mjs"
      provides: "ragTopK 校验与后端注入回归测试"
      contains: "RAG_TOP_K"
  key_links:
    - from: "electron/store.ts"
      to: "electron/main.ts"
      via: "loadSettings -> backendChildEnv"
      pattern: "RAG_TOP_K"
    - from: "electron/tests/settings-rag-topk.test.mjs"
      to: "electron/store.ts"
      via: "测试 validate/save/load ragTopK 约束"
      pattern: "ragTopK"
---

<objective>
为 RAG Top-K 提供运行时配置主链路：类型定义、持久化校验、后端重启注入，确保配置可保存且可生效。

Purpose: 先落地 KB-RAG-04 的配置基础设施，避免后续后端/前端实现出现配置悬空。
Output: Electron 配置链路支持 ragTopK，含自动化测试门禁。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/REQUIREMENTS.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-CONTEXT.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-RESEARCH.md
@d:/bid_management/contract_info/electron/types/ipc.ts
@d:/bid_management/contract_info/electron/store.ts
@d:/bid_management/contract_info/electron/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：扩展 Electron 设置契约与默认值</name>
  <files>electron/types/ipc.ts, electron/store.ts</files>
  <behavior>
    - Test 1: 未提供 `ragTopK` 时，读取设置返回默认值 3（per D-08）。
    - Test 2: `ragTopK` 小于 1 或大于 10 时，校验失败并阻断保存（per D-08）。
  </behavior>
  <action>在 `AppSettings` 增加 `ragTopK` 字段；在 `DEFAULT_SETTINGS` 中设默认值 `3`；`validateSettings` 增加整数与范围校验（1-10，非法值报可读错误）。保持现有 LLM 配置校验逻辑不回退，并显式复用 `22-PATTERNS.md` 中 `electron/types/ipc.ts`、`electron/store.ts` analog 与 Shared Pattern 3（双层校验）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && node --test electron/tests/settings-rag-topk.test.mjs</automated>
  </verify>
  <done>配置模型支持 ragTopK 且非法输入在 store 层被拒绝。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：将 ragTopK 注入后端子进程环境</name>
  <files>electron/main.ts</files>
  <behavior>
    - Test 1: `backendChildEnv` 包含 `RAG_TOP_K`，值来自已保存设置（per D-07）。
    - Test 2: 保存设置后触发重启路径时仍沿用现有回滚语义，不跳过重启（per D-07）。
  </behavior>
  <action>扩展 `backendChildEnv`，把 `settings.ragTopK` 规范化为字符串后注入 `RAG_TOP_K`。不引入热更新，不改变“保存后重启生效”机制（per D-07）；实现方式对齐 `22-PATTERNS.md` 中 `electron/main.ts` analog 与 Shared Pattern 4。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && node --test electron/tests/settings-rag-topk.test.mjs</automated>
  </verify>
  <done>后端重启后可读取到最新 RAG_TOP_K 环境变量。</done>
</task>

<task type="auto">
  <name>任务 3：补齐 Electron 端 ragTopK 回归测试</name>
  <files>electron/tests/settings-rag-topk.test.mjs</files>
  <action>新增/扩展测试覆盖：默认值为 3、越界值拒绝保存、`backendChildEnv` 注入 `RAG_TOP_K`、保存设置后仍走重启流程。测试保持无 GUI 依赖，执行时间控制在 60 秒内；断言风格对齐 `22-PATTERNS.md` 中 `electron/tests/settings-restart.test.mjs` 的 role-match 模式。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && node --test electron/tests/settings-rag-topk.test.mjs</automated>
  </verify>
  <done>Electron 侧 ragTopK 的持久化与生效链路有自动化回归保障。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings input->electron store | 来自 UI 的配置值进入本地持久化层 |
| electron->backend process env | 本地配置跨进程传递到后端运行时 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `electron/store.ts` | mitigate | 对 `ragTopK` 强制整数且范围 1-10（per D-08） |
| T-22-02 | Denial of Service | `electron/main.ts` env 注入 | mitigate | 限制 Top-K 上界并复用重启回滚流程，避免异常值放大 prompt |
| T-22-03 | Repudiation | `saveSettings` 失败场景 | accept | 沿用既有错误返回结构，失败可追踪 reason |
</threat_model>

<verification>
cd /d d:\bid_management\contract_info && node --test electron/tests/settings-rag-topk.test.mjs
</verification>

<success_criteria>
- `ragTopK` 成为 `AppSettings` 一部分并持久化（默认 3）
- 越界或非法 Top-K 在 Electron 设置层被拒绝（1-10）
- 保存设置后后端重启使用最新 `RAG_TOP_K`（D-07）
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-22-rag-llm/22-01-SUMMARY.md`
</output>
