---
phase: 22-rag-llm
plan: "03"
type: execute
wave: 3
depends_on:
  - 22-02-PLAN.md
files_modified:
  - frontend/src/components/LlmConfigForm.vue
  - frontend/src/views/SettingsView.vue
  - frontend/src/stores/appBootstrap.ts
  - frontend/tests/frontend/llm-config-form.test.mjs
autonomous: true
requirements:
  - KB-RAG-04
must_haves:
  truths:
    - "Settings 页面可编辑 `RAG Top-K`，默认值为 3（KB-RAG-04）"
    - "输入约束为 1-10 整数，非法值在表单层阻断并给出提示（per D-08）"
    - "保存后沿用现有“重启后端生效”交互，不做热更新（per D-07）"
  artifacts:
    - path: "frontend/src/components/LlmConfigForm.vue"
      provides: "RAG Top-K 表单项与校验状态"
      contains: "el-input-number :min=1 :max=10"
    - path: "frontend/src/views/SettingsView.vue"
      provides: "保存 payload 携带 ragTopK"
      exports: ["save"]
    - path: "frontend/src/stores/appBootstrap.ts"
      provides: "ragTopK 启动默认值与草稿同步"
      exports: ["EMPTY_SETTINGS"]
    - path: "frontend/tests/frontend/llm-config-form.test.mjs"
      provides: "ragTopK 前端约束回归测试"
      contains: "ragTopK"
  key_links:
    - from: "frontend/src/components/LlmConfigForm.vue"
      to: "frontend/src/views/SettingsView.vue"
      via: "v-model AppSettings.ragTopK"
      pattern: "update:modelValue"
    - from: "frontend/src/views/SettingsView.vue"
      to: "electron/types/ipc.ts"
      via: "saveSettings payload"
      pattern: "ragTopK"
    - from: "frontend/src/stores/appBootstrap.ts"
      to: "frontend/src/views/SettingsView.vue"
      via: "loadSettings 默认值回填"
      pattern: "EMPTY_SETTINGS"
---

<objective>
完成 Settings 前端可视化配置与保存链路接入，使用户可安全配置 RAG Top-K 并通过重启机制生效。

Purpose: 闭合 KB-RAG-04 的用户交互层，确保配置项“可见、可改、可验证”。
Output: Settings 表单新增 RAG Top-K、保存链路接通、前端回归测试通过。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/REQUIREMENTS.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-CONTEXT.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-RESEARCH.md
@d:/bid_management/contract_info/frontend/src/components/LlmConfigForm.vue
@d:/bid_management/contract_info/frontend/src/views/SettingsView.vue
@d:/bid_management/contract_info/frontend/src/stores/appBootstrap.ts

<interfaces>
From electron/types/ipc.ts:
```typescript
export interface AppSettings extends LlmSettings {
  temperature?: number;
  ragTopK?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：在 LLM 配置表单新增 RAG Top-K 输入</name>
  <files>frontend/src/components/LlmConfigForm.vue</files>
  <behavior>
    - Test 1: 组件渲染 `ragTopK` 输入，默认显示 3（KB-RAG-04）。
    - Test 2: 小于 1 或大于 10 时组件 valid 状态为 false（per D-08）。
  </behavior>
  <action>在 `LlmConfigForm` 增加 `ragTopK` 的 `el-input-number`，`min=1`、`max=10`、`step=1`，并并入现有 `valid` 计算逻辑。保持既有温度等字段校验行为不变；输入控件与校验实现显式对齐 `22-PATTERNS.md` Shared Pattern 3（表单与配置双层校验）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend -- llm-config-form.test.mjs</automated>
  </verify>
  <done>表单层可输入并校验 ragTopK，非法输入被阻断。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：接入 Settings 保存与启动默认值链路</name>
  <files>frontend/src/views/SettingsView.vue, frontend/src/stores/appBootstrap.ts</files>
  <behavior>
    - Test 1: loadSettings 结果可回填 ragTopK。
    - Test 2: saveSettings payload 含 ragTopK，且仍触发保存后重启流程（per D-07）。
  </behavior>
  <action>在 `SettingsView` 的加载/保存映射中加入 `ragTopK`；在 `appBootstrap` 的 `EMPTY_SETTINGS` 和 merge 逻辑中补齐该字段默认值 3。禁止引入“无需重启立即生效”的新逻辑（per D-07）；保存后生效链路按 `22-PATTERNS.md` Shared Pattern 4（保存后重启生效）实现。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend</automated>
  </verify>
  <done>设置加载、保存、重启链路完整包含 ragTopK 且默认值稳定。</done>
</task>

<task type="auto">
  <name>任务 3：扩展前端配置回归测试</name>
  <files>frontend/tests/frontend/llm-config-form.test.mjs</files>
  <action>扩展现有表单测试：覆盖 ragTopK 默认值、边界值（1/10）与越界值（0/11）校验结果；断言 `update:modelValue` 输出含 ragTopK。测试应保持无后端依赖，并以 `22-PATTERNS.md` 中 `LlmConfigForm.vue`/`appBootstrap.ts` analog 的断言风格保持一致。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend -- llm-config-form.test.mjs</automated>
  </verify>
  <done>ragTopK 前端交互与约束具备稳定自动化测试保障。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input->frontend form | 用户输入进入配置表单约束 |
| frontend payload->electron save API | 表单值跨 IPC 进入持久化层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Tampering | `LlmConfigForm.vue` | mitigate | `el-input-number` + 计算属性校验双重限制 1-10（per D-08） |
| T-22-09 | Denial of Service | `SettingsView.vue` payload | mitigate | 保存前阻断越界值，避免异常 Top-K 进入后端 |
| T-22-10 | Repudiation | 设置保存反馈 | accept | 复用现有保存成功/失败消息机制，保留可追踪反馈 |
</threat_model>

<verification>
cd /d d:\bid_management\contract_info && npm run test:frontend --prefix frontend -- llm-config-form.test.mjs
</verification>

<success_criteria>
- Settings 页面新增 `RAG Top-K`，默认 3，范围 1-10（KB-RAG-04）
- 保存流程携带 ragTopK 且保持“重启后生效”语义（D-07）
- 前端测试覆盖 ragTopK 的默认值与边界约束
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-22-rag-llm/22-03-SUMMARY.md`
</output>
