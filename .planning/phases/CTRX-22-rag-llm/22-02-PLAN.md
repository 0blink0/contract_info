---
phase: 22-rag-llm
plan: "02"
type: execute
wave: 2
depends_on:
  - 22-01-PLAN.md
files_modified:
  - backend/app/config.py
  - backend/app/services/kb_service.py
  - backend/app/extract/llm/chapter_prompts.py
  - backend/app/extract/llm/performance_fee.py
  - backend/app/extract/pipeline.py
  - backend/tests/test_rag_prompt_injection.py
autonomous: true
requirements:
  - KB-RAG-01
  - KB-RAG-02
  - KB-RAG-03
must_haves:
  truths:
    - "仅在业绩报酬提取链路触发语义检索，不扩展到 PathB 其它字段（per D-01）"
    - "RAG 查询文本优先来源于业绩报酬相关上下文，不引入多字段拼接策略（per D-02）"
    - "prompt 注入案例为按相似度排序的紧凑列表，结构固定字段名/字段值/原文摘录，且不注入分数（per D-03, D-04）"
    - "知识库为空或不可用时不注入 few-shot 块且流程不报错，不输出占位提示（per D-05, D-06）"
  artifacts:
    - path: "backend/app/services/kb_service.py"
      provides: "query->Top-K 相似案例检索接口"
      exports: ["KbService.search_similar_entries"]
    - path: "backend/app/extract/pipeline.py"
      provides: "业绩报酬提取前 RAG 检索编排"
      contains: "仅 fees 分支注入"
    - path: "backend/app/extract/llm/performance_fee.py"
      provides: "few-shot 案例块注入与空库降级"
      exports: ["extract_performance_fee_section_llm"]
    - path: "backend/tests/test_rag_prompt_injection.py"
      provides: "RAG 注入与空库降级回归测试"
      contains: "top-k and empty kb"
  key_links:
    - from: "backend/app/extract/pipeline.py"
      to: "backend/app/services/kb_service.py"
      via: "fees 分支调用 search_similar_entries"
      pattern: "search_similar_entries"
    - from: "backend/app/extract/pipeline.py"
      to: "backend/app/extract/llm/performance_fee.py"
      via: "将 rag cases 传入 performance fee 提取"
      pattern: "extract_performance_fee_section_llm"
    - from: "backend/app/extract/llm/performance_fee.py"
      to: "backend/app/extract/llm/chapter_prompts.py"
      via: "复用 prompt 片段拼接模板"
      pattern: "历史案例参考"
---

<objective>
在后端抽取链路中落地 RAG 检索与 few-shot 注入，确保仅作用于业绩报酬场景，且在空库时稳定降级。

Purpose: 完成 KB-RAG-01/02/03 的核心价值交付，提升业绩报酬字段提取稳定性而不破坏现有流程。
Output: 后端 RAG 检索+注入实现、空库降级策略、自动化回归测试。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/bid_management/contract_info/.planning/ROADMAP.md
@d:/bid_management/contract_info/.planning/REQUIREMENTS.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-CONTEXT.md
@d:/bid_management/contract_info/.planning/phases/CTRX-22-rag-llm/22-RESEARCH.md
@d:/bid_management/contract_info/backend/app/services/kb_service.py
@d:/bid_management/contract_info/backend/app/extract/pipeline.py
@d:/bid_management/contract_info/backend/app/extract/llm/performance_fee.py
@d:/bid_management/contract_info/backend/app/extract/llm/chapter_prompts.py

<interfaces>
From backend/app/services/kb_service.py:
```python
class KbService:
    async def add_entries(self, items: list[dict[str, str]]) -> list[str]: ...
    def list_entries(self, field_name: str | None = None, page: int = 1, page_size: int = 20) -> tuple[list[dict[str, Any]], int]: ...
```

From backend/app/extract/llm/performance_fee.py:
```python
async def extract_performance_fee_section_llm(client: Any, text: str) -> tuple[dict[str, str], bool, list[ExtractionWarning]]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：补齐 RAG 注入行为测试（先测后改）</name>
  <files>backend/tests/test_rag_prompt_injection.py</files>
  <behavior>
    - Test 1: fees 分支触发检索并将最多 Top-K 条案例注入 performance fee prompt（per D-01, D-03）。
    - Test 2: 注入块仅包含字段名/字段值/原文摘录且按排序输出，不出现相似度分数（per D-04）。
    - Test 3: 空库/检索不可用时不注入案例块且流程继续成功（per D-05, D-06）。
  </behavior>
  <action>新增测试文件，使用 mock KbService 与 mock LLM client 验证 pipeline 到 prompt 的链路。测试应显式断言：只在 `fees` 路径触发检索（D-01）、query 文本来自业绩报酬上下文（D-02）、空结果不报错（D-05）；测试组织与断言风格对齐 `22-PATTERNS.md` 中 `backend/tests/test_extract_pipeline.py` 的 role-match analog。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_rag_prompt_injection.py -q</automated>
  </verify>
  <done>测试可稳定复现 RAG 注入与空库降级行为，并在改动前至少有失败断言。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：实现 kb_service 检索与 pipeline 注入编排</name>
  <files>backend/app/config.py, backend/app/services/kb_service.py, backend/app/extract/pipeline.py</files>
  <behavior>
    - Test 1: `search_similar_entries(query, k)` 使用 LanceDB search limit 返回最多 k 条结构化案例（KB-RAG-01）。
    - Test 2: pipeline 仅在 performance_fee 链路调用检索并传给下游 LLM（per D-01）。
    - Test 3: kb 不可用或空结果时返回空案例，不中断提取主流程（KB-RAG-03）。
  </behavior>
  <action>在 `config.py` 增加 `rag_top_k`（默认 3，且运行时夹逼 1-10）；`kb_service.py` 增加检索函数，复用现有 embedding 语义模板；`pipeline.py` 在 fees 分支中构造 query 并取 Top-K，失败时记录 warning 后传空案例继续执行。禁止把检索扩散到其它字段路径（per D-01）；实现时分别复用 `22-PATTERNS.md` Shared Pattern 2（Pipeline 非阻塞集成）与 Shared Pattern 3（参数边界双层约束）。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_rag_prompt_injection.py -q</automated>
  </verify>
  <done>pipeline 在业绩报酬路径可获得 Top-K 案例，并对空库/不可用场景自动降级。</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：实现 few-shot prompt 注入模板</name>
  <files>backend/app/extract/llm/chapter_prompts.py, backend/app/extract/llm/performance_fee.py</files>
  <behavior>
    - Test 1: 有案例时 user prompt 含“历史案例参考”块，条目结构固定字段名/字段值/原文摘录（KB-RAG-02, per D-03）。
    - Test 2: prompt 中不出现 `score` 或相似度百分比字段（per D-04）。
    - Test 3: 无案例时不拼接案例块、不拼占位文本（per D-05, D-06）。
  </behavior>
  <action>在 prompt 构造层新增 RAG 片段生成函数，保持文本紧凑、可预测；`performance_fee.py` 接收可选案例列表并按 D-03/D-04 注入。保持原有 JSON 输出约束与重试逻辑不变，避免影响现有抽取协议；模板拼接与降级路径显式对齐 `22-PATTERNS.md` Shared Pattern 1（LLM 调用与降级）及 `chapter_prompts.py` analog。</action>
  <verify>
    <automated>cd /d d:\bid_management\contract_info && pytest backend/tests/test_rag_prompt_injection.py -q</automated>
  </verify>
  <done>few-shot 注入格式稳定且符合 D-03~D-06，空库时无噪声文本。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text->rag query | 合同文本是非可信输入，进入检索与提示词 |
| kb records->llm prompt | 历史案例内容进入 LLM 上下文，存在提示污染风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | `performance_fee.py` prompt 注入 | mitigate | 注入内容仅允许字段名/字段值/原文摘录三元组（per D-03）并保持模板化 |
| T-22-05 | Information Disclosure | `chapter_prompts.py` 组装逻辑 | accept | 不新增敏感字段，仅复用已存在案例字段集合 |
| T-22-06 | Denial of Service | `pipeline.py` / `kb_service.py` | mitigate | Top-K 受 `rag_top_k` 1-10 边界控制（per D-08） |
| T-22-07 | Repudiation | 空库降级路径 | mitigate | 检索失败仅记录 warning 并可追踪，不中断主流程（per D-05） |
</threat_model>

<verification>
cd /d d:\bid_management\contract_info && pytest backend/tests/test_rag_prompt_injection.py -q
</verification>

<success_criteria>
- 抽取前触发 Top-K 检索并把案例注入业绩报酬 prompt（KB-RAG-01/02）
- 注入案例格式固定且不含相似度分数字段（D-03/D-04）
- 空库/不可用场景无错误并自动回落原流程（KB-RAG-03）
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-22-rag-llm/22-02-SUMMARY.md`
</output>
