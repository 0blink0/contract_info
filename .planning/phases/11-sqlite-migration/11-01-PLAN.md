---
phase: 11-sqlite-migration
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_db_migration.py
  - backend/tests/test_path_resolution.py
  - backend/tests/test_db_wal.py
  - backend/tests/test_desktop_main.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04

must_haves:
  truths:
    - "All 10 DB-01..DB-04 test cases exist and FAIL before implementation begins (RED state confirmed)"
    - "test_db_migration.py covers SQLite upgrade head + JSON roundtrip"
    - "test_path_resolution.py covers data_dir() env var, fallback, and _bundle_base() frozen mock"
    - "test_db_wal.py covers WAL pragma and concurrent writes"
    - "test_desktop_main.py covers fresh-DB migration and idempotency"
  artifacts:
    - path: "backend/tests/test_db_migration.py"
      provides: "DB-01a (migration), DB-01b (json roundtrip)"
    - path: "backend/tests/test_path_resolution.py"
      provides: "DB-02a (env), DB-02b (fallback), DB-02c (frozen)"
    - path: "backend/tests/test_db_wal.py"
      provides: "DB-03a (WAL pragma), DB-03b (concurrent writes)"
    - path: "backend/tests/test_desktop_main.py"
      provides: "DB-04a (fresh DB), DB-04b (idempotent)"
  key_links:
    - from: "test_db_migration.py"
      to: "alembic upgrade head"
      via: "programmatic Config with sqlite:///tmp/test.db"
    - from: "test_path_resolution.py"
      to: "backend.app.config.data_dir / _bundle_base"
      via: "monkeypatch env + sys.frozen mock"
    - from: "test_desktop_main.py"
      to: "desktop_main.run_migrations"
      via: "function call with tmp_path SQLite URL"
---

<objective>
Create the four Wave-0 test files that cover all 10 DB-01..DB-04 test cases. These tests must be written
in RED state (failing) BEFORE plan 11-02..11-04 touch any production code. The RED gate proves each test
actually validates the implementation it is designed to cover.

Purpose: Establish an automated safety net that fails right now and passes only after the correct
implementation is in place. Prevents false-green tests from masking broken migrations.

Output: Four test files in backend/tests/, all in RED state (import errors or assertion failures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-sqlite-migration/11-RESEARCH.md
@.planning/phases/11-sqlite-migration/11-VALIDATION.md
@backend/tests/conftest.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test files for DB-01 and DB-02</name>
  <files>
    backend/tests/test_db_migration.py
    backend/tests/test_path_resolution.py
  </files>
  <read_first>
    backend/tests/conftest.py
    .planning/phases/11-sqlite-migration/11-RESEARCH.md (sections: DB-01, DB-02, Code Examples 1-4)
    .planning/phases/11-sqlite-migration/11-VALIDATION.md
    alembic/versions/001_contract_files.py
    alembic/versions/002_extraction_columns.py
    backend/app/config.py
  </read_first>
  <behavior>
    test_db_migration.py:
    - test_sqlite_migration_all_revisions: create a temp SQLite DB via alembic.config.Config(),
      set script_location to alembic/, set sqlalchemy.url to sqlite:///tmp/path/test.db,
      call alembic.command.upgrade(cfg, "head"), assert no exception is raised and
      all 7 revision IDs (001..007) are present in alembic_version table
    - test_json_roundtrip: after migration, INSERT a ContractFile row with extraction_result={"key":"val"},
      SELECT it back using sa engine, assert type(row.extraction_result) is dict and
      row.extraction_result["key"] == "val" (not a JSON string)

    test_path_resolution.py:
    - test_uploads_dir_uses_env(tmp_path, monkeypatch): set CTRX_DATA_DIR=str(tmp_path),
      call get_settings.cache_clear(), call uploads_dir(), assert result == tmp_path / "uploads"
    - test_uploads_dir_fallback(monkeypatch): delenv CTRX_DATA_DIR, call get_settings.cache_clear(),
      assert uploads_dir() == PROJECT_ROOT / "uploads"
    - test_bundle_base_frozen(monkeypatch, tmp_path): monkeypatch sys.frozen=True and
      sys._MEIPASS=str(tmp_path), importlib.reload backend.app.config,
      assert cfg._bundle_base() == tmp_path

    Both files must FAIL (RED) before any production code changes:
    - test_db_migration.py will fail because postgresql.UUID causes ImportError without psycopg2,
      or migration 001 fails with "no such function: now()"
    - test_path_resolution.py will fail because data_dir() and _bundle_base() do not yet exist in config.py
  </behavior>
  <action>
    Create backend/tests/test_db_migration.py with two test functions. Import pattern:
    import pytest, tempfile, uuid from std; from alembic.config import Config; from alembic import command;
    from sqlalchemy import create_engine, text; from backend.app.models.contract_file import ContractFile.
    Use tmp_path fixture for the DB file path. Build Config() with no file argument (not from alembic.ini).
    Set "script_location" to str(Path(__file__).resolve().parents[2] / "alembic").
    Set "sqlalchemy.url" to f"sqlite:///{tmp_path}/test.db". Call command.upgrade(cfg, "head").
    For test_json_roundtrip: create engine with the same sqlite URL, use engine.connect() to INSERT
    a row then SELECT it, assert extraction_result returns dict.

    Create backend/tests/test_path_resolution.py with three test functions. Import pattern:
    import importlib, sys, os; import pytest; from pathlib import Path.
    For test_uploads_dir_uses_env: monkeypatch.setenv("CTRX_DATA_DIR", str(tmp_path)), then
    import and call get_settings.cache_clear(), then import uploads_dir from backend.app.config,
    call it, assert result == tmp_path / "uploads".
    For test_uploads_dir_fallback: monkeypatch.delenv("CTRX_DATA_DIR", raising=False),
    cache_clear(), import PROJECT_ROOT and uploads_dir, assert uploads_dir() == PROJECT_ROOT / "uploads".
    For test_bundle_base_frozen: monkeypatch sys attrs, then importlib.reload(backend.app.config),
    assert cfg._bundle_base() == tmp_path. Per RESEARCH.md Code Example 4.

    Do NOT add conftest.py markers that skip on missing DATABASE_URL — these tests use tmp_path
    SQLite, not the real database.
  </action>
  <verify>
    <automated>cd "d:/bid_management/contract_info" && python -m pytest backend/tests/test_db_migration.py backend/tests/test_path_resolution.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_db_migration.py exists and contains "test_sqlite_migration_all_revisions" and "test_json_roundtrip"
    - backend/tests/test_path_resolution.py exists and contains "test_uploads_dir_uses_env", "test_uploads_dir_fallback", "test_bundle_base_frozen"
    - pytest output shows these tests FAIL (not ERROR due to syntax), confirming RED state; acceptable failures: ImportError on postgresql dialect, AttributeError on missing data_dir/uploads_dir/exports_dir/_bundle_base, or OperationalError on "now()" in SQLite DDL
    - No test has a bare "pass" body — each must have at least one assert or raises block
  </acceptance_criteria>
  <done>Both test files exist with all 5 test functions; running pytest shows failures (not syntax errors), confirming RED state before production code changes.</done>
</task>

<task type="tdd">
  <name>Task 2: Write RED test files for DB-03 and DB-04</name>
  <files>
    backend/tests/test_db_wal.py
    backend/tests/test_desktop_main.py
  </files>
  <read_first>
    .planning/phases/11-sqlite-migration/11-RESEARCH.md (sections: DB-03, DB-04)
    .planning/phases/11-sqlite-migration/11-VALIDATION.md
    backend/tests/conftest.py
    backend/tests/test_db_migration.py  (for consistent import patterns just written)
  </read_first>
  <behavior>
    test_db_wal.py:
    - test_wal_mode_enabled(tmp_path): create SQLite engine pointing to tmp_path/wal_test.db,
      with the WAL event listener from session.py applied (import engine and run a connect event),
      execute "PRAGMA journal_mode" via a raw connection, assert result == "wal"
    - test_concurrent_writes(tmp_path): run 3 threads via concurrent.futures.ThreadPoolExecutor,
      each calling a helper that opens a NEW sqlite3 connection (not shared), INSERT one row,
      commit; assert all 3 futures complete without OperationalError (use as_completed + timeout=10)

    test_desktop_main.py:
    - test_migrations_fresh_db(tmp_path): call desktop_main.run_migrations(db_url, alembic_dir)
      where db_url = f"sqlite:///{tmp_path}/fresh.db" and alembic_dir = Path("alembic"),
      then connect to fresh.db, execute "SELECT name FROM sqlite_master WHERE type='table'",
      assert "contract_files" in results
    - test_migrations_idempotent(tmp_path): call run_migrations twice on same db_url/alembic_dir,
      assert second call completes without exception (no duplicate-table error)

    Both files must FAIL (RED) before production code:
    - test_db_wal.py will fail because session.py has no WAL event listener yet
    - test_desktop_main.py will fail because desktop_main.py does not exist yet
  </behavior>
  <action>
    Create backend/tests/test_db_wal.py. Import: concurrent.futures, sqlite3, pytest, Path, sqlalchemy.
    For test_wal_mode_enabled: create_engine with sqlite URL, attach a @event.listens_for(engine, "connect")
    that runs PRAGMA journal_mode=WAL — this is the same code that session.py WILL have after Plan 11-04
    is executed; the test is self-contained and tests the pragma fires correctly.
    Connect, run "PRAGMA journal_mode", check result. The test RED state is confirmed by absence of WAL
    event listener in production session.py: import the real engine from backend.app.db.session,
    get a raw connection, check PRAGMA — will return "delete" (not "wal") until Plan 11-04 is done.
    So the test should use the real engine from session.py (not a standalone engine) to confirm RED.
    For test_concurrent_writes: create standalone SQLite test engine with WAL applied, run 3 threads
    each doing insert+commit, assert no lock OperationalError.

    Create backend/tests/test_desktop_main.py. Import: pytest, Path, sys.
    For both tests: import desktop_main at function scope (to get ImportError when file absent, not at
    collection time). Use try/import inside test body: "from desktop_main import run_migrations".
    alembic_dir = Path(__file__).resolve().parents[2] / "alembic".
    Assert "contract_files" in table names after first call.
    Assert second call does not raise.

    desktop_main.py does not yet exist — tests will fail with ImportError (RED confirmed).
  </action>
  <verify>
    <automated>cd "d:/bid_management/contract_info" && python -m pytest backend/tests/test_db_wal.py backend/tests/test_desktop_main.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_db_wal.py exists and contains "test_wal_mode_enabled" and "test_concurrent_writes"
    - backend/tests/test_desktop_main.py exists and contains "test_migrations_fresh_db" and "test_migrations_idempotent"
    - pytest output shows RED failures: test_db_wal.py fails because real engine has no WAL listener (PRAGMA returns "delete"); test_desktop_main.py fails with ImportError (desktop_main.py absent)
    - All 4 test functions have at least one assert statement (no pass-only bodies)
  </acceptance_criteria>
  <done>All 4 test files exist with 10 total test cases; pytest output shows RED failures with meaningful assertion/import errors, not collection errors or syntax problems.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env → real filesystem | Tests use tmp_path (pytest-managed); no real DB affected |
| test imports → production modules | Importing backend.app.config triggers module-level PROJECT_ROOT evaluation; must guard against stale lru_cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | test_path_resolution — sys.frozen monkeypatching | accept | Monkeypatching sys attributes is test-local; test isolation via pytest fixtures prevents cross-test contamination |
| T-11-02 | Denial of Service | test_concurrent_writes ThreadPoolExecutor | accept | Test uses timeout=10 to prevent infinite hang; 3 threads against tmp_path DB are low-risk |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — package legitimacy audit confirms no installs |
</threat_model>

<verification>
All 10 test functions exist across 4 files:
- backend/tests/test_db_migration.py: test_sqlite_migration_all_revisions, test_json_roundtrip
- backend/tests/test_path_resolution.py: test_uploads_dir_uses_env, test_uploads_dir_fallback, test_bundle_base_frozen
- backend/tests/test_db_wal.py: test_wal_mode_enabled, test_concurrent_writes
- backend/tests/test_desktop_main.py: test_migrations_fresh_db, test_migrations_idempotent

RED gate: `pytest backend/tests/test_db_migration.py backend/tests/test_path_resolution.py backend/tests/test_db_wal.py backend/tests/test_desktop_main.py -x -q` — ALL fail (RED state).
</verification>

<success_criteria>
- All 4 test files created in backend/tests/
- All 10 test functions contain meaningful assertions (not just pass)
- pytest shows failures (RED), not collection errors
- No test silently passes due to skips or missing imports absorbed at module level
</success_criteria>

<output>
Create .planning/phases/11-sqlite-migration/11-01-SUMMARY.md when done
</output>
