---
phase: "08-path-b-json"
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - contract_info/backend/app/api/routes/jobs.py
  - contract_info/backend/app/api/schemas.py
  - contract_info/backend/tests/test_api_path_b.py
  - contract_info/backend/app/extract/llm/path_b_extract.py
  - contract_info/backend/tests/test_path_b_llm.py
autonomous: true
requirements:
  - PATHB-04
user_setup: []

must_haves:
  truths:
    - "GET /jobs/{id}/path-b 在 extracted 状态返回完整 JSON"
    - "parsed 状态返回 409"
    - "JobDetail 含 path_b_available 摘要"
    - "无 path_b_json 时 GET 返回 404"
  artifacts:
    - path: "contract_info/backend/app/api/routes/jobs.py"
      provides: "path-b 端点"
      contains: "path-b"
  key_links:
    - from: "backend/app/api/routes/jobs.py"
      to: "ContractFile.path_b_json"
      via: "_get_record"
      pattern: "path_b_json"
---

<objective>
路径 B API：GET /path-b、JobDetail 摘要、API 测试；可选 LLM refine 模块与 llm 标记测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/08-path-b-json/08-CONTEXT.md
@contract_info/backend/app/api/routes/jobs.py
@contract_info/backend/app/services/preview_service.py
@contract_info/backend/tests/test_api_download.py
</context>

<tasks>

<task id="08-03-01" type="auto">
  <name>任务 1：API schema + GET /path-b</name>
  <files>
    contract_info/backend/app/api/schemas.py
    contract_info/backend/app/api/routes/jobs.py
  </files>
  <action>
`schemas.py`：

```python
class PathBResponse(BaseModel):
    job_id: uuid.UUID
    performance_fee: dict[str, Any] = Field(default_factory=dict)
    open_day: dict[str, Any] = Field(default_factory=dict)
    source_snippets: dict[str, str] = Field(default_factory=dict)
```

`JobDetailResponse` 增加：`path_b_available: bool = False`

`jobs.py`：

```python
@router.get("/{job_id}/path-b", response_model=PathBResponse)
def get_path_b(job_id: uuid.UUID) -> PathBResponse:
```

- 门禁：`record.status in PREVIEW_STATUSES`（从 `preview_service` 导入或复制 frozenset）
- 无 `path_b_json` → 404
- 返回拆分顶层 `performance_fee` / `open_day` / `source_snippets`（非裸 dict 包一层）

`_record_to_detail`：`path_b_available=bool(record.path_b_json)`

**不修改** `frontend/`（D-A01）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_path_b.py -q</automated>
  </verify>
</task>

<task id="08-03-02" type="auto">
  <name>任务 2：test_api_path_b</name>
  <files>
    contract_info/backend/tests/test_api_path_b.py
  </files>
  <action>
仿 `test_api_download.py` / preview 测试：

- `test_path_b_requires_extracted` — status=parsed → 409
- `test_path_b_success` — mock record，`path_b_json` 种子数据 → 200 + JSON 含 performance_fee
- `test_path_b_missing_json` — extracted 但 path_b_json=None → 404
- `test_job_detail_path_b_available` — detail 中 `path_b_available=True`

  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_path_b.py -q</automated>
  </verify>
</task>

<task id="08-03-03" type="optional">
  <name>任务 3（可选）：LLM path_b refine</name>
  <files>
    contract_info/backend/app/extract/llm/path_b_extract.py
    contract_info/backend/app/extract/pipeline.py
    contract_info/backend/tests/test_path_b_llm.py
  </files>
  <action>
若时间允许（非 CI 必需，D-E02）：

- `refine_path_b_llm(client, fees_text, path_b_dict) -> dict` 优化 `performance_fee.tiers` / `summary`
- pipeline 在 `client.available` 时调用
- `test_path_b_llm.py`：`@pytest.mark.llm`，无 Key skip

可整块 skip 并在 SUMMARY 记为 deferred，不阻塞 phase 验收。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_path_b_llm.py -q -m llm 2>&1 || true</automated>
  </verify>
</task>

</tasks>

<verification>
`cd contract_info && python -m pytest backend/tests/test_path_b_rules.py backend/tests/test_api_path_b.py -q -m "not llm"`
</verification>

<success_criteria>
PATHB-04：API 可返回路径 B JSON；前端 Phase 10
</success_criteria>

<output>
完成后创建 `08-03-SUMMARY.md`、`08-VERIFICATION.md`
</output>
