---
phase: "08-path-b-json"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - contract_info/backend/app/extract/schemas.py
  - contract_info/backend/app/extract/path_b_assemble.py
  - contract_info/backend/app/extract/rules/path_b_rules.py
  - contract_info/backend/app/extract/rules/__init__.py
  - contract_info/backend/app/extract/pipeline.py
  - contract_info/backend/tests/test_path_b_rules.py
  - contract_info/backend/tests/golden/fixtures/contract_expected.json
autonomous: true
requirements:
  - PATHB-01
  - PATHB-02
  - PATHB-03
user_setup: []

must_haves:
  truths:
    - "extract_path_b_rules 产出含 performance_fee 与 open_day 的 dict"
    - "source_snippets 为点分路径扁平 dict，critical 字段有摘录"
    - "extract_document_sync(LlmOff) 返回第三项 path_b 非空"
    - "石云/福禄 open_day.fixed_schedule 与 contract_expected 开放日规则一致或子串匹配"
  artifacts:
    - path: "contract_info/backend/app/extract/rules/path_b_rules.py"
      provides: "路径 B 规则抽取"
      contains: "extract_path_b_rules"
    - path: "contract_info/backend/app/extract/path_b_assemble.py"
      provides: "PathB JSON 组装"
      contains: "build_path_b_document"
  key_links:
    - from: "backend/app/extract/pipeline.py"
      to: "backend/app/extract/rules/path_b_rules.py"
      via: "extract_path_b_rules"
      pattern: "path_b_dict"
---

<objective>
路径 B 规则层：Pydantic schema、path_b_rules（业绩报酬 tiers + 开放日）、path_b_assemble（source_snippets）；pipeline 返回三元组；contract_expected 与单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/08-path-b-json/08-CONTEXT.md
@contract_info/.planning/phases/08-path-b-json/08-RESEARCH.md
@contract_info/.planning/phases/08-path-b-json/08-PATTERNS.md
@contract_info/FIELD_SPEC.md
@contract_info/backend/app/extract/rules/subscription_rules.py
@contract_info/backend/app/extract/rules/product_rules.py
@contract_info/backend/tests/golden/fixtures/contract_expected.json
</context>

<tasks>

<task id="08-01-01" type="auto">
  <name>任务 1：PathB schema + assemble</name>
  <files>
    contract_info/backend/app/extract/schemas.py
    contract_info/backend/app/extract/path_b_assemble.py
  </files>
  <action>
在 `schemas.py` 新增（或独立 `path_b/schemas.py` 再 export）：

- `PerformanceFeeTier`：`share_class`, `benchmark`, `threshold`, `ratio_pct`, `description`
- `PerformanceFeeModule`：`extraction_method`, `benchmark_type`, `hurdle_nav`, `extraction_timing`, `summary`, `tiers[]`
- `OpenDayModule`：`fixed_schedule`, `open_business`, `temporary_open`, `ad_hoc_rules`
- `PathBDocument`：`performance_fee`, `open_day`, `source_snippets`

`path_b_assemble.py`：

```python
def build_path_b_document(
    performance_fields: dict[str, FieldValue | None],
    open_day_fields: dict[str, FieldValue | None],
    tiers: list[PerformanceFeeTier],
) -> dict[str, Any]:
    """返回可 JSON 序列化的 dict（exclude_none），并填充 source_snippets。"""
```

- 点分路径键：`performance_fee.extraction_method`、`open_day.fixed_schedule`、`performance_fee.tiers[0].ratio_pct` 等
- 无 value 的字段 **不** 写入业务对象也不写 snippet（D-SN03）
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.extract.path_b_assemble import build_path_b_document; from backend.app.extract.schemas import FieldValue; d=build_path_b_document({'extraction_method': FieldValue(value='超额', snippet='超额计提')}, {}, []); assert 'performance_fee' in d and d.get('source_snippets')"</automated>
  </verify>
</task>

<task id="08-01-02" type="auto">
  <name>任务 2：path_b_rules 规则抽取</name>
  <files>
    contract_info/backend/app/extract/rules/path_b_rules.py
    contract_info/backend/app/extract/rules/__init__.py
  </files>
  <action>
实现 `extract_path_b_rules(document, windows, *, fund_name, product_elements) -> dict[str, Any]`：

**业绩报酬（PATHB-01）：**

1. 解析份额分类表行「业绩报酬」→ 每份额 `PerformanceFeeTier`（`description` 为单元格原文；能 regex 出 `%` 则填 `ratio_pct`）
2. `windows["fees"]` 补充 `extraction_method` / `extraction_timing`（关键词：计提、提取、高水位 等，Claude 酌情）
3. 无法拆 tier 时：`tiers=[]`，`summary` 取 fees 窗内最长业绩报酬相关段（≤500 字）

**开放日（PATHB-02）：**

1. `open_day.fixed_schedule`：优先 `product_elements["开放日规则"]` 的 value；否则 subscription 窗 `_RE_OPEN_SCHEDULE` 类 regex（复用 product_rules 模式）
2. `open_business`：subscription 窗匹配「申购」「赎回」业务描述句
3. `temporary_open`：全文/ subscription 补扫「临时开放」段（仿 subscription 短期赎回补扫）

返回 `build_path_b_document(...)` 的 dict。

导出 `__init__.py`。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_path_b_rules.py -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - 石云/福禄各至少 1 条 tier 或 summary 非空
    - open_day.fixed_schedule 非空且与 expected 开放日规则子串匹配
  </acceptance_criteria>
</task>

<task id="08-01-03" type="auto">
  <name>任务 3：pipeline 三元组 + 调用点清单</name>
  <files>
    contract_info/backend/app/extract/pipeline.py
  </files>
  <action>
`extract_document` / `extract_document_sync` 返回：

`tuple[ExtractionResult, list[ExtractionWarning], dict[str, Any]]`

在 `merge_extraction` 之后调用 `extract_path_b_rules`，第三项为 `path_b_dict`。

**同 PR 必须更新所有调用方**（执行 08-02 前可先让 extract_service 解包第三项；本任务至少更新）：

- `backend/app/services/extract_service.py`（暂存 path_b 变量，08-02 写 DB）
- `backend/cli.py`（若有 extract 命令）
- `backend/tests/*` 凡 `extract_document_sync` 解包处
- `backend/tests/golden/helpers/pipeline_runner.py`（若使用 sync）

path_b 相关 warnings（如 tiers 为空）追加到 warnings 列表，code 建议 `path_b_incomplete`。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_path_b_rules.py backend/tests/test_extract_pipeline.py -q -m "not llm" --tb=no 2>&1 | head -20</automated>
  </verify>
</task>

<task id="08-01-04" type="auto">
  <name>任务 4：contract_expected + test_path_b_rules</name>
  <files>
    contract_info/backend/tests/test_path_b_rules.py
    contract_info/backend/tests/golden/fixtures/contract_expected.json
  </files>
  <action>
`contract_expected.json` 每 docx 增加 `path_b`：

```json
"path_b": {
  "open_day": { "fixed_schedule_contains": "每自然月" },
  "performance_fee": { "min_tiers": 1, "summary_contains": "业绩报酬" }
}
```

（真值以本地规则抽取实测填入。）

**test_path_b_rules.py**（无 LLM）：

- `test_shiyun_path_b_structure` / `test_fulu_path_b_structure`
- `test_open_day_schedule_matches_product`
- `test_source_snippets_for_critical_fields`
- `test_extract_document_sync_returns_path_b`

  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_path_b_rules.py -q -m "not llm"</automated>
  </verify>
</task>

</tasks>

<verification>
`cd contract_info && python -m pytest backend/tests/test_path_b_rules.py -q -m "not llm"`
</verification>

<success_criteria>
PATHB-01–03 抽取侧就绪：可序列化 path_b dict + snippets
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/08-path-b-json/08-01-SUMMARY.md`
</output>
