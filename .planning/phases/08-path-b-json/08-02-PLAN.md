---
phase: "08-path-b-json"
plan: "02"
type: execute
wave: 1
depends_on:
  - "08-01"
files_modified:
  - contract_info/backend/app/models/contract_file.py
  - contract_info/alembic/versions/006_path_b_json.py
  - contract_info/backend/app/services/extract_service.py
  - contract_info/backend/tests/test_extract_persist.py
autonomous: true
requirements:
  - PATHB-03
  - PATHB-04
user_setup: []

must_haves:
  truths:
    - "contract_files.path_b_json 列存在（JSONB）"
    - "persist_extract 成功后 path_b_json 非空（石云样例集成测）"
    - "extraction_result 内不含 path_b 冗余（仅列存储）"
  artifacts:
    - path: "contract_info/alembic/versions/006_path_b_json.py"
      provides: "DB 迁移"
    - path: "contract_info/backend/app/services/extract_service.py"
      provides: "extract 持久化 path_b"
  key_links:
    - from: "backend/app/services/extract_service.py"
      to: "backend/app/extract/pipeline.py"
      via: "第三返回值 path_b_dict"
      pattern: "path_b_json"
---

<objective>
路径 B 持久化：Alembic `path_b_json` JSONB 列、`extract_service` 与 `persist_extract` 同次写入；扩展集成测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/08-path-b-json/08-CONTEXT.md
@contract_info/alembic/versions/002_extraction_columns.py
@contract_info/alembic/versions/005_subscription_xlsx_path.py
@contract_info/backend/app/services/extract_service.py
</context>

<tasks>

<task id="08-02-01" type="auto">
  <name>任务 1：Alembic 006 + ORM</name>
  <files>
    contract_info/alembic/versions/006_path_b_json.py
    contract_info/backend/app/models/contract_file.py
  </files>
  <action>
新增 `006_path_b_json.py`（`down_revision = "005"`）：

```python
op.add_column(
    "contract_files",
    sa.Column("path_b_json", postgresql.JSONB(astext_type=sa.Text()), nullable=True),
)
```

`ContractFile` 增加：

```python
path_b_json: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
```

（SQLite 测试库若不用 JSONB，conftest 仍用 PostgreSQL 或 skip 集成测。）
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.models.contract_file import ContractFile; assert 'path_b_json' in ContractFile.__table__.columns"</automated>
  </verify>
</task>

<task id="08-02-02" type="auto">
  <name>任务 2：extract_service 写入 path_b_json</name>
  <files>
    contract_info/backend/app/services/extract_service.py
  </files>
  <action>
更新 `_run_extract_on_document`：

```python
result, warnings, path_b = extract_document_sync(document)
return extraction_result_to_dict(result), warnings, path_b
```

`persist_extract` 成功分支：

```python
record.extraction_result = result_dict
record.path_b_json = path_b
record.extraction_warnings = warnings
```

`extract_from_docx_path` / `extract_from_file_id` 若对外暴露，返回三元组或保持 dict+list 并在 docstring 说明 path_b 已持久化。

**不要** 把 path_b 写入 `extraction_result` JSON（D-P01）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_extract_persist.py -q -m integration 2>&1 || echo "skip if no db"</automated>
  </verify>
</task>

<task id="08-02-03" type="auto">
  <name>任务 3：test_extract_persist 断言 path_b_json</name>
  <files>
    contract_info/backend/tests/test_extract_persist.py
  </files>
  <action>
集成测（`@pytest.mark.integration`）在 `persist_extract` 后：

```python
assert row.path_b_json
assert row.path_b_json.get("performance_fee")
assert row.path_b_json.get("open_day")
assert row.path_b_json.get("source_snippets")
```

  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_extract_persist.py -q -m integration</automated>
  </verify>
</task>

</tasks>

<verification>
ORM 列存在；集成测通过或文档注明需 `alembic upgrade head`
</verification>

<success_criteria>
PATHB-03 持久化：DB 列 + extract 写入
</success_criteria>

<output>
完成后创建 `08-02-SUMMARY.md`
</output>
