---
phase: 15-api
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - backend/app/api/schemas.py
  - backend/app/services/preview_service.py
  - backend/app/services/preview_edit_service.py
  - backend/app/api/routes/jobs.py
  - backend/tests/test_preview_edit.py
  - backend/tests/test_preview_section_api.py
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "D-07/D-10: GET/PUT /preview/{section} 五枚举与全量片段一致"
    - "D-08: 分表 PUT 仅 merge 对应子树"
    - "D-09: 全量 PUT Optional + exclude_unset，禁止 or [] 清空"
    - "D-11: 分表 PUT 后 persist_export 全五表"
    - "D-22: 单表 PUT 后其它四表 extraction 行数不变（测试）"
    - "GET /jobs/{id}/preview/{section} 返回与全量 preview 中对应片段一致的数据"
    - "PUT /jobs/{id}/preview/{section} 仅合并该 section 的 extraction 子树，其它四表行数与内容不变"
    - "PUT /jobs/{id}/preview 全量端点保留；未传字段不修改对应 extraction（Optional + exclude_unset）"
    - "任一分表或全量 PUT 成功后仍调用 persist_export 重生成五表 xlsx"
  artifacts:
    - path: backend/app/api/schemas.py
      provides: PreviewSection, section update/response models, Optional JobPreviewUpdateRequest
    - path: backend/app/services/preview_service.py
      provides: slice_preview(full, section)
    - path: backend/app/services/preview_edit_service.py
      provides: apply_section_preview_edits, fixed apply_preview_edits
    - path: backend/app/api/routes/jobs.py
      provides: GET/PUT /{job_id}/preview/{section}
    - path: backend/tests/test_preview_section_api.py
      provides: sectional API contract tests
  key_links:
    - from: backend/app/api/routes/jobs.py update_section_preview
      to: backend/app/services/preview_edit_service.py apply_section_preview_edits
      via: section-scoped merge then persist_export
      pattern: apply_section_preview_edits
    - from: backend/app/api/routes/jobs.py update_job_preview
      to: backend/app/services/preview_edit_service.py apply_preview_edits
      via: body.model_dump(exclude_unset=True)
      pattern: exclude_unset
---

<objective>
Implement sectional preview GET/PUT (API-01) and fix full PUT empty-clearing bug (D-07–D-11): five section enums, merge-only edits, Optional full payload, persist_export after save, contract tests.

Purpose: Phase 17 table pages need per-table read/write without wiping sibling extractions; v1.2 clients keep full preview with safe partial updates.
Output: schemas, slice_preview, apply_section_preview_edits, routes, extended test_preview_edit.py, new test_preview_section_api.py.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md
@contract_info/.planning/phases/CTRX-15-api/15-RESEARCH.md
@contract_info/.planning/phases/CTRX-15-api/15-PATTERNS.md
@contract_info/FIELD_SPEC.md

<interfaces>
Section → extraction keys (D-07):
- product-elements → product_elements; GET fields product_rows
- fee-rates → fee_rates; GET fee_columns, fee_rows
- lock-periods → lock_periods; GET lock_columns, lock_rows
- share-classes → share_classes; GET share_columns, share_rows
- subscription-fee-rates → subscription_fees; GET subscription_columns, subscription_rows

From preview_edit_service.py:
- _apply_product_edits(extraction, product_rows)
- _apply_list_table_edits(extraction, list_key=..., preview_rows=..., label_to_key=...)
- apply_preview_edits ends with persist_export(file_id) and build_job_preview

From preview_service.py:
- build_job_preview(record) returns dict with all table keys
- PREVIEW_STATUSES for edit gate
- SNIPPET_DISPLAY = "摘录原文" — skip in list edits

From jobs.py download routes: path segments fee-rates, lock-periods match section URL naming
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas and preview_service slice_preview (D-07, D-10)</name>
  <files>backend/app/api/schemas.py, backend/app/services/preview_service.py</files>
  <read_first>
    - contract_info/backend/app/api/schemas.py — JobPreviewResponse, JobPreviewUpdateRequest, ProductPreviewItem
    - contract_info/backend/app/services/preview_service.py — build_job_preview return dict keys lines 502-514
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-07, D-09, D-10
    - contract_info/FIELD_SPEC.md — column headers for list tables
  </read_first>
  <action>
    In schemas.py per D-07, D-09:
    - Add PreviewSection = Literal with five values: product-elements, fee-rates, lock-periods, share-classes, subscription-fee-rates
    - Add ProductSectionUpdate (product_rows only), FeeSectionUpdate (fee_columns optional, fee_rows), LockSectionUpdate, ShareSectionUpdate, SubscriptionSectionUpdate — each only section fields
    - Add JobPreviewSectionResponse base: job_id, section, source; section-specific response models or one model with optional table fields matching GET slice
    - Change JobPreviewUpdateRequest: every list/product field to type | None = None (remove default_factory=list) per D-09

    In preview_service.py:
    - Add slice_preview(full: dict, section: PreviewSection) -> dict containing job_id, source, section, and only keys for that section (product-elements → product_rows only; fee-rates → fee_columns + fee_rows; etc.)
    - Invalid section should not occur at service layer if route uses Literal

    OpenAPI description on JobPreviewUpdateRequest / PUT /preview: note new clients should use sectional PUT per D-09.
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.api.schemas import PreviewSection, JobPreviewUpdateRequest; from backend.app.services.preview_service import slice_preview; f={'job_id':'00000000-0000-0000-0000-000000000001','source':'x','product_rows':[],'fee_columns':[],'fee_rows':[],'lock_columns':[],'lock_rows':[],'share_columns':[],'share_rows':[],'subscription_columns':[],'subscription_rows':[]}; s=slice_preview(f,'fee-rates'); assert 'fee_rows' in s and 'product_rows' not in s"</automated>
  </verify>
  <acceptance_criteria>
    - PreviewSection Literal has exactly five section strings
    - JobPreviewUpdateRequest fields are Optional (None default), not default_factory=list
    - slice_preview returns only section-relevant keys plus job_id, source, section
    - Import/slice smoke exits 0
  </acceptance_criteria>
  <done>Section types and slice_preview ready for routes and edit service</done>
</task>

<task type="auto">
  <name>Task 2: Section merge + full PUT fix + routes (D-08, D-09, D-11)</name>
  <files>backend/app/services/preview_edit_service.py, backend/app/api/routes/jobs.py</files>
  <read_first>
    - contract_info/backend/app/services/preview_edit_service.py — apply_preview_edits, _apply_product_edits, _apply_list_table_edits
    - contract_info/backend/app/api/routes/jobs.py — preview_job, update_job_preview, _preview_response_from_data
    - contract_info/backend/app/export/column_map.py — extraction_key_for_fee_header, extraction_key_for_subscription_header
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-08, D-11
  </read_first>
  <action>
    preview_edit_service.py:
    - Fix apply_preview_edits: only call _apply_product_edits when payload.get("product_rows") is not None; same for each fee_rows, lock_rows, share_rows, subscription_rows with correct list_key and label_to_key — NEVER use payload.get("x") or [] per D-08
    - Add apply_section_preview_edits(file_id, section, payload: dict) mirroring session/status/deepcopy/commit/persist_export/build_job_preview shell of apply_preview_edits but branch on section to call exactly one _apply_* helper:
      product-elements → _apply_product_edits only
      fee-rates → _apply_list_table_edits list_key fee_rates, label_to_key extraction_key_for_fee_header
      lock-periods → lock_periods, lambda label: label
      share-classes → share_classes, lambda label: label
      subscription-fee-rates → subscription_fees, extraction_key_for_subscription_header
    - Always call persist_export(file_id) after commit per D-11
    - Return build_job_preview(record) full dict (caller slices for response)

    jobs.py:
    - GET /{job_id}/preview/{section}: get_job_preview → slice_preview → validate JobPreviewSectionResponse; same 404/409 mapping as preview_job
    - PUT /{job_id}/preview/{section}: body typed per section (use Union or separate route handlers / dependency); apply_section_preview_edits(job_id, section, body.model_dump()); return sliced response
    - update_job_preview: pass body.model_dump(exclude_unset=True) to apply_preview_edits per D-09
    - Add helper _section_response_from_data if needed to avoid duplicating field mapping

    Illegal section path → FastAPI 422 via Literal type on path param.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_preview_edit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - apply_section_preview_edits exists and touches only one extraction subtree per section
    - apply_preview_edits does not clear tables when keys omitted from payload
    - GET and PUT /jobs/{id}/preview/{section} registered for all five sections
    - Full PUT uses exclude_unset=True at route
    - persist_export still called after sectional and full saves
  </acceptance_criteria>
  <done>Sectional and fixed full preview edit paths wired in API</done>
</task>

<task type="auto">
  <name>Task 3: Preview edit and sectional API tests (D-22)</name>
  <files>backend/tests/test_preview_edit.py, backend/tests/test_preview_section_api.py</files>
  <read_first>
    - contract_info/backend/tests/test_preview_edit.py — FakeSession patch pattern
    - contract_info/backend/tests/test_api_validation.py — api_client + _get_record patch pattern
    - contract_info/.planning/phases/CTRX-15-api/15-VALIDATION.md — tasks 15-02-01, 15-02-02
  </read_first>
  <action>
    Extend test_preview_edit.py:
    - test_section_put_fee_does_not_clear_lock: extraction fixture with fee_rates and lock_periods rows; apply_section_preview_edits with fee-rates payload only; assert len(lock_periods) unchanged
    - test_full_put_omitted_fields_unchanged: apply_preview_edits with payload only product_rows key; assert fee_rates list length unchanged

    Create test_preview_section_api.py:
    - test_get_section_fee_matches_full: patch get_job_preview returning known dict; GET /preview/fee-rates and GET /preview; assert fee_rows deep equal
    - test_put_section_returns_200: patch apply_section_preview_edits; PUT /preview/product-elements with product_rows body; status 200
    - test_invalid_section_422: GET /preview/not-a-section → 422

    Mock persist_export in unit tests to avoid filesystem IO.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_preview_edit.py backend/tests/test_preview_section_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_section_put_fee_does_not_clear_lock passes
    - test_full_put_omitted_fields_unchanged passes
    - test_preview_section_api.py exists with GET consistency and PUT smoke tests
    - Combined pytest command exits 0
  </acceptance_criteria>
  <done>API-01 contract covered by unit and API tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PUT preview | Untrusted row payloads; job scoped by UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Denial of Service | PUT preview body | mitigate | Reuse existing max row limits from preview_service patterns; reject oversized lists via Pydantic |
| T-15-04 | Tampering | sectional PUT | mitigate | apply_section_preview_edits only writes mapped list_key for declared section |
</threat_model>

<verification>
cd contract_info && pytest backend/tests/test_preview_edit.py backend/tests/test_preview_section_api.py -q
</verification>

<success_criteria>
- API-01: five sectional GET/PUT endpoints; PUT does not clear other tables
- ROADMAP criterion 2 satisfied at API layer (persistence isolation)
- All plan 02 tests green
</success_criteria>

<output>
After completion, create contract_info/.planning/phases/CTRX-15-api/15-02-SUMMARY.md
</output>
