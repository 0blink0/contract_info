---
phase: 15-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/job_runner_service.py
  - backend/app/services/pipeline_service.py
  - backend/app/main.py
  - backend/app/api/routes/jobs.py
  - backend/tests/test_parallel_run.py
  - backend/tests/test_api_pipeline.py
autonomous: true
requirements:
  - API-03
  - UP-04

must_haves:
  truths:
    - "D-01: pending 不占 IN_PROGRESS 并发槽"
    - "D-02: count_in_progress>=3 时 POST /run 返回 409 与 active_count"
    - "D-03: 单 job PipelineBusyError 409 文案与全局槽位 409 可区分"
    - "D-04/D-05: JobRunner ThreadPoolExecutor(3) submit run_pipeline，worker 独立 SessionLocal"
    - "D-06: lifespan shutdown_runner(wait=False)"
    - "D-22/D-23: 3×submit smoke + 第 4 路 409 自动化测试"
    - "第 4 个任务在已有 3 个 pipeline 执行中时 POST /run 返回 409，detail 含可读中文说明"
    - "pending 状态 job 不占全局并发槽"
    - "单 job 已在 parsing/extracting/exporting 时 POST /run 仍返回 409（PipelineBusyError 文案与全局槽位错误可区分）"
    - "POST /run 通过 ThreadPoolExecutor(max_workers=3) 提交 run_pipeline，不再使用 BackgroundTasks"
    - "应用关闭时 executor 可 shutdown，pytest teardown 不挂死"
  artifacts:
    - path: backend/app/services/job_runner_service.py
      provides: JobRunner singleton with submit and shutdown_runner
      exports: ["submit", "shutdown_runner", "get_runner"]
    - path: backend/app/services/pipeline_service.py
      provides: count_in_progress global slot query
      contains: count_in_progress
    - path: backend/app/main.py
      provides: FastAPI lifespan calling shutdown_runner
    - path: backend/tests/test_parallel_run.py
      provides: global 409 and runner submit contract tests
  key_links:
    - from: backend/app/api/routes/jobs.py run_job
      to: backend/app/services/job_runner_service.py
      via: get_runner().submit(job_id) after count_in_progress check
      pattern: submit\(
    - from: backend/app/services/job_runner_service.py _run_in_worker
      to: backend/app/services/pipeline_service.py run_pipeline
      via: worker thread invokes sync pipeline
      pattern: run_pipeline\(
    - from: backend/app/main.py lifespan
      to: backend/app/services/job_runner_service.py
      via: shutdown_runner on app shutdown
      pattern: shutdown_runner
---

<objective>
Deliver bounded parallel pipeline execution (API-03) and global 3-slot gate on POST /run (UP-04): JobRunner with ThreadPoolExecutor(3), count_in_progress, lifespan shutdown, and automated tests.

Purpose: Replace BackgroundTasks fake-parallelism so up to 3 jobs can advance independently without blocking the event loop; enforce product cap before queueing a 4th run.
Output: job_runner_service.py, pipeline_service.count_in_progress, main.py lifespan, run_job wiring, test_parallel_run.py, updated test_api_pipeline.py.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/PROJECT.md
@contract_info/.planning/ROADMAP.md
@contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md
@contract_info/.planning/phases/CTRX-15-api/15-RESEARCH.md
@contract_info/.planning/phases/CTRX-15-api/15-PATTERNS.md
@contract_info/.planning/phases/CTRX-15-api/15-VALIDATION.md

<interfaces>
From backend/app/services/pipeline_service.py:
- IN_PROGRESS = frozenset({"parsing", "extracting", "exporting"})
- assert_can_run(status) raises PipelineBusyError when status in IN_PROGRESS
- run_pipeline(file_id: UUID) — sync; opens own SessionLocal per sub-step

From backend/app/api/routes/jobs.py (current run_job):
- Uses BackgroundTasks.add_task(run_pipeline, job_id) — MUST remove
- Maps PipelineBusyError → HTTPException 409 detail=str(exc)

From backend/tests/test_api_pipeline.py:
- Patches backend.app.api.routes.jobs.run_pipeline — MUST change patch target to job runner submit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JobRunner service and count_in_progress (D-04, D-05, D-06)</name>
  <files>backend/app/services/job_runner_service.py, backend/app/services/pipeline_service.py</files>
  <read_first>
    - contract_info/backend/app/services/pipeline_service.py — IN_PROGRESS, run_pipeline, assert_can_run
    - contract_info/backend/tests/test_db_wal.py — ThreadPoolExecutor(max_workers=3) pattern
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-01, D-04, D-05, D-06
    - contract_info/.planning/phases/CTRX-15-api/15-PATTERNS.md — job_runner_service section
  </read_first>
  <action>
    Create backend/app/services/job_runner_service.py per D-04–D-06:
    - Module-level ThreadPoolExecutor(max_workers=3, thread_name_prefix="ctrx-pipeline")
    - Class JobRunner with submit(file_id: uuid.UUID) calling executor.submit(_run_in_worker, file_id)
    - _run_in_worker(file_id): try/except with logger.exception on failure; body only calls run_pipeline(file_id) from pipeline_service — no session passed from caller thread
    - get_runner() returns module singleton
    - shutdown_runner(wait: bool = False) calls executor.shutdown(wait=wait)

    Add to pipeline_service.py per D-01:
    - count_in_progress(session=None) -> int using sqlalchemy select(func.count()) on ContractFile where status.in_(IN_PROGRESS)
    - Optional session param: if None, open SessionLocal(), query, close in finally; if provided, use caller session
    - pending status MUST NOT be counted (only IN_PROGRESS members)

    Do NOT share SessionLocal across threads. Do NOT use asyncio for pipeline.
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.services.job_runner_service import get_runner, shutdown_runner; from backend.app.services.pipeline_service import count_in_progress, IN_PROGRESS; assert callable(count_in_progress); r=get_runner(); assert hasattr(r,'submit'); shutdown_runner(wait=True)"</automated>
  </verify>
  <acceptance_criteria>
    - job_runner_service.py exists with submit, get_runner, shutdown_runner exported
    - pipeline_service.count_in_progress exists and filters by IN_PROGRESS only
    - Worker _run_in_worker only invokes run_pipeline, no cross-thread session reuse
    - Import smoke command exits 0
  </acceptance_criteria>
  <done>JobRunner and count_in_progress implemented and importable</done>
</task>

<task type="auto">
  <name>Task 2: Wire run_job + FastAPI lifespan (D-01, D-02, D-03)</name>
  <files>backend/app/api/routes/jobs.py, backend/app/main.py</files>
  <read_first>
    - contract_info/backend/app/api/routes/jobs.py — run_job lines 192-208, delete_job 409 pattern
    - contract_info/backend/app/main.py — current FastAPI() without lifespan
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-02, D-03
  </read_first>
  <action>
    Update run_job in jobs.py per D-01–D-03:
    1. Remove BackgroundTasks parameter and FastAPI BackgroundTasks import if unused elsewhere
    2. Keep _get_record and assert_can_run first — PipelineBusyError → HTTPException(409, detail=str(exc)) unchanged for single-job busy
    3. After assert_can_run succeeds, if count_in_progress() >= 3: raise HTTPException(status_code=409, detail={"detail": "已有 3 个任务正在处理，请稍后再试", "active_count": 3}) per D-02 (structured dict for Phase 19 UI)
    4. Optional double-check: call count_in_progress() again immediately before submit to reduce race window per RESEARCH
    5. Call get_runner().submit(job_id); return RunResponse unchanged (202)

    Register lifespan on main.py per D-06:
    - Use contextlib.asynccontextmanager
    - yield on startup; on shutdown import shutdown_runner from job_runner_service and call shutdown_runner(wait=False)
    - Pass lifespan= to FastAPI() constructor

    Do NOT change can_run or pending slot semantics. Do NOT lower max_workers below 3.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_api_pipeline.py::test_run_returns_202 backend/tests/test_api_pipeline.py::test_run_conflict_when_exporting -x -q</automated>
  </verify>
  <acceptance_criteria>
    - run_job signature has no background_tasks parameter
    - run_job calls get_runner().submit after slot check, not background_tasks.add_task
    - Global 409 uses detail dict with keys detail and active_count when count_in_progress >= 3
    - main.py FastAPI app uses lifespan that calls shutdown_runner(wait=False) on teardown
    - test_api_pipeline.py patches submit (or get_runner().submit) instead of run_pipeline at route layer
  </acceptance_criteria>
  <done>run_job uses JobRunner; app lifespan shuts down executor</done>
</task>

<task type="auto">
  <name>Task 3: Parallel run contract tests (D-22, D-23)</name>
  <files>backend/tests/test_parallel_run.py, backend/tests/test_api_pipeline.py</files>
  <read_first>
    - contract_info/backend/tests/test_api_pipeline.py — existing run tests and patch patterns
    - contract_info/.planning/phases/CTRX-15-api/15-VALIDATION.md — task 15-01-01, 15-01-02
    - contract_info/.planning/phases/CTRX-15-api/15-RESEARCH.md — Test Plan table
  </read_first>
  <action>
    Create backend/tests/test_parallel_run.py:
    - test_fourth_run_returns_409_when_three_in_progress: patch count_in_progress to return 3; POST /api/v1/jobs/{id}/run with api_client; assert status 409; assert response JSON detail contains "3" (string or nested detail field)
    - test_run_submits_via_job_runner: patch get_runner or submit on job_runner; pending record; POST run; assert submit called once with job_id
    - test_three_submits_when_slots_available (D-22 parallel smoke): patch count_in_progress to return 0,1,2 on successive calls; patch get_runner().submit; create 3 pending jobs; POST run on each; assert submit.call_count == 3
    - test_job_runner_max_workers_is_three: assert get_runner() executor _max_workers == 3 (inspect ThreadPoolExecutor on JobRunner module)
    - test_count_in_progress_excludes_pending: unit test count_in_progress with mocked session returning count only for IN_PROGRESS statuses (or integration with FakeSession if pattern exists)

    Update test_api_pipeline.py test_run_returns_202:
    - Replace patch of backend.app.api.routes.jobs.run_pipeline with patch of backend.app.services.job_runner_service.get_runner or submit
    - Patch count_in_progress to return 0 so global gate passes

    Add optional conftest autouse fixture only if tests leak threads: yield test then shutdown_runner(wait=True)

    Use mock run_pipeline or short sleep only if needed; prefer mocking submit per D-23.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_parallel_run.py backend/tests/test_api_pipeline.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_parallel_run.py exists with global 409, single submit, and test_three_submits_when_slots_available (3× submit smoke per D-22)
    - test_job_runner_max_workers_is_three asserts max_workers == 3
    - test_run_returns_202 passes with runner patch (not run_pipeline at routes)
    - pytest backend/tests/test_parallel_run.py backend/tests/test_api_pipeline.py exits 0
  </acceptance_criteria>
  <done>Automated tests prove UP-04 gate and API-03 submit path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /run | Untrusted job_id; authenticated via verify_api_key on router |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | POST /run | mitigate | count_in_progress >= 3 → 409; executor max_workers=3 |
| T-15-02 | Tampering | job_id UUID | mitigate | _get_record → 404 if missing; no cross-job enqueue |
</threat_model>

<verification>
cd contract_info && pytest backend/tests/test_parallel_run.py backend/tests/test_api_pipeline.py -q
</verification>

<success_criteria>
- UP-04: 4th POST /run when 3 jobs in IN_PROGRESS returns 409 with readable message
- API-03: ThreadPoolExecutor(3) runs run_pipeline; each pipeline step uses own SessionLocal
- All plan 01 tests green
</success_criteria>

<output>
After completion, create contract_info/.planning/phases/CTRX-15-api/15-01-SUMMARY.md
</output>
