---
phase: 15-api
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - backend/app/services/verification_service.py
  - backend/app/services/validation_service.py
  - backend/app/api/schemas.py
  - backend/app/api/routes/jobs.py
  - backend/tests/test_verification_service.py
  - backend/tests/test_api_verification.py
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - "D-12: GET /verification/{table_key} 五表枚举"
    - "D-13/D-15: extraction 为主四列 + TableVerificationResponse"
    - "D-14: validation overlay 可选字段"
    - "D-16/D-17: _page_for_block 查找 page 元数据，无则 null+note"
    - "D-18: path-b 不在 verification 端点"
    - "D-19/D-20/D-21: Semaphore(2) 削峰，skipped 路径不变"
    - "D-22: verification API 自动化测试"
    - "GET /jobs/{id}/verification/{table_key} 返回字段名、字段值、摘录页码（可 null）、原文摘录"
    - "无 validation_result 时仍返回基于 extraction_result 的行（非空表）"
    - "有 validation_result 时按 field 路径叠加 validation_status 与 validation_reason"
    - "page_no_available 为 false 当所有 page_no 为 null（当前解析层无页码）"
    - "path-b 不在此端点；LLM validation 最多 2 路并发（D-19）"
  artifacts:
    - path: backend/app/services/verification_service.py
      provides: build_verification_rows(record, table_key)
    - path: backend/app/api/schemas.py
      provides: VerificationRow, TableVerificationResponse
    - path: backend/app/api/routes/jobs.py
      provides: GET /{job_id}/verification/{table_key}
    - path: backend/app/services/validation_service.py
      provides: _llm_validation_semaphore around run_llm_validation_sync
  key_links:
    - from: backend/app/api/routes/jobs.py get_table_verification
      to: backend/app/services/verification_service.py
      via: build_verification_rows after PREVIEW_STATUSES check
      pattern: build_verification_rows
    - from: backend/app/services/verification_service.py
      to: backend/app/validate/evidence.py
      via: resolve_evidence_text for excerpt column
      pattern: resolve_evidence_text
    - from: backend/app/services/validation_service.py persist_validation
      to: run_llm_validation_sync
      via: with _llm_validation_semaphore
      pattern: Semaphore\(2\)
---

<objective>
Deliver per-table verification API (API-02) and LLM validation throttling (D-12–D-21): build_verification_rows from extraction with optional validation overlay, stable page_no contract, GET route, Semaphore(2) in persist_validation, tests.

Purpose: Phase 17 table pages need excerpt review grid; parallel pipelines must not trigger LLM storms.
Output: verification_service.py, schemas, route, validation_service semaphore, test_verification_service.py, test_api_verification.py.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md
@contract_info/.planning/phases/CTRX-15-api/15-RESEARCH.md
@contract_info/FIELD_SPEC.md

<interfaces>
From backend/app/api/routes/jobs.py get_validation:
- PREVIEW_STATUSES gate → 409 Job not extracted yet
- label_for_validation_field from backend.app.validate.field_labels

From backend/app/validate/evidence.py:
- resolve_evidence_text(snippet, block_id, parse_json) -> str | None

From backend/app/services/preview_service.py:
- _product_from_extraction, list table helpers for row/column structure reference

table_key: same five values as PreviewSection (D-12); path-b excluded (D-18)

VerificationRow fields (D-15): field, field_label, value, page_no, page_no_note, excerpt, validation_status optional, validation_reason optional

TableVerificationResponse: job_id, table_key, rows, page_no_available bool
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: verification_service and schemas (D-12–D-18)</name>
  <files>backend/app/services/verification_service.py, backend/app/api/schemas.py</files>
  <read_first>
    - contract_info/backend/app/services/preview_service.py — _product_from_extraction, list table extraction helpers
    - contract_info/backend/app/validate/evidence.py — resolve_evidence_text
    - contract_info/backend/app/validate/field_labels.py — label_for_validation_field
    - contract_info/backend/app/parse/schemas.py — Block has no page field (confirm)
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-12 through D-18
    - contract_info/FIELD_SPEC.md — Chinese field labels
  </read_first>
  <action>
    schemas.py per D-15:
    - VerificationRow: field, field_label, value optional, page_no optional int, page_no_note optional str, excerpt optional, validation_status optional, validation_reason optional
    - TableVerificationResponse: job_id UUID, table_key PreviewSection, rows list, page_no_available bool default False

    Create verification_service.py:
    - build_verification_rows(record, table_key: str) -> list[dict] or list[VerificationRow]
    - Primary data from record.extraction_result per table_key:
      product-elements: one row per product_elements entry with value and excerpt from snippet/block via resolve_evidence_text(record.parse_json)
      fee-rates, lock-periods, share-classes, subscription-fee-rates: flatten list rows — one row per non-snippet column per list item; field path like fee_rates[i].{col} aligned with validation field paths per RESEARCH
    - field_label via label_for_validation_field(field, extraction_result) or Chinese column header for list tables
    - page_no: implement _page_for_block(block_id, parse_json) that reads block dict for keys page, page_index, or page_no if present in parse_json blocks; return int when found; else None with page_no_note 页码暂未解析 per D-16/D-17 (do not skip lookup — only return null when metadata absent)
    - page_no_available = any(row.page_no is not None for row in rows) per D-15
    - Overlay: if record.validation_result has items, index by field string; attach validation_status from item status and validation_reason from item reason — do not drop extraction rows when validation missing per D-14
    - Do NOT include path-b data per D-18
    - Do NOT modify docx parser for page numbers per D-17

    Export build_verification_rows for route use.
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.services.verification_service import build_verification_rows; from backend.app.api.schemas import VerificationRow, TableVerificationResponse; print('verification imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - verification_service.py exists with build_verification_rows
    - VerificationRow and TableVerificationResponse in schemas.py
    - Product table rows have field_label in Chinese and excerpt from snippet when present
    - All rows have page_no None and page_no_available False for fixtures without page metadata
    - Validation overlay attaches when matching validation item exists
    - Import smoke command exits 0
  </acceptance_criteria>
  <done>Verification row builder and response models implemented</done>
</task>

<task type="auto">
  <name>Task 2: GET verification route + LLM Semaphore + API test stub (D-19–D-21)</name>
  <files>backend/app/api/routes/jobs.py, backend/app/services/validation_service.py, backend/tests/test_api_verification.py</files>
  <read_first>
    - contract_info/backend/app/api/routes/jobs.py — get_validation PREVIEW_STATUSES and 404 pattern
    - contract_info/backend/app/services/validation_service.py — persist_validation, run_llm_validation_sync
    - contract_info/.planning/phases/CTRX-15-api/15-CONTEXT.md — D-19, D-20, D-21
  </read_first>
  <action>
    jobs.py:
    - Add GET /{job_id}/verification/{table_key} with table_key: PreviewSection
    - Flow: _get_record → if status not in PREVIEW_STATUSES raise 409 same message as get_validation → rows = build_verification_rows(record, table_key) → return TableVerificationResponse
    - LookupError from missing job already 404 via _get_record

    validation_service.py per D-19–D-21:
    - import threading; module-level _llm_validation_semaphore = threading.Semaphore(2)
    - Wrap run_llm_validation_sync call inside persist_validation with with _llm_validation_semaphore:
    - Do not skip validation when API key present per D-20
    - Skipped validation path unchanged when no key per D-21

    Create backend/tests/test_api_verification.py with minimal tests (expanded in Task 3):
    - test_verification_requires_extracted: pending job → GET verification → 409
    - test_verification_404: random uuid → 404

    Do not add path-b to verification route. Do not change extract pipeline order.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_api_verification.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_api_verification.py exists with at least test_verification_requires_extracted and test_verification_404
    - GET route registered on jobs router for /{job_id}/verification/{table_key}
    - persist_validation acquires semaphore before run_llm_validation_sync
    - pytest backend/tests/test_api_verification.py exits 0 for stub tests
  </acceptance_criteria>
  <done>Verification endpoint live; LLM calls capped at 2 concurrent</done>
</task>

<task type="auto">
  <name>Task 3: Verification unit and API tests (D-22)</name>
  <files>backend/tests/test_verification_service.py, backend/tests/test_api_verification.py</files>
  <read_first>
    - contract_info/backend/tests/test_api_validation.py — extracted vs not extracted tests
    - contract_info/backend/tests/test_validation_persist.py — mock run_llm_validation_sync pattern
    - contract_info/.planning/phases/CTRX-15-api/15-VALIDATION.md — tasks 15-03-01, 15-03-02
  </read_first>
  <action>
    Create test_verification_service.py:
    - Fixture SimpleNamespace or ContractFile-like with extraction_result containing product_elements and fee_rates with snippets
    - test_product_rows_have_labels_and_excerpt: build_verification_rows; assert field_label non-empty Chinese; excerpt populated when snippet present
    - test_page_no_unavailable: assert all page_no is None; page_no_available is False
    - test_validation_overlay: validation_result items with matching field adds validation_status

    Extend test_api_verification.py (stub from Task 2):
    - test_verification_success: status extracted, patch build_verification_rows or use minimal record; GET → 200 and rows key present
    - test_invalid_table_key_returns_422: invalid table_key segment → 422

    Optional semaphore test: mock run_llm_validation_sync with time.sleep — mark slow or skip if flaky; not required for phase gate per 15-VALIDATION.

    Use api_client and api_headers from conftest.
  </action>
  <verify>
    <automated>cd contract_info && pytest backend/tests/test_verification_service.py backend/tests/test_api_verification.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_verification_service.py and test_api_verification.py exist
    - Service tests assert four logical columns: field_label, value, page_no, excerpt
    - API tests cover 409, 200, 404
    - Combined pytest exits 0
  </acceptance_criteria>
  <done>API-02 fully covered by automated tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET verification | Read-only; job_id scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Information Disclosure | verification rows | mitigate | 404 for unknown job_id; no cross-job data |
| T-15-06 | Denial of Service | LLM validation | mitigate | Semaphore(2) limits concurrent API calls per D-19 |
</threat_model>

<verification>
cd contract_info && pytest backend/tests/test_verification_service.py backend/tests/test_api_verification.py -q
cd contract_info && pytest backend/tests -q -m "not llm" --ignore=backend/tests/golden
</verification>

<success_criteria>
- API-02: verification endpoint returns stable four-column rows with nullable page_no
- D-19: Semaphore(2) wraps LLM validation
- ROADMAP criteria 3 and 4 supported (API contract + parallel LLM peak control)
- Phase 15 gate: non-LLM test suite green
</success_criteria>

<output>
After completion, create contract_info/.planning/phases/CTRX-15-api/15-03-SUMMARY.md
</output>
