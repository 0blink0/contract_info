---
phase: "10-integration-docs"
plan: "02"
type: execute
wave: 1
depends_on:
  - "10-01"
files_modified:
  - contract_info/frontend/src/api/types.ts
  - contract_info/frontend/src/api/client.ts
  - contract_info/frontend/src/components/JobDetail.vue
  - contract_info/frontend/src/components/PathBPanel.vue
  - contract_info/frontend/src/components/ValidationPanel.vue
autonomous: true
requirements:
  - UI-01
  - API-02
  - VAL-04
user_setup: []

must_haves:
  truths:
    - "exported 后显示第 5 个申赎下载按钮"
    - "extracted+ 可展开 Path B 与校验面板"
    - "首次展开懒加载 /path-b 与 /validation"
    - "校验表默认仅 fail+warn，可切换显示 pass"
  artifacts:
    - path: "contract_info/frontend/src/components/PathBPanel.vue"
      provides: "路径 B 展示"
    - path: "contract_info/frontend/src/components/ValidationPanel.vue"
      provides: "摘录校验展示"
  key_links:
    - from: "frontend/src/components/JobDetail.vue"
      to: "frontend/src/api/client.ts"
      via: "getPathB/getValidation"
      pattern: "subscription-fee-rates"
---

<objective>
前端 API 类型与 client；JobDetail 五下载；PathBPanel、ValidationPanel 懒加载集成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/10-integration-docs/10-CONTEXT.md
@contract_info/frontend/src/components/JobDetail.vue
@contract_info/backend/app/api/schemas.py
</context>

<tasks>

<task id="10-02-01" type="auto">
  <name>任务 1：types + client</name>
  <files>
    contract_info/frontend/src/api/types.ts
    contract_info/frontend/src/api/client.ts
  </files>
  <action>
**types.ts** — 扩展 `JobDetail`：
`subscription_xlsx_path`, `path_b_available`, `validation_available`, `validation_fail_count`, `validation_warn_count`

新增：
```typescript
export interface PathBResponse { job_id, performance_fee, open_day, source_snippets }
export interface ValidationItem { field, status, value?, reason, suggestion? }
export interface ValidationResponse { job_id, validated_at?, model?, skipped, items, summary }
```

`DownloadKind` 增加 `'subscription-fee-rates'`

**client.ts**：
- `getPathB(jobId)`
- `getValidation(jobId)`
  </action>
  <verify>
    <automated>cd contract_info/frontend && npm run build 2>&1 | Select-Object -Last 15</automated>
  </verify>
</task>

<task id="10-02-02" type="auto">
  <name>任务 2：JobDetail 五下载 + 面板挂载</name>
  <files>
    contract_info/frontend/src/components/JobDetail.vue
  </files>
  <action>
- 第 5 按钮：「下载申赎费率」→ `subscription-fee-rates`
- `PREVIEW_PLUS` computed：`extracted|exporting|exported|export_failed`
- 顺序（D-UX01）：WarningsList → ValidationPanel → PathBPanel → ExportPreview
- 传入 `jobId`、`status`、`detail`（validation 计数、path_b_available）
- fail>0 时非阻塞 `el-alert`（D-VAL06）
  </action>
  <verify>
    <automated>cd contract_info/frontend && npm run build</automated>
  </verify>
</task>

<task id="10-02-03" type="auto">
  <name>任务 3：PathBPanel.vue</name>
  <files>
    contract_info/frontend/src/components/PathBPanel.vue
  </files>
  <action>
`el-collapse` 标题「路径 B（需 CRM 手录）」。

展开时 `getPathB`（仅一次，可 refresh 按钮）。

内容：
- `performance_fee` / `open_day` 简要字段（tiers 表）
- `source_snippets` 键值表
- `<pre>` 格式化完整 JSON
- 按钮：复制（clipboard）、下载 Blob `path_b_{jobId}.json`

`path_b_available=false` → 禁用或 empty 提示。
  </action>
  <verify>
    <automated>cd contract_info/frontend && npm run build</automated>
  </verify>
</task>

<task id="10-02-04" type="auto">
  <name>任务 4：ValidationPanel.vue</name>
  <files>
    contract_info/frontend/src/components/ValidationPanel.vue
  </files>
  <action>
`el-collapse`「摘录一致性校验」；标题 badges：fail/warn 计数（来自 props 或加载后 summary）。

展开 → `getValidation`。

表格列：字段、状态（`el-tag`）、值、原因、建议。

`row-class-name` 按 status 着色。

`el-switch`「显示 pass」默认关；过滤 items。

`skipped` 时显示说明文案。
  </action>
  <verify>
    <automated>cd contract_info/frontend && npm run build</automated>
  </verify>
</task>

</tasks>

<verification>
`npm run build` in frontend/
</verification>

<success_criteria>
UI-01 核心：5 下载 + path B + 校验面板可交互
</success_criteria>

<output>
完成后创建 `10-02-SUMMARY.md`
</output>
