---
phase: "10-integration-docs"
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - contract_info/frontend/src/api/types.ts
  - contract_info/frontend/src/components/ExportPreview.vue
  - contract_info/README.md
  - contract_info/FIELD_SPEC.md
  - contract_info/.env.example
  - contract_info/frontend/.env.example
autonomous: true
requirements:
  - UI-01
  - UI-02
  - TEST-03
user_setup: []

must_haves:
  truths:
    - "ExportPreview 有申赎费率 Tab 且展示 subscription_rows"
    - "README 描述 v1.1 五表+path B+校验+LLM 配置"
    - "FIELD_SPEC 含 validation_result 与五表说明"
    - ".env.example 含 OPENAI_* 与 alembic 提示"
  artifacts:
    - path: "contract_info/frontend/src/components/ExportPreview.vue"
      provides: "五表预览"
      contains: "申赎"
    - path: "contract_info/README.md"
      provides: "运营文档"
  key_links:
    - from: "frontend/src/components/ExportPreview.vue"
      to: "GET /preview"
      via: "subscription_rows"
      pattern: "subscription_columns"
---

<objective>
ExportPreview 申赎 Tab；README / FIELD_SPEC / .env.example 更新；阶段验收文档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/10-integration-docs/10-CONTEXT.md
@contract_info/README.md
@contract_info/FIELD_SPEC.md
</context>

<tasks>

<task id="10-03-01" type="auto">
  <name>任务 1：ExportPreview 申赎 Tab</name>
  <files>
    contract_info/frontend/src/api/types.ts
    contract_info/frontend/src/components/ExportPreview.vue
  </files>
  <action>
`JobPreview` 增加 `subscription_columns`、`subscription_rows`。

新增 `el-tab-pane`「申赎费率（N 行）」— 动态列渲染（同 fee tab）。

  </action>
  <verify>
    <automated>cd contract_info/frontend && npm run build</automated>
  </verify>
</task>

<task id="10-03-02" type="auto">
  <name>任务 2：README 更新</name>
  <files>
    contract_info/README.md
  </files>
  <action>
章节更新：
- **v1.1 能力**：5 表导出、路径 B JSON、LLM 摘录校验
- **环境变量**：`OPENAI_API_KEY` / `OPENAI_BASE_URL` / `LLM_MODEL`（生产必填方可 LLM+校验）
- **数据库**：`alembic upgrade head`（迁移至 007）
- **Docker**：compose 中挂载 `.env` 说明
- **前端**：五下载、path B 面板、校验面板说明

不单独 DEPLOY.md。
  </action>
  <verify>
    <manual>README 与当前端点/文件名一致</manual>
  </verify>
</task>

<task id="10-03-03" type="auto">
  <name>任务 3：FIELD_SPEC + .env.example</name>
  <files>
    contract_info/FIELD_SPEC.md
    contract_info/.env.example
    contract_info/frontend/.env.example
  </files>
  <action>
**FIELD_SPEC.md**：
- 五表导出文件列表（含 subscription_fee_rates.xlsx）
- 路径 B JSON 结构（performance_fee、open_day、source_snippets）
- `validation_result` vs `extraction_warnings`
- API 端点索引（download×5、path-b、validation）

**.env.example`**：
- `OPENAI_API_KEY=`、`OPENAI_BASE_URL=`、`LLM_MODEL=`
- `DATABASE_URL`、`API_KEY` 注释
- 一行 `alembic upgrade head` 提示

**frontend/.env.example**：`VITE_API_KEY` 注释保留
  </action>
  <verify>
    <manual>对照 backend config.py 字段名</manual>
  </verify>
</task>

<task id="10-03-04" type="auto">
  <name>任务 4：10-VERIFICATION + 里程碑勾选</name>
  <files>
    contract_info/.planning/phases/10-integration-docs/10-VERIFICATION.md
  </files>
  <action>
编写 `10-VERIFICATION.md`：
- 手工 UAT：上传合同 → run → 见 5 下载、展开 path B、校验、预览 5 Tab
- 自动化：`pytest -m "not llm"` + `npm run build`

更新 ROADMAP Phase 10 plans 勾选（execute 后）。

  </action>
</task>

</tasks>

<verification>
`npm run build`；`pytest backend/tests -q -m "not llm"`
</verification>

<success_criteria>
TEST-03 / UI-02；v1.1 Phase 10 运营交付面完整
</success_criteria>

<output>
完成后创建 `10-03-SUMMARY.md`、`10-VERIFICATION.md`
</output>
