---
phase: "10-integration-docs"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - contract_info/backend/app/services/preview_service.py
  - contract_info/backend/app/api/schemas.py
  - contract_info/backend/app/api/routes/jobs.py
  - contract_info/backend/tests/test_api_preview.py
autonomous: true
requirements:
  - UI-01
  - API-02
user_setup: []

must_haves:
  truths:
    - "GET /preview 返回 subscription_columns 与 subscription_rows"
    - "exported 时优先读 subscription_fee_rates.xlsx"
    - "extracted 兜底读 extraction_result.subscription_fees"
  artifacts:
    - path: "contract_info/backend/app/services/preview_service.py"
      provides: "申赎预览数据"
      contains: "subscription_columns"
  key_links:
    - from: "backend/app/api/schemas.py"
      to: "preview_service.build_job_preview"
      via: "JobPreviewResponse"
      pattern: "subscription_rows"
---

<objective>
扩展 preview API：申赎费率列/行；JobPreviewResponse schema；API 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/10-integration-docs/10-CONTEXT.md
@contract_info/backend/app/services/preview_service.py
@contract_info/backend/app/export/column_map.py
</context>

<tasks>

<task id="10-01-01" type="auto">
  <name>任务 1：preview_service 申赎</name>
  <files>
    contract_info/backend/app/services/preview_service.py
  </files>
  <action>
新增 `_subscription_from_extraction(extraction)`（仿 `_fee_from_extraction`，用 `template_header_for_subscription_key`）。

`build_job_preview`：
- `exported` + `subscription_xlsx_path` → `_read_xlsx_table(SUBSCRIPTION_SHEET, ...)`
- 无 xlsx 行时从 `extraction_result.subscription_fees` 兜底
- 返回 dict 增加 `subscription_columns`、`subscription_rows`

空数据时仍返回空列表（不 fail）；与 fee/lock 一致。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_preview.py -q -k subscription 2>&1 || python -m pytest backend/tests/test_api_preview.py -q</automated>
  </verify>
</task>

<task id="10-01-02" type="auto">
  <name>任务 2：JobPreviewResponse schema</name>
  <files>
    contract_info/backend/app/api/schemas.py
    contract_info/backend/app/api/routes/jobs.py
  </files>
  <action>
`JobPreviewResponse` 增加：
- `subscription_columns: list[str]`
- `subscription_rows: list[dict[str, str | None]]`

`preview_job` 路由映射新字段。
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.api.schemas import JobPreviewResponse; JobPreviewResponse(subscription_columns=[], subscription_rows=[])"</automated>
  </verify>
</task>

<task id="10-01-03" type="auto">
  <name>任务 3：test_api_preview 申赎</name>
  <files>
    contract_info/backend/tests/test_api_preview.py
  </files>
  <action>
新增或扩展测试：
- mock record `extracted` + `extraction_result.subscription_fees` → 200 且 `subscription_rows` 非空
- 可选：`exported` + 临时 xlsx 路径（仿 fee 测试）

  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_preview.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
`pytest backend/tests/test_api_preview.py -q`
</verification>

<success_criteria>
D-PR02：preview API 可供前端申赎 Tab 使用
</success_criteria>

<output>
完成后创建 `10-01-SUMMARY.md`
</output>
