---
phase: 19-multi-upload
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - frontend/src/composables/useJobsPoll.ts
  - frontend/src/components/upload/UploadJobCard.vue
autonomous: true
requirements:
  - FE-01
  - UP-02

must_haves:
  truths:
    - "D-10/D-11: 单 interval 轮询多个 jobId"
    - "D-04: UploadJobCard 独立展示步骤与操作"
    - "activate(jobId) 在 run 后强制轮询"
  artifacts:
    - path: frontend/src/composables/useJobsPoll.ts
    - path: frontend/src/components/upload/UploadJobCard.vue
---

<objective>
Implement multi-job polling registry (FE-01) and reusable per-job upload card (UP-02 building block).

Output: useJobsPoll.ts, UploadJobCard.vue.
</objective>

<context>
@contract_info/frontend/src/composables/useJobPoll.ts
@contract_info/frontend/src/views/UploadView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: useJobsPoll (D-10, D-11)</name>
  <files>frontend/src/composables/useJobsPoll.ts</files>
  <action>
    ```ts
    type Entry = { status: Ref<string>; onUpdate: (d: JobDetail) => void; forcePoll: boolean }
  register(jobId, initialStatus, onUpdate) => unregister on duplicate replace
  tick(): for each entry where shouldPoll(jobId, status) → getJob
  activate(jobId): forcePoll true, tick once
  onUnmounted: clear interval
    ```
    Export `jobsPoll` singleton or factory `createJobsPoll()` used by UploadView one instance.
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 2: UploadJobCard (D-04, D-09)</name>
  <files>frontend/src/components/upload/UploadJobCard.vue</files>
  <action>
    Props: `job: UploadSessionJob` (detail required when loaded), `running: boolean`
    Emits: `start`, `view-result`
    UI: progress-header, ProcessStepper, error alert, 开始/重试/查看结果 buttons
    Use statusLabelZh, canStartRun, canRetry, isPipelineActive from status constants
  </action>
</task>

</tasks>

<verification>npm run typecheck</verification>
