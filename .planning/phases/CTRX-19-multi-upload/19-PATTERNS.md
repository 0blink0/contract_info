# Phase 19 — Patterns

| New | Analog |
|-----|--------|
| `useJobsPoll` | `useJobPoll` × N in one timer |
| `UploadJobCard` | UploadView progress-card block |
| `GET /jobs/concurrency` | `count_in_progress` in run_job |
| Multi upload | Parallel `upload()` calls |
