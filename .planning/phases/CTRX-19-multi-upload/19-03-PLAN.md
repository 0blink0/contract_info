---
phase: 19-multi-upload
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - frontend/src/views/UploadView.vue
  - frontend/tests/router/upload-multi.test.mjs
autonomous: true
requirements:
  - UP-01
  - UP-02
  - UP-03
  - FE-01

must_haves:
  truths:
    - "D-01/D-03: multiple limit 3 parallel upload"
    - "D-05/D-06/D-12: UploadView 多卡 + useJobsPoll"
    - "D-07/D-08: 批量开始处理 + 409 处理"
    - "D-14: 展示 concurrency active/max"
    - "D-15: upload-multi.test.mjs"
  artifacts:
    - path: frontend/src/views/UploadView.vue
    - path: frontend/tests/router/upload-multi.test.mjs
---

<objective>
Rewrite UploadView for ≤3 multi-file upload, batch run, and concurrency UX (UP-01~03, FE-01).

Output: production UploadView, static tests, phase gate.
</objective>

<context>
@contract_info/.planning/phases/CTRX-19-multi-upload/19-VALIDATION.md
@contract_info/frontend/src/views/UploadView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: UploadView multi-file (D-01–D-06, D-12)</name>
  <files>frontend/src/views/UploadView.vue</files>
  <action>
    - Remove single activeJobId / single useJobPoll.
    - `sessionJobs = ref<UploadSessionJob[]>([])` max length 3.
    - el-upload: multiple, limit=3, on-exceed, on-change handler:
      - Filter new files from change (handle batch select — use fileList or accumulate).
      - For each new raw file: upload → push session job → registerJobsPoll.
    - Implement `handleFileChange(uploadFile, uploadFiles)` pattern: only upload files not yet in session (track by name+size or upload uid).
    - `useJobsPoll` instance; each job register on add, unregister on remove if clear button added (optional).
    - Poll refresh updates matching sessionJobs[i].detail and status.
    - Show concurrency banner from periodic getJobConcurrency (poll every 5s or on tick).
  </action>
</task>

<task type="auto">
  <name>Task 2: Batch run toolbar (D-07, D-08, D-14)</name>
  <files>frontend/src/views/UploadView.vue</files>
  <action>
    -「全部开始处理」: disabled when no pending OR concurrency.active >= max.
    - Loop pending jobs: runJob → activate(jobId); catch parse 409 message.
    - Helper `parseRunError(e)` for string or `{ detail: string }`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Static tests + gate (D-15, D-16)</name>
  <files>frontend/tests/router/upload-multi.test.mjs</files>
  <action>
    Assert UploadView.vue contains: useJobsPoll, UploadJobCard, :limit="3" or limit={3}, multiple, 全部开始处理, getJobConcurrency.
    Assert useJobsPoll.ts exists and setInterval.
    Run frontend test:router, typecheck, build; pytest concurrency test.
  </action>
</task>

</tasks>

<verification>
See 19-VALIDATION.md
</verification>

<success_criteria>
ROADMAP Phase 19 four success criteria met via manual UAT
</success_criteria>
