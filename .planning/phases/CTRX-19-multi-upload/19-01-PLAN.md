---
phase: 19-multi-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/schemas.py
  - backend/app/api/routes/jobs.py
  - backend/tests/test_api_concurrency.py
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/constants/upload.ts
autonomous: true
requirements:
  - UP-02

must_haves:
  truths:
    - "D-13: GET /jobs/concurrency 返回 active 与 max=3"
    - "D-14: 前端 getJobConcurrency 可调用"
    - "MAX_PARALLEL_JOBS 常量=3"
  artifacts:
    - path: backend/app/api/routes/jobs.py
      provides: GET /jobs/concurrency
    - path: frontend/src/api/client.ts
      provides: getJobConcurrency
---

<objective>
Add lightweight concurrency read API for upload page (D-13) and frontend client/constants.

Output: JobConcurrencyResponse, route, test, client helper.
</objective>

<context>
@contract_info/backend/app/services/pipeline_service.py
@contract_info/backend/app/api/routes/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend concurrency endpoint</name>
  <files>backend/app/api/schemas.py, backend/app/api/routes/jobs.py, backend/tests/test_api_concurrency.py</files>
  <action>
    - Add `JobConcurrencyResponse { active: int, max: int }` with max=3 constant.
    - `@router.get("/concurrency", response_model=JobConcurrencyResponse)` before `/{job_id}` routes if needed (order: place after list jobs).
    - `active = count_in_progress()`, `max = 3`.
    - Test: mock count_in_progress returns 2 → GET returns active=2 max=3.
  </action>
  <verify>pytest backend/tests/test_api_concurrency.py -q</verify>
</task>

<task type="auto">
  <name>Task 2: Frontend client + constant</name>
  <files>frontend/src/api/types.ts, frontend/src/api/client.ts, frontend/src/constants/upload.ts</files>
  <action>
    - `export const MAX_UPLOAD_FILES = 3`, `MAX_PARALLEL_JOBS = 3`
  - `JobConcurrencyResponse` type
    - `getJobConcurrency(): Promise<JobConcurrencyResponse>`
  </action>
  <verify>npm run typecheck</verify>
</task>

</tasks>
