---
phase: 16-detail-nav
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - frontend/src/layouts/AppLayout.vue
  - frontend/src/views/JobHubView.vue
  - frontend/src/views/JobTableView.vue
  - frontend/src/views/JobFieldBView.vue
  - frontend/src/views/FileListView.vue
  - frontend/src/views/UploadView.vue
autonomous: true
requirements:
  - NAV-01
  - NAV-03

must_haves:
  truths:
    - "D-09/D-10: AppLayout 在 route.params.id 时显示「文件详情」el-sub-menu"
    - "D-11/D-13: 子菜单默认展开；子项 default-active 为 route.path"
    - "D-12: /jobs/:id/* 时全局「文件列表」仍 active /jobs"
    - "D-08: 六项链接路径符合 /jobs/:id、/tables/:key、/field-b"
    - "D-14: FileListView 与 UploadView 跳转 job-hub"
    - "D-17: Hub 占位 — 六张导航卡片链到子路由"
    - "D-18/D-19: 表页与字段 B 占位文案，不调 preview/verification API"
  artifacts:
    - path: frontend/src/layouts/AppLayout.vue
      provides: JobDetailSubNav six links
    - path: frontend/src/views/JobHubView.vue
      provides: hub navigation cards
    - path: frontend/src/views/JobTableView.vue
      provides: table placeholder with section label
    - path: frontend/src/views/JobFieldBView.vue
      provides: field-b placeholder
  key_links:
    - from: frontend/src/layouts/AppLayout.vue
      to: frontend/src/constants/jobSections.ts
      via: build menu indices from JOB_TABLE_SECTIONS
      pattern: JOB_TABLE_SECTIONS
    - from: frontend/src/views/JobHubView.vue
      to: frontend/src/router/index.ts
      via: RouterLink name job-table / job-field-b
      pattern: job-table
---

<objective>
Deliver AppLayout job submenu (NAV-01), stub child views (D-17–D-19), and entry routes defaulting to Hub (NAV-03).

Purpose: Satisfy navigability and highlight requirements before Phase 17 fills table pages.
Output: AppLayout submenu, JobHubView/JobTableView/JobFieldBView, updated list/upload navigation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-16-detail-nav/16-CONTEXT.md
@contract_info/frontend/src/layouts/AppLayout.vue
@contract_info/frontend/src/views/FileListView.vue
@contract_info/frontend/src/views/UploadView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppLayout 文件详情子菜单 (D-09–D-13)</name>
  <files>frontend/src/layouts/AppLayout.vue</files>
  <action>
    - `const jobId = computed(() => typeof route.params.id === 'string' ? route.params.id : null)`.
    - When `jobId`: render `el-sub-menu` index `job-detail-nav`, title「文件详情」.
    - `default-openeds`: include `job-detail-nav` when jobId set (use `:default-openeds` binding).
    - Child `el-menu-item` indices:
      - Hub: `/jobs/${jobId}` (or router-link to job-hub)
      - Each table: `/jobs/${jobId}/tables/${key}`
      - Field B: `/jobs/${jobId}/field-b`
    - Separate `jobSubActive = computed(() => route.path)` for submenu `:default-active` on a second `el-menu` OR use single menu with sub-menu — avoid breaking global three items.
    - Keep `activeMenu` returning `/jobs` for any `/jobs/:id` path (D-12).
  </action>
  <verify>Manual: enter job → six sub-items visible and clickable</verify>
</task>

<task type="auto">
  <name>Task 2: JobHubView 导航占位 (D-17)</name>
  <files>frontend/src/views/JobHubView.vue</files>
  <action>
    - `useJobDetailInject()` for optional status display (filename from detail).
    - Title「任务总览」+ 简短说明（完整摘要 Phase 18）.
    - `el-row` / cards: 5 tables + 字段 B，each `RouterLink` to `job-table` with params or path to `job-field-b`.
    - No ValidationPanel / ExportPreview.
  </action>
  <verify>Hub cards navigate without full page reload losing Layout state</verify>
</task>

<task type="auto">
  <name>Task 3: JobTableView + JobFieldBView 占位 (D-18, D-19)</name>
  <files>frontend/src/views/JobTableView.vue, frontend/src/views/JobFieldBView.vue</files>
  <action>
    - JobTableView: read `route.params.tableKey`, `sectionLabel` when valid, else rely on guard.
    - inject context — display status tag only; **no** getJob / useJobPoll / preview API calls.
    - Placeholder: `el-empty` or alert「本页编辑与核对功能将在下一阶段开放」.
    - JobFieldBView: title「字段 B」+ same placeholder; no PathBPanel.
  </action>
  <verify>grep preview/verification/getJob in JobTableView — only inject read</verify>
</task>

<task type="auto">
  <name>Task 4: 入口改为 job-hub (D-14)</name>
  <files>frontend/src/views/FileListView.vue, frontend/src/views/UploadView.vue</files>
  <action>
    - Replace `name: 'job-detail'` with `name: 'job-hub'` everywhere.
    - Grep repo for `job-detail` route name — update all references.
  </action>
  <verify>grep job-detail frontend/src — zero route name references</verify>
</task>

</tasks>

<verification>
- Manual walk ROADMAP criteria 1–3
- `cd frontend && npm run typecheck`
</verification>

<success_criteria>
- Submenu six items match routes; list opens Hub
- Stub pages show placeholders only
</success_criteria>
