---
phase: 16-detail-nav
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - frontend/tests/router/job-detail-routes.test.mjs
autonomous: true
requirements:
  - NAV-02
  - NAV-03
  - FE-02

must_haves:
  truths:
    - "D-26: 路由单元测试覆盖 job-hub、tables/:tableKey、field-b、isValidTableKey guard"
    - "D-27: typecheck + test:router 作为阶段自动化门禁"
    - "子页 views 无 useJobPoll import（静态 grep 测试）"
  artifacts:
    - path: frontend/tests/router/job-detail-routes.test.mjs
      provides: route contract assertions
  key_links:
    - from: frontend/tests/router/job-detail-routes.test.mjs
      to: frontend/src/router/index.ts
      via: fs.readFileSync static analysis
      pattern: job-hub
---

<objective>
Lock Phase 16 routing and poll contracts with automated router tests (D-26) and phase validation gate (D-27).

Purpose: Prevent regressions when Phase 17 adds API calls to child views.
Output: job-detail-routes.test.mjs; green test:router + typecheck.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-16-detail-nav/16-VALIDATION.md
@contract_info/frontend/tests/router/wizard-gate.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router contract tests</name>
  <files>frontend/tests/router/job-detail-routes.test.mjs</files>
  <action>
    Create tests (node:test + assert) reading `frontend/src/router/index.ts`:
    1. Contains `JobDetailLayout.vue` as `/jobs/:id` component.
    2. Contains `name: 'job-hub'`, `name: 'job-table'`, `name: 'job-field-b'`.
    3. Contains path `tables/:tableKey` and `field-b`.
    4. Contains `isValidTableKey` in beforeEnter (or import from jobSections).
    5. Does **not** contain `name: 'job-detail'` or `JobDetailView.vue`.
    6. `jobSections.ts` exports `product-elements` and `subscription-fee-rates`.
  </action>
  <verify>cd frontend && npm run test:router</verify>
</task>

<task type="auto">
  <name>Task 2: No child polling guard</name>
  <files>frontend/tests/router/job-detail-routes.test.mjs</files>
  <action>
    - Read `JobHubView.vue`, `JobTableView.vue`, `JobFieldBView.vue` as text.
    - Assert none import `useJobPoll`.
    - Assert `JobDetailLayout.vue` contains `useJobPoll`.
  </action>
  <verify>npm run test:router</verify>
</task>

<task type="auto">
  <name>Task 3: Phase gate</name>
  <files></files>
  <action>
    Run `npm run typecheck` and `npm run build` in frontend.
    Document manual UAT checklist in 16-03-SUMMARY.md after execution.
  </action>
  <verify>All commands exit 0</verify>
</task>

</tasks>

<verification>
```bash
cd contract_info/frontend && npm run test:router && npm run typecheck && npm run build
```
</verification>

<success_criteria>
- Router tests pass
- FE-02 grep guard passes
- Build succeeds
</success_criteria>
