---
phase: "06-extract-quality"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - contract_info/backend/tests/golden/README.md
  - contract_info/backend/tests/golden/conftest.py
  - contract_info/backend/tests/golden/fixtures/contract_expected.json
  - contract_info/backend/tests/golden/helpers/normalize.py
  - contract_info/backend/tests/golden/helpers/pipeline_runner.py
  - contract_info/backend/tests/golden/helpers/xlsx_diff.py
  - contract_info/backend/tests/golden/helpers/__init__.py
  - contract_info/backend/tests/test_shiyun_contract_rules.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "执行 `pytest backend/tests/golden/ -q -m \"not llm\"` 可收集用例（允许 skip 缺文件，但目录与 fixture 必须存在）"
    - "Critical 字段清单与四分类空值规则写在 tests/golden/README.md，与 06-FIELD-MATRIX §6 一致"
    - "合同真值来自 contract_expected.json，不依赖黄金 xlsx 的当事人列"
    - "石云/福禄 docx 路径与 D-G01 的 (1).docx 文件名一致"
  artifacts:
    - path: "contract_info/backend/tests/golden/README.md"
      provides: "Critical 清单、归一化、CI/LLM 说明"
    - path: "contract_info/backend/tests/golden/fixtures/contract_expected.json"
      provides: "两份合同规则层 Critical 真值（含费率扩展位）"
    - path: "contract_info/backend/tests/golden/helpers/normalize.py"
      provides: "D-G03 归一化函数"
    - path: "contract_info/backend/tests/golden/helpers/pipeline_runner.py"
      provides: "parse→extract→export 无 LLM 一键调用"
    - path: "contract_info/backend/tests/golden/helpers/xlsx_diff.py"
      provides: "按基金全称读表、Critical diff 入口"
  key_links:
    - from: "backend/tests/golden/conftest.py"
      to: "example/*.(1).docx"
      via: "golden_docx_* fixture 固定路径"
      pattern: "石云中证1000.*\\(1\\)\\.docx"
    - from: "backend/tests/golden/helpers/xlsx_diff.py"
      to: "backend/app/export/column_map.py"
      via: "PRODUCT_SHEET / normalize_header"
      pattern: "from backend.app.export.column_map import"
---

<objective>
搭建 `tests/golden/` 基础设施：合同真值 fixture、归一化与 xlsx diff 助手、无 LLM 管道 runner；修正石云 docx 路径；文档化 Critical/空值四分类（D-F06、D-G02–G04）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/PROJECT.md
@contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md
@contract_info/.planning/phases/06-extract-quality/06-FIELD-MATRIX.md
@contract_info/.planning/phases/06-extract-quality/06-RESEARCH.md
@contract_info/.planning/phases/06-extract-quality/06-PATTERNS.md
@contract_info/.planning/phases/06-extract-quality/06-VALIDATION.md
@contract_info/example/_contract_keys.json
@contract_info/backend/tests/conftest.py
@contract_info/backend/tests/test_export_pipeline.py

<interfaces>
From `backend/tests/conftest.py`:
```python
PROJECT_ROOT = Path(__file__).resolve().parents[2]
EXAMPLE_DIR = PROJECT_ROOT / "example"
```

From `backend/app/export/pipeline.py`:
```python
def export_xlsx(extraction: dict[str, Any], file_id: uuid.UUID | str) -> tuple[str, str, str, str, list[dict[str, Any]]]
```

D-G01 固定路径（conftest 必须使用）:
- `example/石云中证1000资产进取一号私募证券投资基金-基金合同(1).docx`
- `example/石云福禄1000指数增强一号私募证券投资基金(1).docx`
- `example/产品要素 - 副本(1).xlsx`
- `example/产品运营费率导入模板.xlsx`
</interfaces>
</context>

<tasks>

<task id="06-01-01" type="auto">
  <name>任务 1：golden 目录、README、contract_expected fixture</name>
  <read_first>
    - contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-G01–G04、D-F01–F06）
    - contract_info/.planning/phases/06-extract-quality/06-FIELD-MATRIX.md（§2 四分类、§6 Critical）
    - contract_info/example/_contract_keys.json
    - contract_info/example/_golden_per_fund.json（可选：费率列参考）
  </read_first>
  <files>
    contract_info/backend/tests/golden/README.md
    contract_info/backend/tests/golden/fixtures/contract_expected.json
    contract_info/backend/tests/golden/helpers/__init__.py
  </files>
  <action>
创建 `backend/tests/golden/` 包（含 `helpers/`、`fixtures/`）。

**README.md（中文，per D-F06 / D-G02）：**
- Critical 字段清单：与 FIELD-MATRIX §6 一致（基金全称、当事人、风险等级、投资经理、四要素、预警止损、开放日摘要、四类运营费率及 %）
- 空值四分类 A/B/C/D 与测试用语（D-F02）
- **合同真值 vs 黄金表**：Critical 对 `contract_expected.json`；禁止用黄金 xlsx 断言管理人/托管人/外包（D-F04、D-F05）
- 归一化表（D-G03）：日期 `yyyy/m/d`、`【】` 剥离、百分号、机构 trim、`无`/`不设`↔空
- CI 命令：`python -m pytest backend/tests/golden/ -q -m "not llm"`（D-CI01）
- LLM：`@pytest.mark.llm`、阈值 ≥80% critical 长文本非空（D-CI03）、DeepSeek 环境变量说明（D-CI04）
- 明确 **不包含** 申赎费率 xlsx pass/fail（D-E02）

**contract_expected.json：**
- 自 `example/_contract_keys.json` 迁移两份 docx 键
- 每基金增加：`基金全称`（与 docx 产品名一致）、`风险等级`（石云 R4；福禄按规则层实测填入）
- 嵌套 `fee_rates_by_type`：`管理费`/`托管费`/`销售服务费`/`基金服务费` → `rate_annual_pct` 字符串（石云：1、0.03、0.5、0.01；福禄补全后写入实测值）
- 可选 `lock_periods`：石云 180天；福禄空数组或「无子表行」

勿将黄金 xlsx 当事人写入 expected。
  </action>
  <verify>
    <automated>python -c "import json; from pathlib import Path; p=Path('contract_info/backend/tests/golden/fixtures/contract_expected.json'); d=json.loads(p.read_text(encoding='utf-8')); assert len(d)==2 and 'fee_rates_by_type' in next(iter(d.values()))"</automated>
  </verify>
  <acceptance_criteria>
    - README 含 §6 Critical 全表及四分类说明
    - JSON 含两个 D-G01 docx 键且含 `fee_rates_by_type`
    - README 明确申赎费率本阶段不测
  </acceptance_criteria>
  <done>文档与合同真值 fixture 可被下游测试 import</done>
</task>

<task id="06-01-02" type="auto">
  <name>任务 2：normalize / pipeline_runner / xlsx_diff 助手</name>
  <read_first>
    - contract_info/backend/app/export/date_format.py
    - contract_info/backend/app/export/column_map.py
    - contract_info/backend/app/export/xlsx_utils.py
    - contract_info/backend/tests/test_extract_pipeline.py（_LlmOff）
    - contract_info/.planning/phases/06-extract-quality/06-PATTERNS.md
  </read_first>
  <files>
    contract_info/backend/tests/golden/helpers/normalize.py
    contract_info/backend/tests/golden/helpers/pipeline_runner.py
    contract_info/backend/tests/golden/helpers/xlsx_diff.py
  </files>
  <action>
**normalize.py（D-G03）：**
- `normalize_cell(value)`：委托或复用 `normalize_date_slash`、去 `【】`、strip
- `normalize_pct(s)`：统一为可比较的百分数字符串（如 `1` vs `1%` vs `0.01` 文档化规则）
- `normalize_party_name(s)`：strip、全角空格
- `empty_equiv(a, b)`：`无`/`不设`/`""`/`None` 等价

**pipeline_runner.py：**
- `class LlmOff: available=False; model_name=""`
- `run_golden_pipeline(docx_path: Path, *, exports_root: Path) -> GoldenRunResult`（dataclass 或 TypedDict）：`parse_docx` → `document_to_dict` → `extract_document_sync(..., llm_client=LlmOff())` → `export_xlsx(model_dump(), uuid)`；`monkeypatch` 或参数注入 `exports_dir` 指向 `exports_root`（照抄 `test_export_pipeline` 模式）
- 返回 `extraction`、`paths`（product/fee/lock/share 四路径）

**xlsx_diff.py：**
- 常量从 `column_map` 导入 `PRODUCT_SHEET`、`PRODUCT_HEADER_ROW`、`FEE_SHEET` 等
- `find_product_row(wb_path, fund_full_name) -> int`
- `read_product_field(wb_path, row, header_name) -> str | None`
- `assert_critical_product(export_path, expected: dict, *, skip_party_from_golden=True)`：仅对比 contract_expected 中 Critical 键；**不**读取黄金 xlsx 的当事人
- `assert_fee_types_present(export_path, fund_name, expected_by_type: dict)`：按 `运营费类型` 聚合 rate%，不要求与黄金 A/B/C/D 行数 1:1（D-G02、RESEARCH Pattern 3）
- `CRITICAL_FROM_CONTRACT`、`A_CLASS_EMPTY_OK` 集合导出供测试使用

禁止在 `validate_export` 或运行时 pipeline 中 import 黄金 xlsx（D-E03）。
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.tests.golden.helpers.normalize import empty_equiv; from backend.tests.golden.helpers.pipeline_runner import LlmOff; assert empty_equiv('无','') and LlmOff().available is False"</automated>
  </verify>
  <acceptance_criteria>
    - 三个 helper 模块可无 pytest 单独 import
    - xlsx_diff 使用 column_map 表头语义，非手写列号
    - pipeline_runner 使用 LlmOff，不读取 OPENAI_API_KEY
  </acceptance_criteria>
  <done>Wave 2 export/规则测试可复用助手完成 diff 与 E2E</done>
</task>

<task id="06-01-03" type="auto">
  <name>任务 3：golden conftest + 修正石云 docx 路径</name>
  <read_first>
    - contract_info/backend/tests/test_shiyun_contract_rules.py
    - contract_info/backend/tests/golden/README.md（本 plan 任务 1 产出）
    - contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-G01、D-G04）
  </read_first>
  <files>
    contract_info/backend/tests/golden/conftest.py
    contract_info/backend/tests/test_shiyun_contract_rules.py
  </files>
  <action>
**conftest.py：**
- `GOLDEN_ROOT`、`FIXTURES`、`load_contract_expected()` → dict
- `@pytest.fixture(scope="module") def golden_docx_shiyun()` / `golden_docx_fulu()`：D-G01 路径，`is_file()` 否则 skip
- `@pytest.fixture def shiyun_document(golden_docx_shiyun)`：`parse_docx` + `document_to_dict`（与 test_shiyun 相同）
- `@pytest.fixture def fulu_document(golden_docx_fulu)`：同上
- `@pytest.fixture def golden_product_xlsx()` / `golden_fee_xlsx()`：D-G01 xlsx 路径
- `@pytest.fixture def tmp_exports(tmp_path, monkeypatch)`：patch `backend.app.export.pipeline.exports_dir` → `tmp_path/exports`

**修正 test_shiyun_contract_rules.py（per D-G01）：**
- `SHIYUN_DOCX` 改为 `…基金合同(1).docx`（带 `(1)` 后缀）
- 文件头注释注明：完整回归将迁至 `tests/golden/test_golden_rules_shiyun.py`（Wave 1），本文件保留以免破坏已有 import

不在本任务创建规则/导出测试文件（属 06-02/06-03）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_shiyun_contract_rules.py -q --co -q 2>nul | findstr test_shiyun</automated>
  </verify>
  <acceptance_criteria>
    - conftest fixture 路径与 example 目录实际文件名一致
    - `test_shiyun_contract_rules.py` 在 example 存在时不再因路径错误整模块 skip
    - `pytest backend/tests/golden/ --collect-only -q` 无 import 错误
  </acceptance_criteria>
  <done>Wave 1 可直接编写 test_golden_rules_* 与 test_golden_export</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest → example/ | 只读本地黄金样例；固定相对路径，不接受用户输入路径 |
| contract_expected → assert | 真值数据仅来自 fixture JSON，非运行时 DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | `.env` / API Key | mitigate | golden 默认测试不调用 LLM；README 注明勿提交密钥 |
| T-06-02 | T | xlsx_diff 路径 | mitigate | fixture 使用 `PROJECT_ROOT / "example"` 常量，禁止 `..` 拼接用户输入 |
</threat_model>

<verification>
- `cd contract_info && python -m pytest backend/tests/golden/ -q -m "not llm" --collect-only`
- `cd contract_info && python -m pytest backend/tests/test_shiyun_contract_rules.py -q -m "not llm"`（example 齐全时应执行而非 skip）
</verification>

<success_criteria>
- `tests/golden/` 目录、README、helpers、fixtures、conftest 齐备
- 石云 docx `(1)` 路径修正
- TEST-01 所需 runner/diff 骨架就绪（完整用例在 06-03）
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/06-extract-quality/06-01-SUMMARY.md`
</output>
