---
phase: "06-extract-quality"
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - contract_info/backend/tests/golden/test_golden_export.py
  - contract_info/backend/tests/golden/test_golden_llm.py
  - contract_info/backend/tests/golden/helpers/xlsx_diff.py
  - contract_info/backend/tests/golden/helpers/pipeline_runner.py
  - contract_info/backend/tests/golden/README.md
  - contract_info/pytest.ini
autonomous: true
requirements:
  - TEST-01
  - QUAL-03
user_setup:
  - service: DeepSeek / OpenAI compatible LLM
    why: "QUAL-03 可选 e2e；默认 CI 不运行"
    env_vars:
      - name: OPENAI_API_KEY
        source: "项目 .env 或 CI secret（仅 llm 标记作业）"
      - name: OPENAI_BASE_URL
        source: "DeepSeek 时设为 https://api.deepseek.com（见 README D-CI04）"
      - name: LLM_MODEL
        source: "如 deepseek-chat / 项目约定型号"

must_haves:
  truths:
    - "石云/福禄各有一条 parse→extract→export 无 LLM 用例，导出 xlsx Critical 对齐 contract_expected"
    - "导出 diff 按运营费类型聚合，不与黄金 A/B/C/D 行数硬比 1:1"
    - "四类产品 xlsx（产品要素、运营费率、锁定期、分级份额）均生成且可读"
    - "有 API Key 时 LLM 长文本非空率显著高于仅规则层（LlmOff 基线对比）且 ≥80%"
    - "默认 CI：`pytest -m \"not llm\"` 全绿"
  artifacts:
    - path: "contract_info/backend/tests/golden/test_golden_export.py"
      provides: "TEST-01 无 LLM E2E"
    - path: "contract_info/backend/tests/golden/test_golden_llm.py"
      provides: "QUAL-03 可选 LLM 基线"
  key_links:
    - from: "backend/tests/golden/test_golden_export.py"
      to: "backend/tests/golden/helpers/pipeline_runner.py"
      via: "run_golden_pipeline"
      pattern: "run_golden_pipeline"
    - from: "backend/tests/golden/test_golden_export.py"
      to: "backend/tests/golden/helpers/xlsx_diff.py"
      via: "assert_critical_product / assert_fee_types_present"
      pattern: "assert_critical"
---

<objective>
完成 TEST-01：`parse → extract → export` 对现有 4 类 xlsx 与合同真值/黄金结构的 diff；实现 QUAL-03 可选 LLM e2e（D-CI02、D-CI03）。默认 CI 仍不依赖 API Key。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-E01–E03、D-CI01–03）
@contract_info/.planning/phases/06-extract-quality/06-VALIDATION.md
@contract_info/backend/tests/golden/helpers/pipeline_runner.py
@contract_info/backend/tests/golden/helpers/xlsx_diff.py
@contract_info/backend/app/export/pipeline.py
@contract_info/backend/app/llm/client.py
@contract_info/backend/tests/test_extract_pipeline.py

<interfaces>
```python
# export_xlsx 返回四路径 + warnings
def export_xlsx(extraction: dict[str, Any], file_id: uuid.UUID | str) -> tuple[str, str, str, str, list]

# LlmClient（QUAL-03）
class LlmClient:
    @property
    def available(self) -> bool: ...
```

Critical 长文本字段（LLM 阈值计算）:
`投资目标`, `投资范围`, `投资限制`, `投资策略`, `风险等级`（与 README 一致）

MANUAL_ONLY / A 类允许空（不测失败）:
`基金代码`, `成立日期`, `备案日期`, MANUAL_ONLY 六列（见 FIELD_SPEC / README）
</interfaces>
</context>

<tasks>

<task id="06-03-01" type="auto">
  <name>任务 1：TEST-01 导出黄金 diff（石云 + 福禄，无 LLM）</name>
  <read_first>
    - contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-E01、D-G02、D-F05）
    - contract_info/backend/tests/golden/helpers/pipeline_runner.py
    - contract_info/backend/tests/golden/helpers/xlsx_diff.py
    - contract_info/backend/tests/test_export_pipeline.py
    - contract_info/example/产品要素 - 副本(1).xlsx（结构参考，当事人不断言）
  </read_first>
  <files>
    contract_info/backend/tests/golden/test_golden_export.py
    contract_info/backend/tests/golden/helpers/xlsx_diff.py
    contract_info/backend/tests/golden/helpers/pipeline_runner.py
  </files>
  <action>
**扩展 xlsx_diff.py（若 06-01 未实现完整）：**
- `assert_export_structure(paths)`：四文件存在；产品/费率 sheet 名与 `column_map` 一致；表头行可读
- `assert_lock_sheet(export_lock_path, fund_name, *, expect_rows: int | None)`：石云 expect_rows≥1；福禄 expect_rows==0
- `assert_share_sheet_extended(export_share_path)`：**仅**检查文件可打开、sheet 存在；无 LLM 时不 assert 行数=黄金（Extended/warn，RESEARCH Open Q2）
- `diff_extended_against_golden(export_path, golden_path, fund_name)`：软检查——黄金有列则导出表头亦含该列名（normalize_header）；**跳过** SKIP_GOLDEN_PARTY 列

**test_golden_export.py：**
- `@pytest.mark.parametrize("docx_fixture,expected_key", [("golden_docx_shiyun", shiyun键), ("golden_docx_fulu", fulu键)])`
- 使用 `tmp_exports` + `run_golden_pipeline`
- Critical：`assert_critical_product` + `assert_fee_types_present` 对 contract_expected
- 锁定期 xlsx：石云有 180天 相关行；福禄无行
- 费率 Extended（可选 `pytest.warns` 或单独 `test_fee_sheet_has_headers`）：打开 `golden_fee_xlsx` 对比表头集合，不要求行数相等
- **不包含** `产品申赎费率导入模板.xlsx`（D-E02）
- 全部 **无** `llm` marker

 fund_full_name 从 extraction `基金全称` 读取，与 D-G04 关联。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/golden/test_golden_export.py -x -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - 石云、福禄各至少 1 个 export E2E 测试通过
    - 当事人断言仅来自 contract_expected，未读取黄金 xlsx 管理人列
    - 申赎费率 xlsx 未出现在测试中
    - 四类产品 xlsx 路径均 assert 存在
  </acceptance_criteria>
  <done>TEST-01 在 Phase 6 验收范围内完成</done>
</task>

<task id="06-03-02" type="auto">
  <name>任务 2：QUAL-03 LLM 可选 e2e + 全量 golden 门禁</name>
  <read_first>
    - contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-CI02–04、D-M04）
    - contract_info/backend/tests/test_extract_pipeline.py
    - contract_info/backend/app/llm/client.py
    - contract_info/backend/tests/golden/README.md
    - contract_info/pytest.ini
  </read_first>
  <files>
    contract_info/backend/tests/golden/test_golden_llm.py
    contract_info/backend/tests/golden/README.md
    contract_info/pytest.ini
  </files>
  <action>
**test_golden_llm.py：**
- `@pytest.mark.llm`
- 辅助函数 `fill_rate(extraction, fields) -> float`：非空字段数 / len(fields)
- `CRITICAL_LLM_TEXT_FIELDS` = `投资目标`, `投资范围`, `投资限制`, `投资策略`, `风险等级`

**石云 + 福禄各一条（QUAL-03「两份额合同」）：**
- `@pytest.mark.parametrize` 或 `test_golden_llm_shiyun` + `test_golden_llm_fulu`
- 无 `OPENAI_API_KEY` 或 `not LlmClient().available` → `pytest.skip`
- 同一 docx：`ext_rule = extract_document_sync(doc, llm_client=_LlmOff())`；`ext_llm = extract_document_sync(doc, llm_client=LlmClient())`
- `rate_rule = fill_rate(ext_rule, CRITICAL_LLM_TEXT_FIELDS)`；`rate_llm = fill_rate(ext_llm, ...)`
- **断言：** `rate_llm > rate_rule`（QUAL-03「显著高于」）；且 `rate_llm >= 0.8`（D-CI03 阈值）
- 若 `rate_rule == 0`，则要求 `rate_llm >= 0.8` 即可（避免除零）

**README 更新：**
- 写明 LlmOff vs LlmClient 对比语义、80% 绝对阈值、字段列表
- 本地命令：`python -m pytest backend/tests/golden/ -m llm`
- DeepSeek：`OPENAI_BASE_URL` + `LLM_MODEL`（D-CI04）

**pytest.ini：** 确认已有 `llm` marker 描述（无则补充一行中文说明）

**门禁自检（本任务末尾）：**
- 文档化 Wave 2 完成命令：`python -m pytest backend/tests/golden/ -q -m "not llm"` 必须全绿

勿将黄金 xlsx 接入 `validate_export` 或生产 pipeline（D-E03）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/golden/ -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - 无 API Key 时 `test_golden_llm.py` 收集为 skip，不失败
    - 有 Key 时（执行者本地）`rate_llm > rate_rule` 且 `rate_llm >= 0.8`（石云、福禄各测）
    - `backend/tests/golden/` 下非 llm 套件全绿
  </acceptance_criteria>
  <done>QUAL-03 可选覆盖就绪；Phase 6 默认 CI 路径明确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → LLM API | 仅 `@pytest.mark.llm`；密钥来自环境变量 |
| golden xlsx → pytest | 只读；不进运行时 validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | I | OPENAI_API_KEY | mitigate | skip 无 Key；不 commit `.env` |
| T-06-06 | T | export diff | accept | 固定 example 路径；tmp_exports 隔离 |
</threat_model>

<verification>
- Wave 2 门禁：`cd contract_info && python -m pytest backend/tests/golden/ -q -m "not llm"`
- Phase 6 全量：`cd contract_info && python -m pytest backend/tests/ -q -m "not llm"`
- 可选：`cd contract_info && python -m pytest backend/tests/golden/test_golden_llm.py -m llm`（需 Key）
</verification>

<success_criteria>
- TEST-01 与 QUAL-03 均有自动化入口
- ROADMAP Phase 6 成功标准①②③可由 pytest 验证（①②→06-02，③→本 plan 非 llm 套件）
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/06-extract-quality/06-03-SUMMARY.md`
</output>
