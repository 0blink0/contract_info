---
phase: "06-extract-quality"
plan: "02"
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - contract_info/backend/app/extract/merge.py
  - contract_info/backend/app/extract/pipeline.py
  - contract_info/backend/app/extract/rules/fee_rules.py
  - contract_info/backend/app/extract/rules/lock_rules.py
  - contract_info/backend/tests/test_merge_field.py
  - contract_info/backend/tests/golden/test_golden_rules_shiyun.py
  - contract_info/backend/tests/golden/test_golden_rules_fulu.py
  - contract_info/backend/tests/golden/README.md
  - contract_info/backend/tests/golden/fixtures/contract_expected.json
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-04
user_setup: []

must_haves:
  truths:
    - "规则误抽标记（保证、登记为私募基金管理人等）不会压过 LLM 正确值"
    - "预警/止损合同「未设」时合并结果保持「无」"
    - "石云/福禄规则层 Critical 与 contract_expected.json 一致"
    - "福禄无锁定期主字段时不产生 20天+B份额 假锁定期子表行"
    - "福禄费用章可产出基金服务费费率行"
  artifacts:
    - path: "contract_info/backend/app/extract/merge.py"
      provides: "is_invalid_rule_value + field_name 感知 merge_field"
      contains: "is_invalid_rule_value"
    - path: "contract_info/backend/tests/test_merge_field.py"
      provides: "QUAL-04 单元覆盖"
    - path: "contract_info/backend/tests/golden/test_golden_rules_shiyun.py"
      provides: "石云规则 Critical 回归"
    - path: "contract_info/backend/tests/golden/test_golden_rules_fulu.py"
      provides: "福禄规则 Critical 回归"
  key_links:
    - from: "backend/app/extract/pipeline.py"
      to: "backend/app/extract/merge.py"
      via: "merge_field(..., field_name=key)"
      pattern: "field_name="
    - from: "backend/tests/golden/test_golden_rules_*.py"
      to: "fixtures/contract_expected.json"
      via: "load_contract_expected"
      pattern: "contract_expected"
---

<objective>
实现 QUAL-04 `merge_field` 误抽钩子（D-M01–M04）；修复福禄基金服务费与锁定期子表误匹配；建立石云/福禄规则层黄金回归（QUAL-01、QUAL-02），断言以合同真值为准。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md
@contract_info/.planning/phases/06-extract-quality/06-PATTERNS.md
@contract_info/.planning/phases/06-extract-quality/06-RESEARCH.md
@contract_info/backend/app/extract/merge.py
@contract_info/backend/app/extract/pipeline.py
@contract_info/backend/app/extract/rules/party_helpers.py
@contract_info/backend/app/extract/rules/fee_rules.py
@contract_info/backend/app/extract/rules/lock_rules.py
@contract_info/backend/tests/golden/conftest.py
@contract_info/backend/tests/golden/fixtures/contract_expected.json

<interfaces>
当前 merge_field 签名（将扩展 field_name）:
```python
def merge_field(rule_val: FieldValue | None, llm_val: FieldValue | None) -> FieldValue | None
```

party_helpers:
```python
def is_valid_party_name(name: str) -> bool
```

pipeline 内联 merge（须同步）:
```python
for key, llm_fv in llm_fields.items():
    merged_product[key] = merge_field(merged_product.get(key), llm_fv) or llm_fv
```
</interfaces>
</context>

<tasks>

<task id="06-02-01" type="auto" tdd="true">
  <name>任务 1：merge_field 误抽策略 + 单元测试（QUAL-04）</name>
  <read_first>
    - contract_info/.planning/phases/06-extract-quality/06-CONTEXT.md（D-M01–M04）
    - contract_info/backend/app/extract/merge.py
    - contract_info/backend/app/extract/rules/party_helpers.py
    - contract_info/backend/tests/test_export_validate.py（pytest 风格参考）
    - contract_info/.planning/phases/06-extract-quality/06-PATTERNS.md（merge 扩展示意）
  </read_first>
  <files>
    contract_info/backend/tests/test_merge_field.py
    contract_info/backend/app/extract/merge.py
    contract_info/backend/app/extract/pipeline.py
    contract_info/backend/tests/golden/README.md
  </files>
  <behavior>
    - 规则值含「保证」→ 视为 invalid，LLM 机构名胜出
    - 规则「北京石云科技有限公司」+ LLM 其他 → 当事人字段规则胜出（D-M01）
    - 规则预警线「无」+ LLM「0.8」→ 返回规则「无」（D-M03）
    - 规则投资目标 50 字 + LLM 500 字 → 返回更长者（D-M04）
    - 规则空 + LLM 有值 → LLM
  </behavior>
  <action>
**先写 `test_merge_field.py`（RED），再改 merge.py（GREEN）。**

在 `merge.py` 新增（集中实现，勿 per-field 复制 marker 列表 per D-M02）:
- `_PARTY_FIELDS = frozenset({"管理人","托管人","投资顾问","外包机构"})`
- `_LONG_TEXT_FIELDS = frozenset({"投资目标","投资范围","投资限制","投资策略","风险等级"})`
- `_MIS_EXTRACT_MARKERS`：与 CONTEXT 一致（保证、登记为私募基金管理人、若根据、所涉风险、经营风险、技术系统）
- `is_invalid_rule_value(field_name: str, value: object) -> bool`：空值 invalid；当事人走 `is_valid_party_name`；其余字段含 marker 则 invalid
- `merge_field(..., *, field_name: str = "")`：
  - 计算 `effective_rule`（invalid 则 None）
  - `预警线`/`止损线`：effective_rule.value == `无` → 直接返回 effective_rule
  - `_LONG_TEXT_FIELDS`：双方非空时取 `len(str(value))` 更大者（保留对应 FieldValue 元数据）
  - `_PARTY_FIELDS`：effective_rule 存在则优先 effective_rule
  - 其余：在 effective_rule 与 llm 间按原 confidence 逻辑，但 invalid 规则不参与

更新 `merge_extraction` 与 `pipeline.py` 两处循环：`merge_field(..., field_name=key)`。

**README.md** 增加「合并策略」小节：简述 D-M01–M04 与 `is_invalid_rule_value`（满足 QUAL-04 文档化）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_merge_field.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_merge_field.py` ≥5 个用例全部通过
    - `merge_field` 带 `field_name` 且 pipeline 调用已同步
    - README 有合并策略说明
  </acceptance_criteria>
  <done>误抽规则值不再仅凭 confidence 压过 LLM</done>
</task>

<task id="06-02-02" type="auto">
  <name>任务 2：福禄 fee_rules 基金服务费 + lock_rules 假阳性修复</name>
  <read_first>
    - contract_info/backend/app/extract/rules/fee_rules.py
    - contract_info/backend/app/extract/rules/lock_rules.py
    - contract_info/.planning/phases/06-extract-quality/06-RESEARCH.md（Pitfall 2、Open Q1）
    - contract_info/.planning/phases/06-extract-quality/06-FIELD-MATRIX.md（§3 第 10 项）
  </read_first>
  <files>
    contract_info/backend/app/extract/rules/fee_rules.py
    contract_info/backend/app/extract/rules/lock_rules.py
    contract_info/backend/tests/golden/fixtures/contract_expected.json
  </files>
  <action>
**fee_rules.py（福禄 Critical per FIELD-MATRIX #10）：**
- 确认 `_FEE_TYPE_PATTERNS` 含 `基金服务费|运营服务费|运营外包服务费`
- 若 `_rates_from_fee_chapter` 漏匹配福禄表述：扩展费用章 regex/同义词，使福禄 docx 规则层 `extract_fee_rates` 产出 `基金服务费` 行及 `rate_annual_pct`
- 运行本地 extract 后把实测 rate 写入 `contract_expected.json` 福禄条目的 `fee_rates_by_type`

**lock_rules.py（RESEARCH Pitfall 2）：**
- 当 `lock_summary` 为空且无 `has_lock`（无「无锁定期」以外的有效锁定期文本）时，在 `_RE_LOCK_TIME.search(combined)` **之前** `return []`
- 避免从申赎章节全文误匹配 `20天` + `B份额`
- 石云 180天 行为保持不变：有 `lock_summary` 时仍生成子表行

勿改动申赎费率或路径 B 相关代码。
  </action>
  <verify>
    <automated>cd contract_info && python -c "from pathlib import Path; from backend.app.parse import parse_docx; from backend.app.parse.schemas import document_to_dict; from backend.app.extract.section_windows import build_section_windows; from backend.app.extract.rules.lock_rules import extract_lock_periods_rules; from backend.app.extract.rules.product_rules import extract_product_rules; from backend.app.extract.rules.fee_rules import extract_fee_rates; p=Path('example/石云福禄1000指数增强一号私募证券投资基金(1).docx'); d=document_to_dict(parse_docx(str(p))); w,_=build_section_windows(d); pr=extract_product_rules(d,w); fn=str(pr['基金全称'].value); rows=extract_lock_periods_rules(fn, pr.get('锁定期'), w.get('subscription','')); assert rows==[]; fees=extract_fee_rates(d,w,document=d); assert any(r.get('运营费类型')=='基金服务费' for r in fees)"</automated>
  </verify>
  <acceptance_criteria>
    - 福禄 pipeline 无 LLM 时 `extract_lock_periods_rules` 返回 `[]`
    - 福禄 `extract_fee_rates` 含 `基金服务费` 且 rate 与 contract_expected 一致
    - 石云 `test_shiyun_fee_rates` 仍通过
  </acceptance_criteria>
  <done>福禄规则层已知缺口关闭</done>
</task>

<task id="06-02-03" type="auto">
  <name>任务 3：石云/福禄规则层黄金测试（QUAL-01、QUAL-02）</name>
  <read_first>
    - contract_info/backend/tests/test_shiyun_contract_rules.py
    - contract_info/backend/tests/golden/conftest.py
    - contract_info/backend/tests/golden/fixtures/contract_expected.json
    - contract_info/.planning/phases/06-extract-quality/06-FIELD-MATRIX.md（§4、§6）
  </read_first>
  <files>
    contract_info/backend/tests/golden/test_golden_rules_shiyun.py
    contract_info/backend/tests/golden/test_golden_rules_fulu.py
    contract_info/backend/tests/test_shiyun_contract_rules.py
  </files>
  <action>
**test_golden_rules_shiyun.py：**
- 使用 `shiyun_document` fixture + `load_contract_expected()` 石云键
- `test_critical_parties`：管理人/托管人/外包与 JSON 一致；`is_valid_party_name` 对输出为 True（QUAL-01）
- `test_critical_product_fields`：锁定期、预警/止损、风险等级、投资经理、开放日规则（子串或 normalize 后相等）
- `test_critical_fee_rates_by_type`：与 JSON `fee_rates_by_type` 一致（聚合 by 运营费类型）
- 投资目标/策略：规则层允许片段断言（子串）；完整长文本留给 `@pytest.mark.llm`（06-03）
- 从 `test_shiyun_contract_rules` **迁移**断言逻辑；旧文件改为 `pytest.importorskip` 转发或仅保留 `test_shiyun_fee_rates` 薄包装调用 golden，避免双份维护

**test_golden_rules_fulu.py：**
- `fulu_document` + JSON 福禄键
- `test_critical_parties`：托管人=招商证券、管理人=石云科技股份有限公司（与 contract_expected 一致）、`is_valid_party_name`（QUAL-01）
- `test_critical_product_fields`：与石云对称 — **开放日规则**、**投资经理**、**风险等级**、锁定期主字段空、预警/止损=无（子串或 normalize 后与 JSON 一致，§6 Critical）
- `test_critical_fee_rates_by_type`：含基金服务费及 JSON 中四类费率
- `extract_lock_periods_rules` → `[]`

**禁止**断言黄金 xlsx 中的管理人「弈倍」或福禄黄金托管人「华福」（D-F04）。

全部用例 **无** `@pytest.mark.llm`。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/golden/test_golden_rules_shiyun.py backend/tests/golden/test_golden_rules_fulu.py -x -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - 两文件合计 ≥10 个测试通过（example 齐全时 0 skip）；福禄含 `test_critical_product_fields`
    - 失败信息打印字段名与 expected/actual
    - 无 golden xlsx 当事人 hard assert
  </acceptance_criteria>
  <done>QUAL-01/02 规则层回归 CI 可跑且默认不依赖 API Key</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge | 规则/LLM FieldValue 合并；误抽标记为输入卫生（V5） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T | merge_field | mitigate | `is_invalid_rule_value` + party `is_valid_party_name` |
| T-06-04 | I | LLM snippet in tests | accept | 规则测试不调用 LLM |
</threat_model>

<verification>
- `cd contract_info && python -m pytest backend/tests/test_merge_field.py backend/tests/golden/test_golden_rules_*.py -q -m "not llm"`
- `cd contract_info && python -m pytest backend/tests/ -q -m "not llm"`（Wave 1 合并门禁）
</verification>

<success_criteria>
- merge 策略有测试与 README 文档
- 石云/福禄规则 Critical 全绿
- 福禄锁定期子表假行与基金服务费缺口修复
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/06-extract-quality/06-02-SUMMARY.md`
</output>
