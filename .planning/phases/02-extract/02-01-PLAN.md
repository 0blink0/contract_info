---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contract_info/requirements.txt
  - contract_info/.env.example
  - contract_info/README.md
  - contract_info/scripts/export_dicts.py
  - contract_info/dicts/.gitkeep
  - contract_info/backend/app/config.py
  - contract_info/backend/app/models/contract_file.py
  - contract_info/alembic/versions/002_extraction_columns.py
  - contract_info/backend/app/extract/__init__.py
  - contract_info/backend/app/extract/schemas.py
  - contract_info/backend/app/llm/__init__.py
  - contract_info/backend/app/llm/client.py
autonomous: true
requirements:
  - EXT-01
  - EXT-03
  - EXT-04
---

<objective>
Phase 2 基础设施：Alembic 增列、`extraction_result` Pydantic 契约、字典 JSON 导出脚本、OpenAI 兼容 LLM 客户端与配置。
</objective>

<threat_model>
<trust_boundary>应用 ↔ LLM API（密钥泄露、提示注入导致异常 JSON）。</trust_boundary>
<stride_register>
- threat_id: T-02-01
  category: I
  component: .env / logs
  disposition: mitigated
  mitigation_plan: 不记录 API Key；LLM 日志仅记 chapter 与 token 用量占位。
- threat_id: T-02-02
  category: T
  component: LLM response
  disposition: mitigated
  mitigation_plan: Pydantic strict parse；失败重试后 warnings，不执行任意代码。
</stride_register>
</threat_model>

<must_haves>
- `alembic upgrade head` 后 `contract_files` 含 `extraction_result`、`extraction_warnings`
- `dicts/` 下至少一个枚举 JSON（由 export 脚本生成）
- `LlmClient` 在无 key 时 `available=False` 不抛错
- README 说明 `OPENAI_API_KEY` / `LLM_MODEL` / `OPENAI_BASE_URL`
</must_haves>

<tasks>

<task id="02-01-01" type="execute">
  <read_first>
    - contract_info/.planning/phases/02-extract/02-CONTEXT.md
    - contract_info/.planning/phases/02-extract/02-RESEARCH.md
    - contract_info/backend/app/models/contract_file.py
  </read_first>
  <files>contract_info/alembic/versions/002_extraction_columns.py, contract_info/backend/app/models/contract_file.py</files>
  <action>
Alembic revision `002_extraction_columns`：
- `extraction_result` JSONB nullable
- `extraction_warnings` JSONB nullable default `[]` 或 nullable

更新 `ContractFile` 模型映射上述列。

`status` 文档化允许值：`pending|parsing|parsed|failed|extracting|extracted|extraction_failed`（应用层校验，DB 仍 varchar）。
  </action>
  <verify>
    rg "extraction_result" contract_info/alembic/versions/002_extraction_columns.py
    rg "extraction_warnings" contract_info/backend/app/models/contract_file.py
  </verify>
  <acceptance_criteria>
    - 迁移可 upgrade/downgrade 语法正确
    - 模型与迁移列一致
  </acceptance_criteria>
</task>

<task id="02-01-02" type="execute">
  <read_first>
    - contract_info/FIELD_SPEC.md
    - contract_info/.planning/phases/02-extract/02-RESEARCH.md
  </read_first>
  <files>contract_info/backend/app/extract/schemas.py</files>
  <action>
在 `backend/app/extract/schemas.py` 定义：

- `Confidence` = Literal high/medium/low
- `FieldValue`: value (str|float|None), confidence, source (rule|llm), block_id, section_id, snippet
- `ProductElements`: Dict[str, FieldValue] 或显式 P1 字段 Optional 包装（推荐 TypedDict + 运行时 dict）
- `FeeRateRow`: 基金名称、运营费类型、计费频率、计费基准、费率（%/年）等 P1 列
- `ExtractionResult`: product_elements, fee_rates: list[FeeRateRow], _meta
- `ExtractionWarning`: field, code, message, suggestion optional

提供 `extraction_result_to_dict` / `warnings_to_list` 辅助序列化。
  </action>
  <verify>
    python -c "from backend.app.extract.schemas import ExtractionResult, FieldValue; print('ok')"
  </verify>
  <acceptance_criteria>
    - Pydantic v2 模型可 `model_validate` 样例 JSON
  </acceptance_criteria>
</task>

<task id="02-01-03" type="execute">
  <read_first>
    - contract_info/example/产品要素-2.xlsx
    - contract_info/FIELD_SPEC.md §六
  </read_first>
  <files>contract_info/scripts/export_dicts.py, contract_info/dicts/.gitkeep</files>
  <action>
`scripts/export_dicts.py`：用 openpyxl 读取 `example/产品要素-2.xlsx` 中字典 sheet（销售机构及名称对应表、业绩比较基准指数表、产品分类字典等，存在则导出），写入 `dicts/{sheet_slug}.json`（数组或 `{code,name}` 结构）。

`requirements.txt` 增加 `openpyxl>=3.1`, `httpx>=0.27`。

提交生成的 `dicts/*.json`（至少销售机构、业绩基准之一）或 README 说明首次运行脚本生成。
  </action>
  <verify>
    python contract_info/scripts/export_dicts.py
    dir contract_info/dicts/*.json
  </verify>
  <acceptance_criteria>
    - 脚本 exit 0
    - dicts 目录有 JSON 文件
  </acceptance_criteria>
</task>

<task id="02-01-04" type="execute">
  <read_first>
    - contract_info/backend/app/config.py
    - ai_bid_management/bid_tool_agents/backend/app/services/document_pipeline/llm_base.py
  </read_first>
  <files>
    contract_info/backend/app/config.py
    contract_info/backend/app/llm/client.py
    contract_info/.env.example
  </files>
  <action>
`Settings` 增加：`openai_api_key`, `openai_base_url` (default https://api.openai.com/v1), `llm_model`, `llm_timeout`, `llm_max_retries` (default 1 for extract retry at pipeline level).

`LlmClient`：
- `available` property
- `async def chat_json(messages, response_model: type[BaseModel]) -> BaseModel` 解析 message content JSON
- 使用 httpx POST `{base}/chat/completions`，同步包装或全 async（与 pytest 一致即可）

`.env.example` 增加 LLM 变量占位。README 增加「抽取与 LLM」小节。
  </action>
  <verify>
    rg "openai_api_key" contract_info/backend/app/config.py
    rg "chat/completions" contract_info/backend/app/llm/client.py
  </verify>
  <acceptance_criteria>
    - 无 key 时 `LlmClient.available is False`
    - 有 key 时类可实例化（不要求本 task 真实调 API）
  </acceptance_criteria>
</task>

<task id="02-01-05" type="auto">
  <name>Schema push (Alembic)</name>
  <blocking>true</blocking>
  <action>
在 Docker Postgres 运行后执行：`alembic upgrade head`（与 Phase 1 相同 compose）。
  </action>
  <verify>
    alembic current
  </verify>
  <acceptance_criteria>
    - DB revision 为 002 head
  </acceptance_criteria>
</task>

</tasks>
