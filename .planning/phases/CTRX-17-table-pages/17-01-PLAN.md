---
phase: 17-table-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/constants/jobSections.ts
  - frontend/src/composables/useSectionPreview.ts
autonomous: true
requirements:
  - TBL-02

must_haves:
  truths:
    - "D-01/D-03: client 暴露分表 GET/PUT 与 verification GET"
    - "D-07: useSectionPreview 封装 load/save/dirty/canEdit"
    - "section 与 TableKey 类型一致（五枚举）"
  artifacts:
    - path: frontend/src/api/client.ts
      provides: getJobPreviewSection, saveJobPreviewSection, getTableVerification
    - path: frontend/src/composables/useSectionPreview.ts
      provides: sectional preview state machine
  key_links:
    - from: frontend/src/composables/useSectionPreview.ts
      to: frontend/src/api/client.ts
      via: getJobPreviewSection / saveJobPreviewSection
      pattern: getJobPreviewSection
---

<objective>
Add frontend API bindings for Phase 15 sectional preview and verification endpoints (TBL-02 foundation) plus `useSectionPreview` composable for table pages.

Output: types, client methods, section payload helpers in jobSections, composable ready for JobTableView.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-17-table-pages/17-CONTEXT.md
@contract_info/backend/app/api/schemas.py
@contract_info/frontend/src/api/client.ts
@contract_info/frontend/src/constants/jobSections.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types for sectional preview and verification (D-03)</name>
  <files>frontend/src/api/types.ts</files>
  <action>
    Add:
    - `export type PreviewSection = TableKey` (import TableKey from jobSections or duplicate literal union matching backend)
    - `JobPreviewSectionResponse` mirroring backend optional section fields + job_id, section, source
    - `VerificationRow`, `TableVerificationResponse` (page_no_available, rows[])
    - `ProductSectionUpdateBody`, `ListSectionUpdateBody` types for PUT payloads (or inline in save helper)
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 2: API client methods (D-01, D-02)</name>
  <files>frontend/src/api/client.ts</files>
  <action>
    ```ts
    getJobPreviewSection(jobId: string, section: PreviewSection): Promise<JobPreviewSectionResponse>
    saveJobPreviewSection(jobId, section, body: Record<string, unknown>): Promise<JobPreviewSectionResponse>
    getTableVerification(jobId: string, tableKey: PreviewSection): Promise<TableVerificationResponse>
    ```
    Paths: `/jobs/${id}/preview/${section}`, PUT same, `/jobs/${id}/verification/${tableKey}`.
    Keep existing full preview methods for any legacy use.
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 3: Section payload helpers (D-07)</name>
  <files>frontend/src/constants/jobSections.ts</files>
  <action>
    Add per TableKey:
    - `sectionKind(key): 'product' | 'list'`
    - `buildSectionSaveBody(key, state)` — maps local editor state to PUT body (product_rows OR fee_/lock_/share_/subscription_ columns+rows)
    - `parseSectionPreview(key, response)` — returns normalized editor state from GET response
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 4: useSectionPreview composable (D-07, D-10)</name>
  <files>frontend/src/composables/useSectionPreview.ts</files>
  <action>
    Signature: `useSectionPreview(tableKey: Ref<TableKey | string>, status: Ref<string>)`
    - PREVIEW_STATUSES same as ExportPreview
    - `preview`, `loading`, `saving`, `dirty`, `canEdit`, `load()`, `save()`, `markDirty()`
    - `save()` calls buildSectionSaveBody + saveJobPreviewSection; clears dirty on success; ElMessage success/error
    - watch `[tableKey, status]` → load when key valid
    - Requires `jobId` from `useJobDetailInject()` inside composable
  </action>
  <verify>npm run typecheck</verify>
</task>

</tasks>

<verification>
`cd frontend && npm run typecheck`
</verification>

<success_criteria>
Client and composable compile; section keys align with backend PreviewSection
</success_criteria>
