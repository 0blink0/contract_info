---
phase: 17-table-pages
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - frontend/src/views/JobTableView.vue
  - frontend/src/components/ExportPreview.vue
  - frontend/tests/router/section-preview-client.test.mjs
autonomous: true
requirements:
  - TBL-01
  - TBL-02
  - TBL-03
  - TBL-04
  - TBL-05

must_haves:
  truths:
    - "D-08/D-09: JobTableView 含保存、单表下载、编辑区+核对区"
    - "D-11/D-12: beforeRouteLeave dirty 确认"
    - "TBL-04: downloadBlob(kind, TABLE_DOWNLOAD_FILES[key])"
    - "D-14: 静态测试断言 client 分表路径"
    - "五表路由均可独立编辑保存（手工 UAT）"
  artifacts:
    - path: frontend/src/views/JobTableView.vue
      provides: full table work page
    - path: frontend/tests/router/section-preview-client.test.mjs
      provides: API path contract test
  key_links:
    - from: frontend/src/views/JobTableView.vue
      to: frontend/src/composables/useSectionPreview.ts
      via: load/save/dirty
      pattern: useSectionPreview
    - from: frontend/src/views/JobTableView.vue
      to: frontend/src/api/client.ts
      via: downloadBlob on table download button
      pattern: downloadBlob
---

<objective>
Wire JobTableView as the full per-table work page (TBL-01~05): compose editor + verification, save, download, dirty route guard, and client contract test.

Output: production JobTableView; optional ExportPreview deprecation comment; tests green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/CTRX-17-table-pages/17-VALIDATION.md
@contract_info/frontend/src/views/JobTableView.vue
@contract_info/frontend/src/layouts/JobDetailLayout.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: JobTableView integration (D-07–D-10, TBL-01–04)</name>
  <files>frontend/src/views/JobTableView.vue</files>
  <action>
    - `useJobDetailInject()` for jobId + status
    - `tableKey` from route; guard with isValidTableKey
    - `useSectionPreview(computed tableKey, status)`
    - Header: sectionLabel, 保存修改 (primary, :disabled="!dirty"), 下载{label} (when status==='exported' or show after extracted per v1.2)
    - `TablePreviewEditor` bound to preview state; @dirty → markDirty
    - `VerificationExcerptTable` below editor
    - Hint text from ExportPreview (保存后 Excel 更新)
    - When !PREVIEW_STATUSES: el-alert 提示先完成抽取
  </action>
  <verify>npm run typecheck && npm run build</verify>
</task>

<task type="auto">
  <name>Task 2: Dirty route guard (D-11, D-12, TBL-05)</name>
  <files>frontend/src/views/JobTableView.vue</files>
  <action>
    ```ts
    import { onBeforeRouteLeave } from 'vue-router'
    onBeforeRouteLeave(async () => {
      if (!dirty.value) return true
      try {
        await ElMessageBox.confirm('有未保存的修改，确定离开？', ...)
        return true
      } catch { return false }
    })
    ```
    Watch tableKey: reload section (composable handles), reset dirty via load
  </action>
  <verify>Manual: edit without save → click another submenu item → confirm appears</verify>
</task>

<task type="auto">
  <name>Task 3: ExportPreview deprecation note (D-06)</name>
  <files>frontend/src/components/ExportPreview.vue</files>
  <action>
    Add file-top comment: superseded by TablePreviewEditor + JobTableView (Phase 17). Kept for reference until JobDetail.vue removed in Phase 18.
    No functional change required if unused.
  </action>
</task>

<task type="auto">
  <name>Task 4: Client contract test (D-14)</name>
  <files>frontend/tests/router/section-preview-client.test.mjs</files>
  <action>
    Read client.ts as text; assert:
    - `/preview/${` or `/preview/` + section param pattern
    - `/verification/`
    - `getJobPreviewSection` and `getTableVerification` function names
  </action>
  <verify>npm run test:router</verify>
</task>

<task type="auto">
  <name>Task 5: Phase gate</name>
  <files></files>
  <action>Run typecheck, test:router, build. Record manual UAT notes in 17-03-SUMMARY.md.</action>
  <verify>All exit 0</verify>
</task>

</tasks>

<verification>
```bash
cd contract_info/frontend && npm run test:router && npm run typecheck && npm run build
```
</verification>

<success_criteria>
ROADMAP Phase 17 five success criteria achievable via manual UAT
</success_criteria>
