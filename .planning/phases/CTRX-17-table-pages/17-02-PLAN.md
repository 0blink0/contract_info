---
phase: 17-table-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - frontend/src/components/table/TablePreviewEditor.vue
  - frontend/src/components/table/VerificationExcerptTable.vue
autonomous: true
requirements:
  - TBL-01
  - TBL-03

must_haves:
  truths:
    - "D-04: 产品表 field/value 可编辑；列表表列与 ExportPreview 一致（不含摘录原文列编辑）"
    - "D-05: 核对表四列展示；page_no 不可用时显示 note 或 —"
    - "编辑器通过 markDirty 向上通知"
  artifacts:
    - path: frontend/src/components/table/TablePreviewEditor.vue
      provides: single-section editable grid
    - path: frontend/src/components/table/VerificationExcerptTable.vue
      provides: verification read-only table
  key_links:
    - from: frontend/src/components/table/TablePreviewEditor.vue
      to: frontend/src/composables/useSectionPreview.ts
      via: v-model preview state + @dirty
      pattern: markDirty
---

<objective>
Extract v1.2 ExportPreview single-tab UI into reusable table components (TBL-01 editor + TBL-03 verification display).

Output: TablePreviewEditor.vue, VerificationExcerptTable.vue.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/frontend/src/components/ExportPreview.vue
@contract_info/.planning/phases/CTRX-17-table-pages/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TablePreviewEditor (D-04)</name>
  <files>frontend/src/components/table/TablePreviewEditor.vue</files>
  <action>
    Props: `tableKey: TableKey`, `preview: JobPreviewSectionResponse | null`, `canEdit: boolean`, `loading: boolean`
    Emits: `dirty`
    Logic copied/refactored from ExportPreview for ONE section:
    - product-elements: el-table field + value inputs
    - list sections: columnKeys() excluding `摘录原文`; el-table-column per col with el-input when canEdit
    - Reuse `columnKeys` as local function or shared util `previewColumnKeys.ts`
    Empty states per table (same Chinese empty-text as ExportPreview)
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 2: VerificationExcerptTable (D-05)</name>
  <files>frontend/src/components/table/VerificationExcerptTable.vue</files>
  <action>
    Props: `jobId: string`, `tableKey: TableKey`, `status: string`
    - Load `getTableVerification` when status in PREVIEW_STATUSES
    - el-table columns: 字段(field_label), 字段值(value), 摘录页码(page_no or page_no_note or —), 原文摘录(excerpt)
    - Subtitle when `!page_no_available`: 页码暂未解析
    - Optional: validation_status tag column if present on row
    - loading skeleton; empty state
  </action>
  <verify>npm run typecheck</verify>
</task>

</tasks>

<verification>
`cd frontend && npm run typecheck`
</verification>

<success_criteria>
Components render without JobTableView; match ExportPreview column behavior
</success_criteria>
