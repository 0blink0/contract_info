---
phase: "09-llm-validation"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - contract_info/backend/app/validate/__init__.py
  - contract_info/backend/app/validate/schemas.py
  - contract_info/backend/app/validate/evidence.py
  - contract_info/backend/app/validate/prompts.py
  - contract_info/backend/app/validate/llm_validator.py
  - contract_info/backend/tests/test_validation_evidence.py
  - contract_info/backend/tests/test_llm_validator.py
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
user_setup: []

must_haves:
  truths:
    - "collect_validation_candidates 产出带 evidence 的校验项列表"
    - "mock LLM 矛盾样例返回 status=fail（TEST-02）"
    - "mock LLM 一致样例返回 status=pass"
    - "无 snippet 且无 block_id 的字段不进入候选"
  artifacts:
    - path: "contract_info/backend/app/validate/llm_validator.py"
      provides: "LLM 摘录一致性校验"
      contains: "run_llm_validation"
    - path: "contract_info/backend/app/validate/evidence.py"
      provides: "校验候选收集"
      contains: "collect_validation_candidates"
  key_links:
    - from: "backend/app/validate/llm_validator.py"
      to: "backend/app/llm/client.py"
      via: "chat_json"
      pattern: "ValidationBatchResponse"
---

<objective>
实现 LLM 校验核心：ValidationResult schema、证据收集、批校验 prompt + llm_validator；TEST-02 mock 单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/09-llm-validation/09-CONTEXT.md
@contract_info/.planning/phases/09-llm-validation/09-RESEARCH.md
@contract_info/backend/app/extract/schemas.py
@contract_info/backend/app/llm/client.py
@contract_info/backend/app/extract/llm/chapter_extract.py
</context>

<tasks>

<task id="09-01-01" type="auto">
  <name>任务 1：validate 包 + schemas</name>
  <files>
    contract_info/backend/app/validate/schemas.py
    contract_info/backend/app/validate/__init__.py
  </files>
  <action>
新建 `backend/app/validate/`（**不要** 修改 `extract/validate.py`）。

`schemas.py`：
- `ValidationCandidate(field, value, evidence_text)`
- `ValidationItem(field, status, value?, reason, suggestion?)` — status ∈ pass|warn|fail
- `ValidationResult(validated_at, model, skipped, items, summary)`

`to_dict()` / `model_dump` 供 JSONB 持久化。
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.validate.schemas import ValidationResult, ValidationItem; ValidationItem(field='管理人', status='fail', reason='x')"</automated>
  </verify>
</task>

<task id="09-01-02" type="auto">
  <name>任务 2：evidence 候选收集</name>
  <files>
    contract_info/backend/app/validate/evidence.py
  </files>
  <action>
`collect_validation_candidates(extraction_result: dict, path_b_json: dict | None, parse_json: dict) -> list[ValidationCandidate]`

1. **product_elements**：遍历 FieldValue dict；有 value + (snippet 或 block_id)
2. **fee_rates / subscription_fees**：行 dict；若有 snippet 字段则纳入（费率、运营费类型等）
3. **path_b**：遍历 `source_snippets`；field 为键，evidence 为值；value 从 path_b 嵌套解析

`resolve_evidence_text(snippet, block_id, parse_json)`：
- snippet ≥20 字直接用
- 否则按 block_id 在 blocks 找 paragraph text

**当事人字段**（管理人、托管人、投资顾问、外包机构）标记 `party=True` 供 prompt 强调。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_validation_evidence.py -q</automated>
  </verify>
</task>

<task id="09-01-03" type="auto">
  <name>任务 3：llm_validator + prompts</name>
  <files>
    contract_info/backend/app/validate/prompts.py
    contract_info/backend/app/validate/llm_validator.py
  </files>
  <action>
`prompts.py`：`build_validation_messages(candidates: list[ValidationCandidate]) -> list[dict]`

系统提示要点（VAL-02）：
- 仅根据「摘录」判断 value 是否合理
- 值未在摘录中出现或明显矛盾 → fail
- 摘录不足无法判断 → warn
- 一致 → pass
- 当事人须为公司/机构全称出现在摘录

`llm_validator.py`：
```python
async def run_llm_validation(
    extraction_result: dict,
    path_b_json: dict | None,
    parse_json: dict,
    *,
    llm_client: LlmClient | None = None,
) -> ValidationResult:
```

- 分批（默认 10）调用 `client.chat_json`
- 合并 items，计算 summary 计数
- client 不可用 → `ValidationResult(skipped=True, items=[])`

`run_llm_validation_sync` wrapper 供 service 调用。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_llm_validator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - TEST-02：value=「北京石云」+ snippet 含「登记为管理人」→ fail
    - 一致样例 → pass
  </acceptance_criteria>
</task>

<task id="09-01-04" type="auto">
  <name>任务 4：单元测试 TEST-02</name>
  <files>
    contract_info/backend/tests/test_validation_evidence.py
    contract_info/backend/tests/test_llm_validator.py
  </files>
  <action>
**test_validation_evidence.py**：用 fixture extraction dict（含 product_elements 管理人）；断言候选含管理人且 evidence 非空。

**test_llm_validator.py**：
- `MockLlmClient` 按输入返回预设 `ValidationBatchResponse`
- `test_contradiction_returns_fail` — 矛盾响应
- `test_consistent_returns_pass`
- `test_llm_off_returns_skipped`

不使用真实 API Key。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_validation_evidence.py backend/tests/test_llm_validator.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
`pytest backend/tests/test_validation_evidence.py backend/tests/test_llm_validator.py -q`
</verification>

<success_criteria>
VAL-01/02/03 校验引擎可独立运行（无 DB）
</success_criteria>

<output>
完成后创建 `09-01-SUMMARY.md`
</output>
