---
phase: "09-llm-validation"
plan: "02"
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - contract_info/alembic/versions/007_validation_result.py
  - contract_info/backend/app/models/contract_file.py
  - contract_info/backend/app/services/validation_service.py
  - contract_info/backend/app/services/extract_service.py
  - contract_info/backend/tests/test_validation_persist.py
autonomous: true
requirements:
  - VAL-01
  - VAL-03
user_setup: []

must_haves:
  truths:
    - "persist_extract 后 contract_files.validation_result 非空（有 Key 时）"
    - "LlmOff 时 validation_skipped warning 且 skipped=true"
    - "validation_result 不写入 extraction_warnings 的 pass 项"
  artifacts:
    - path: "contract_info/backend/app/services/validation_service.py"
      provides: "校验持久化"
      contains: "persist_validation"
    - path: "contract_info/alembic/versions/007_validation_result.py"
      provides: "DB 迁移"
  key_links:
    - from: "backend/app/services/extract_service.py"
      to: "backend/app/services/validation_service.py"
      via: "persist_validation"
      pattern: "validation_result"
---

<objective>
校验结果持久化：Alembic、ORM、validation_service、extract_service 挂钩；skipped 与 warnings 处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/09-llm-validation/09-CONTEXT.md
@contract_info/backend/app/services/extract_service.py
@contract_info/alembic/versions/006_path_b_json.py
</context>

<tasks>

<task id="09-02-01" type="auto">
  <name>任务 1：Alembic 007 + ORM</name>
  <files>
    contract_info/alembic/versions/007_validation_result.py
    contract_info/backend/app/models/contract_file.py
  </files>
  <action>
`007_validation_result.py`（`down_revision=006`）：`validation_result` JSONB nullable。

`ContractFile.validation_result: Mapped[dict | None]`
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.models.contract_file import ContractFile; assert 'validation_result' in ContractFile.__table__.columns"</automated>
  </verify>
</task>

<task id="09-02-02" type="auto">
  <name>任务 2：validation_service + extract 集成</name>
  <files>
    contract_info/backend/app/services/validation_service.py
    contract_info/backend/app/services/extract_service.py
  </files>
  <action>
`validation_service.persist_validation(file_id: uuid.UUID) -> dict | None`：
- 读 record：extraction_result, path_b_json, parse_json
- `run_llm_validation_sync(...)`
- 写 `record.validation_result = result.model_dump()`
- 若 skipped：append `ExtractionWarning(code='validation_skipped', ...)`
- 若 fail/warn：可选 append 摘要 warning `validation_issues`（仅计数，明细在 validation_result）
- commit

**extract_service.persist_extract**：在写 extraction/path_b 后、status=extracted 前调用 `persist_validation(file_id)`。

**不阻止 export**（D-V01）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_validation_persist.py -q -m "not llm"</automated>
  </verify>
</task>

<task id="09-02-03" type="auto">
  <name>任务 3：test_validation_persist</name>
  <files>
    contract_info/backend/tests/test_validation_persist.py
  </files>
  <action>
- `test_persist_validation_with_mock_llm` — patch `run_llm_validation_sync` 返回固定 ValidationResult；内存或 sqlite 测 ORM 字段赋值逻辑（可 mock SessionLocal）
- `test_extract_sets_validation_skipped_without_key` — patch LlmOff，persist_extract 后 warnings 含 validation_skipped

集成测可选 `@pytest.mark.integration` 仿 test_extract_persist。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_validation_persist.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
ORM 列存在；test_validation_persist 通过
</verification>

<success_criteria>
VAL-03 持久化：extract 后 validation_result 写入 DB
</success_criteria>

<output>
完成后创建 `09-02-SUMMARY.md`
</output>
