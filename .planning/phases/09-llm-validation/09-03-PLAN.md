---
phase: "09-llm-validation"
plan: "03"
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - contract_info/backend/app/api/schemas.py
  - contract_info/backend/app/api/routes/jobs.py
  - contract_info/backend/tests/test_api_validation.py
  - contract_info/backend/tests/test_validation_llm.py
autonomous: true
requirements:
  - VAL-03
  - VAL-04
user_setup: []

must_haves:
  truths:
    - "GET /jobs/{id}/validation 返回 items + summary"
    - "parsed 状态返回 409"
    - "JobDetail 含 validation_fail_count / validation_available"
    - "无 validation_result 时 GET 404"
  artifacts:
    - path: "contract_info/backend/app/api/routes/jobs.py"
      provides: "validation API"
      contains: "/validation"
  key_links:
    - from: "backend/app/api/routes/jobs.py"
      to: "ContractFile.validation_result"
      via: "get_validation"
      pattern: "validation_result"
---

<objective>
校验 API：GET /validation、JobDetail 摘要、API 测试；可选真实 LLM 抽检。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/09-llm-validation/09-CONTEXT.md
@contract_info/backend/app/api/routes/jobs.py
@contract_info/backend/tests/test_api_path_b.py
</context>

<tasks>

<task id="09-03-01" type="auto">
  <name>任务 1：API schema + GET /validation</name>
  <files>
    contract_info/backend/app/api/schemas.py
    contract_info/backend/app/api/routes/jobs.py
  </files>
  <action>
`ValidationResponse`：
- job_id, validated_at, model, skipped
- items: list[ValidationItemResponse]
- summary: dict

`JobDetailResponse` 增加：
- `validation_available: bool`
- `validation_fail_count: int = 0`
- `validation_warn_count: int = 0`

`GET /jobs/{job_id}/validation`：
- `PREVIEW_STATUSES` 门禁
- 无 validation_result → 404
- skipped=true 仍返回 200（items 可能为空）

`_record_to_detail` 从 validation_result.summary 填计数。

**不修改 frontend/**（D-A01）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_validation.py -q</automated>
  </verify>
</task>

<task id="09-03-02" type="auto">
  <name>任务 2：test_api_validation</name>
  <files>
    contract_info/backend/tests/test_api_validation.py
  </files>
  <action>
仿 test_api_path_b：
- 409 when parsed
- 200 with seed validation_result
- 404 when extracted but no validation_result
- job detail shows fail/warn counts

  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_validation.py -q</automated>
  </verify>
</task>

<task id="09-03-03" type="optional">
  <name>任务 3（可选）：@pytest.mark.llm 抽检</name>
  <files>
    contract_info/backend/tests/test_validation_llm.py
  </files>
  <action>
`test_validation_llm_shiyun_sample`：真实 extract+validate 石云合同；断言 validation_result 有 items 且 summary.pass > 0。无 Key skip。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_validation_llm.py -q -m llm 2>&1 || true</automated>
  </verify>
</task>

</tasks>

<verification>
`pytest backend/tests/test_validation_evidence.py backend/tests/test_llm_validator.py backend/tests/test_api_validation.py -q -m "not llm"`
</verification>

<success_criteria>
VAL-03/04 后端：API 可查询校验结果；前端 Phase 10
</success_criteria>

<output>
完成后创建 `09-03-SUMMARY.md`、`09-VERIFICATION.md`
</output>
