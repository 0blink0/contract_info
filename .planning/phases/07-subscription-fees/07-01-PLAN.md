---
phase: "07-subscription-fees"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - contract_info/backend/app/extract/schemas.py
  - contract_info/backend/app/extract/rules/subscription_rules.py
  - contract_info/backend/app/extract/rules/__init__.py
  - contract_info/backend/app/extract/pipeline.py
  - contract_info/backend/tests/test_subscription_rules.py
  - contract_info/backend/tests/golden/fixtures/contract_expected.json
autonomous: true
requirements:
  - XLS-01
user_setup: []

must_haves:
  truths:
    - "ExtractionResult 含 subscription_fees 数组且可 model_dump 持久化"
    - "石云/福禄各份额类至少产出认购费+申购费行（规则层、无 LLM）"
    - "合同有短期赎回分段时产出多行赎回费（计费基准含区间）"
    - "未写明费率时 rate/费率 字段为 0 或等价"
  artifacts:
    - path: "contract_info/backend/app/extract/rules/subscription_rules.py"
      provides: "份额表+申赎章规则抽取"
      contains: "extract_subscription_fees_rules"
    - path: "contract_info/backend/app/extract/schemas.py"
      provides: "SubscriptionFeeRow"
      contains: "SubscriptionFeeRow"
  key_links:
    - from: "backend/app/extract/pipeline.py"
      to: "backend/app/extract/rules/subscription_rules.py"
      via: "extract_subscription_fees_rules"
      pattern: "extract_subscription_fees"
---

<objective>
实现申赎费率规则层抽取：SubscriptionFeeRow schema、subscription_rules（份额分类表优先 + 申赎章赎回分段），并入 extract pipeline；补充 contract_expected 申赎真值段与单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract_info/.planning/phases/07-subscription-fees/07-CONTEXT.md
@contract_info/.planning/phases/07-subscription-fees/07-RESEARCH.md
@contract_info/.planning/phases/07-subscription-fees/07-PATTERNS.md
@contract_info/.planning/phases/06-extract-quality/06-FIELD-MATRIX.md
@contract_info/backend/app/extract/rules/share_rules.py
@contract_info/backend/app/extract/rules/fee_rules.py
@contract_info/backend/tests/golden/fixtures/contract_expected.json
@contract_info/example/产品申赎费率导入模板.xlsx

<interfaces>
份额表表头常见：年认购费率、申购费率、销售服务费率（申赎表用认购/申购列）

```python
def extract_subscription_fees_rules(
    document: dict[str, Any],
    windows: dict[str, str],
    *,
    fund_name: str | None,
    share_classes: list[ShareClassRow],
    product_elements: dict[str, Any],
) -> list[SubscriptionFeeRow]: ...
```
</interfaces>
</context>

<tasks>

<task id="07-01-01" type="auto">
  <name>任务 1：SubscriptionFeeRow + ExtractionResult 扩展</name>
  <files>
    contract_info/backend/app/extract/schemas.py
  </files>
  <action>
在 `schemas.py` 新增 `SubscriptionFeeRow`（字段对齐申赎模板第 3 行表头，见 RESEARCH）。

更新 `ExtractionResult`：`subscription_fees: list[SubscriptionFeeRow] = Field(default_factory=list)`。

更新 `extraction_result_to_dict` 序列化 `subscription_fees` 列表（同 fee_rates 处理）。

`申赎费类型` 枚举约束在规则层保证为：认购费、申购费、赎回费。
  </action>
  <verify>
    <automated>cd contract_info && python -c "from backend.app.extract.schemas import SubscriptionFeeRow, ExtractionResult; r=SubscriptionFeeRow(申赎费类型='认购费',费率='0.5'); assert r.申赎费类型=='认购费'"</automated>
  </verify>
  <acceptance_criteria>
    - SubscriptionFeeRow 可 import
    - ExtractionResult.model_dump 含 subscription_fees 键
  </acceptance_criteria>
</task>

<task id="07-01-02" type="auto">
  <name>任务 2：subscription_rules 规则抽取</name>
  <read_first>
    - 07-CONTEXT.md D-S01–S04、D-G02、D-G04
    - share_rules.py、fee_rules.py
    - 石云/福禄 docx 份额分类表（本地 parse 验证）
  </read_first>
  <files>
    contract_info/backend/app/extract/rules/subscription_rules.py
    contract_info/backend/app/extract/rules/__init__.py
  </files>
  <action>
实现 `extract_subscription_fees_rules`：

1. **份额类来源**：`share_classes` 非空用其；否则从 document 表扫描 A/B/C/D 类（复用 share 表逻辑或调用 share_rules 结果）。

2. **基金名称**（D-G02）：`format_subscription_fund_name(基金全称, 份额标识)` — 石云风格 `…证券投资基金{A}`，福禄风格 `…一号{A}类`（可从份额表行推断）。

3. **基金代码**（D-G01）：优先 `ShareClassRow.分级份额代码`，否则空。

4. **认购/申购**：解析份额分类表中含「认购」「申购」费率行；无则费率 `0` + 生成 warning 码 `subscription_rate_defaulted`。

5. **赎回**（D-S03）：仅在 `windows["subscription"]` 匹配短期赎回/持有天数分段时追加行；`计费基准` 用「区间（…）」类；填 `区间开始`/`区间结束`/`时间区间单位`；勿从 lock_rules 误抽。

6. **最小填列**（D-T03）：仅写抽取到的列；`计费方式`/`费率类型` 等若合同无则 **不** 默认价外法（导出阶段可 warning）。

导出 `__init__.py` 暴露函数。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_subscription_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 石云 docx 规则层 ≥8 行 subscription（4 份额 × 认购+申购）
    - 福禄 docx 同理
    - 含赎回分段合同样本能产出 ≥1 行赎回费
  </acceptance_criteria>
</task>

<task id="07-01-03" type="auto">
  <name>任务 3：pipeline 集成 + contract_expected 申赎段</name>
  <files>
    contract_info/backend/app/extract/pipeline.py
    contract_info/backend/tests/test_subscription_rules.py
    contract_info/backend/tests/golden/fixtures/contract_expected.json
  </files>
  <action>
在 `extract_document` 中于 `share_classes` 之后调用 `extract_subscription_fees_rules`，传入 `merge_extraction` / `ExtractionResult`（扩展 `merge_extraction` 签名接受 `subscription_fees`）。

**test_subscription_rules.py**（无 LLM）：
- `test_shiyun_subscription_min_rows`
- `test_fulu_subscription_min_rows`
- `test_subscription_fee_types`（认购+申购存在）
- 可选：`test_shiyun_redeem_tier_rows` 若石云合同有短期赎回条款

**contract_expected.json**：每 docx 键增加 `subscription_fees_by_share`：
```json
"A": {"认购费": "0.5", "申购费": "0.5"},
...
```
真值以本地规则抽取实测填入（合同为准，非黄金 xlsx）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_subscription_rules.py -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - extract_document_sync(LlmOff) 返回的 subscription_fees 非空（石云/福禄 example 存在时）
    - contract_expected 含 subscription_fees_by_share
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd contract_info && python -m pytest backend/tests/test_subscription_rules.py -q -m "not llm"`
</verification>

<success_criteria>
- XLS-01 抽取侧就绪：extraction JSON 含可导出申赎费率行
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/07-subscription-fees/07-01-SUMMARY.md`
</output>
