---
phase: "07-subscription-fees"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - contract_info/backend/app/api/routes/jobs.py
  - contract_info/backend/app/api/schemas.py
  - contract_info/backend/tests/test_api_download.py
  - contract_info/backend/tests/golden/helpers/pipeline_runner.py
  - contract_info/backend/tests/golden/helpers/xlsx_diff.py
  - contract_info/backend/tests/golden/test_golden_export.py
  - contract_info/backend/tests/golden/README.md
  - contract_info/backend/tests/golden/fixtures/contract_expected.json
autonomous: true
requirements:
  - XLS-02
  - XLS-04
  - API-01
user_setup: []

must_haves:
  truths:
    - "GET /jobs/{id}/download/subscription-fee-rates 在 exported 状态返回 xlsx"
    - "golden E2E 断言 5 个导出文件存在"
    - "申赎 Critical 断言以 contract_expected.subscription 为准"
    - "默认 pytest -m 'not llm' 全绿"
  artifacts:
    - path: "contract_info/backend/app/api/routes/jobs.py"
      provides: "download/subscription-fee-rates"
    - path: "contract_info/backend/tests/golden/test_golden_export.py"
      provides: "5 表 E2E"
  key_links:
    - from: "backend/tests/golden/helpers/pipeline_runner.py"
      to: "backend/app/export/pipeline.py"
      via: "5-tuple export_xlsx"
      pattern: "subscription_path"
---

<objective>
下载 API、JobDetail schema、golden 五表 E2E 与申赎 xlsx diff；本阶段不做前端。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@contract_info/.planning/phases/07-subscription-fees/07-CONTEXT.md
@contract_info/.planning/phases/04-backend-api/04-CONTEXT.md
@contract_info/backend/tests/golden/test_golden_export.py
@contract_info/backend/tests/test_api_download.py
</context>

<tasks>

<task id="07-03-01" type="auto">
  <name>任务 1：下载 API + schema</name>
  <files>
    contract_info/backend/app/api/routes/jobs.py
    contract_info/backend/app/api/schemas.py
    contract_info/backend/tests/test_api_download.py
  </files>
  <action>
**jobs.py** 新增：
```python
@router.get("/{job_id}/download/subscription-fee-rates")
def download_subscription_fee_rates(job_id: uuid.UUID) -> FileResponse:
    ...
    filename="subscription_fee_rates.xlsx"
```
门禁：`status == exported`，`_resolve_export_path(record.subscription_xlsx_path)`。

**schemas.py** `JobDetailResponse` 增加 `subscription_xlsx_path: str | None`。

**test_api_download.py**：仿 fee-rates 用例；未 exported 返回 409；路径不存在 404。

**不修改** frontend（D-A01）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/test_api_download.py -q -k subscription</automated>
  </verify>
  <acceptance_criteria>
    - API-01 端点存在且测试通过
  </acceptance_criteria>
</task>

<task id="07-03-02" type="auto">
  <name>任务 2：golden 五表 E2E</name>
  <files>
    contract_info/backend/tests/golden/helpers/pipeline_runner.py
    contract_info/backend/tests/golden/helpers/xlsx_diff.py
    contract_info/backend/tests/golden/test_golden_export.py
    contract_info/backend/tests/golden/fixtures/contract_expected.json
    contract_info/backend/tests/golden/README.md
  </files>
  <action>
**pipeline_runner.py**：
- `GoldenRunResult` 增加 `subscription_path`
- 解包 `export_xlsx` 五元组

**xlsx_diff.py**：
- `assert_subscription_fee_types(export_path, expected_by_share: dict)` — 按份额类+申赎费类型聚合费率（normalize_pct）
- `assert_subscription_structure` — sheet 名、表头行

**test_golden_export.py**：
- 扩展 parametrized E2E：断言 5 文件存在
- 石云/福禄：`assert_subscription_fee_types` 对 `contract_expected.subscription_fees_by_share`
- 赎回行：有 expected 则 assert，无则仅断言认购+申购

**README.md**：Phase 7 申赎 Critical 说明；5 表 CI 命令不变。

更新 `contract_expected.json` 申赎真值（若 07-01 未写完）。

**禁止** 用黄金 xlsx 费率数值作 hard fail（D-G03）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/golden/ -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - golden 套件含 5 文件断言
    - 石云、福禄 export E2E 通过（example 齐全时）
  </acceptance_criteria>
</task>

<task id="07-03-03" type="auto">
  <name>任务 3：全量回归门禁</name>
  <files>
    contract_info/FIELD_SPEC.md
  </files>
  <action>
运行 `pytest backend/tests/ -q -m "not llm"` 全绿。

**FIELD_SPEC.md**（最小更新）：认申赎费导出状态改为 Phase 7 已覆盖（第 9 项路径 A）。

勿改前端、勿改 preview_service（Phase 10）。
  </action>
  <verify>
    <automated>cd contract_info && python -m pytest backend/tests/ -q -m "not llm"</automated>
  </verify>
  <acceptance_criteria>
    - 全量非 llm 测试通过
    - ROADMAP Phase 7 成功标准①②③可由 pytest 验证
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd contract_info && python -m pytest backend/tests/golden/ backend/tests/test_api_download.py -q -m "not llm"`
- `cd contract_info && python -m pytest backend/tests/ -q -m "not llm"`
</verification>

<success_criteria>
- API-01、XLS-02、XLS-04 验收完成
- Phase 7 后端闭环（无前端）
</success_criteria>

<output>
完成后创建 `contract_info/.planning/phases/07-subscription-fees/07-03-SUMMARY.md`
</output>
