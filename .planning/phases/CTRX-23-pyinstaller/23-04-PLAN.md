---
phase: "23"
plan: "04"
type: execute
wave: 3
depends_on:
  - "23-02"
  - "23-03"
files_modified:
  - docs/smoke-test-23.md
autonomous: false
requirements:
  - KB-PKG-03

must_haves:
  truths:
    - "docs/smoke-test-23.md exists as a complete step-by-step manual checklist"
    - "The checklist covers the full KB chain: PathB 录入 → embedding 生成 → LanceDB 持久化 → 语义检索 → RAG prompt 注入"
    - "All four D-14 pass criteria appear as checkable items in the checklist"
    - "The checklist references the correct log file path (app.getPath('userData')/logs/backend.log)"
    - "A human can execute the checklist on a freshly-installed packaged app without consulting any other document"
  artifacts:
    - path: "docs/smoke-test-23.md"
      provides: "Full-chain KB smoke test checklist for Phase 23 release gate"
      contains: "ModuleNotFoundError"
  key_links:
    - from: "docs/smoke-test-23.md"
      to: "D-14 four pass criteria"
      via: "Checklist items referencing exact log keywords and UI messages"
      pattern: "RAG context|few-shot"
---

<objective>
Create docs/smoke-test-23.md — a complete manual smoke-test checklist for the Phase 23
packaged installer. The checklist must cover the full KB chain and include the four D-14
pass criteria as explicitly checkable items.

Purpose: KB-PKG-03 — the Phase 23 release gate requires a human to execute this checklist
on the packaged installer. All four items must pass before the v1.4 milestone ships.
The smoke test is the only acceptance signal for the full packaged end-to-end chain
(automated tests cannot cover the PyInstaller bundle itself).

Output:
- docs/smoke-test-23.md: complete human-executable checklist
- Human checkpoint: checklist is executed on the packaged installer and all four items pass
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/CTRX-23-pyinstaller/23-CONTEXT.md
@.planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md

<interfaces>
From docs/packaging/smoke-checklist-template.md (Phase 12 template structure — use as base):
  Sections: Execution Metadata, Preconditions, Smoke Chain, Data Isolation, Evidence Collected

D-14 four pass criteria (must all appear as checkable items):
  1. 后端启动日志无 ModuleNotFoundError（embedding 模型加载行可见）
  2. PathB 页录入并提交后出现「已存入 N 条」ElMessage.success 提示
  3. 知识库配置页刷新后可见该新录入条目
  4. 重新处理含业绩报酬条款合同后，后端日志包含 "RAG context" 或 "few-shot" 关键字

Log file location (verified from electron/main.ts backendLogPath() line 43):
  path.join(app.getPath('userData'), 'logs', 'backend.log')
  On Windows: %APPDATA%\CTRX\logs\backend.log  (or equivalent userData path)

Backend startup embedding model load log line (from kb_service.py — produced on warm startup):
  Contains: "Embedding model loaded" or similar — executor should look for absence of ModuleNotFoundError
  and presence of any line confirming sentence-transformers initialization.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/smoke-test-23.md</name>
  <files>docs/smoke-test-23.md</files>
  <read_first>
    - docs/packaging/smoke-checklist-template.md (full file — copy section structure verbatim, then extend)
    - .planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md (docs/smoke-test-23.md section — Phase 23 additions and preconditions)
    - .planning/phases/CTRX-23-pyinstaller/23-CONTEXT.md (D-13, D-14, D-15, D-16 decisions)
    - electron/main.ts (line 43: backendLogPath() to confirm the userData log path)
  </read_first>
  <action>
    Create docs/smoke-test-23.md. The document must be usable by anyone (not just the author)
    on a freshly-installed CTRX-Setup-1.4.0.exe, on a Windows machine without Python installed.

    Structure (follow smoke-checklist-template.md section order):

    ---
    # CTRX v1.4 Smoke Test — Phase 23: KB 全链路打包验证

    ## 执行元数据

    | 字段 | 值 |
    |------|-----|
    | 执行人 | |
    | 执行日期 | |
    | 机器名 / OS | |
    | 安装包版本 | CTRX-Setup-1.4.0.exe |
    | 安装路径 | |
    | CTRX_DATA_DIR (userData) | （Windows: %APPDATA%\CTRX 或启动日志第一行）|
    | 后端日志路径 | %APPDATA%\CTRX\logs\backend.log |
    | bge-m3 模型目录是否存在 | 安装完成后检查: <安装路径>\resources\electron\resources\models\bge-m3\ |
    | CTRX_MODELS_DIR（注入值） | 通过 Electron DevTools console 确认（可选）|

    ## 前提条件

    全部勾选后方可继续：

    - [ ] 执行机器**无 Python 运行时**（确保测试的是打包产物而非开发环境）
    - [ ] 已安装 CTRX-Setup-1.4.0.exe 至测试机器
    - [ ] <安装路径>\resources\electron\resources\models\bge-m3\ 目录存在且包含 config.json
    - [ ] 备有一份含**业绩报酬条款**的 .docx 合同文件（用于步骤 C 和步骤 D）
    - [ ] %APPDATA%\CTRX\logs\ 目录可写（首次启动后自动创建）

    ## 烟测链路

    按顺序执行以下步骤。每步完成后勾选对应检查项。

    ### 步骤 A：应用启动 + 后端健康（D-14 标准 1）

    1. 双击 CTRX 桌面快捷方式启动应用
    2. 等待加载完成（最多 60 秒）
    3. 打开后端日志文件：`%APPDATA%\CTRX\logs\backend.log`
    4. 检查日志内容：

    **Pass 标准 1（D-14.1）：**
    - [ ] 日志中**不含** `ModuleNotFoundError` 字样
    - [ ] 日志中可见 embedding 模型加载相关行（如 `Embedding model loaded`、
          `SentenceTransformer` 或 `bge-m3` 字样）
    - [ ] 应用主界面正常显示（无「后端启动失败」对话框）

    ### 步骤 B：PathB 录入 → embedding 生成 → LanceDB 持久化（D-14 标准 2 + 3）

    1. 在应用中上传步骤 A 准备的含业绩报酬条款 .docx 文件
    2. 点击「开始处理」，等待处理完成
    3. 进入该任务详情 → 字段 B 页
    4. 在页面底部知识库录入表格中，确认 4 行均已预填字段值
    5. 勾选至少 1 行复选框
    6. 点击「存入知识库」按钮

    **Pass 标准 2（D-14.2）：**
    - [ ] 界面出现 `ElMessage.success` 提示，文字格式为「已存入 N 条」（N 为勾选行数）

    7. 进入左侧菜单「知识库配置」页面
    8. 刷新页面（F5 或重新点击菜单项）

    **Pass 标准 3（D-14.3）：**
    - [ ] 知识库列表中可见刚才录入的条目（字段名 / 字段值 / 原文摘录 / 来源合同均显示正确）

    ### 步骤 C：RAG 检索 + prompt 注入验证（D-14 标准 4）

    > 前提：步骤 B 中已存入至少 1 条案例，否则 RAG 无数据可检索。

    1. 在应用中重新上传同一份含业绩报酬条款的 .docx 文件（或上传另一份）
    2. 点击「开始处理」，等待处理完成
    3. 重新打开后端日志文件：`%APPDATA%\CTRX\logs\backend.log`
    4. 在日志中搜索关键字（Ctrl+F 或 grep）：

    **Pass 标准 4（D-14.4）：**
    - [ ] 日志中包含 `RAG context` **或** `few-shot` 关键字
          （证明 kb_service 检索到结果并注入了 prompt 构造块）

    ### 步骤 D：数据隔离验证

    - [ ] 所有写入（LanceDB 数据、日志、上传文件）均在 `%APPDATA%\CTRX\` 下
    - [ ] 安装目录（Program Files）中无运行时生成文件

    ## 通过判定

    **四项标准全部满足 = PASS，可发布 v1.4。任一项不满足 = FAIL，阻断发布（D-16）。**

    | 标准 | 描述 | 结果 |
    |------|------|------|
    | D-14.1 | 后端启动日志无 ModuleNotFoundError，embedding 加载行可见 | PASS / FAIL |
    | D-14.2 | PathB 录入后「已存入 N 条」提示出现 | PASS / FAIL |
    | D-14.3 | 知识库配置页刷新后条目可见 | PASS / FAIL |
    | D-14.4 | 重处理合同后日志含 RAG context 或 few-shot | PASS / FAIL |
    | **总体** | | **PASS / FAIL** |

    ## 失败处理

    如任一标准失败：

    1. 截图 / 复制相关日志片段
    2. 记录失败步骤编号和实际观察
    3. 在本文件 `## 证据收集` 节填写
    4. 阻断发布，通知开发者修复后重新执行本清单

    ## 证据收集

    | 项目 | 内容 |
    |------|------|
    | 后端启动日志片段（步骤 A） | （粘贴前 50 行或模型加载行附近）|
    | 「已存入 N 条」截图 / 文字（步骤 B） | |
    | 知识库列表截图（步骤 B.8）| |
    | RAG 日志行（步骤 C）| （粘贴包含 RAG context 或 few-shot 的日志行）|
    | 最终判定 | PASS / FAIL |
    | 执行人签名 | |

    ---
    *Phase 23 Smoke Test — generated 2026-06-02*
    *Pass criteria source: 23-CONTEXT.md D-14*

    ---

    Save the file as docs/smoke-test-23.md. Use UTF-8 encoding (Chinese characters must be
    preserved correctly).

    After writing, verify the file exists and contains all four D-14 criteria:
      grep -c "D-14\." docs/smoke-test-23.md   (should be >= 4)
      grep "RAG context\|few-shot" docs/smoke-test-23.md  (should return at least 1 line)
      grep "ModuleNotFoundError" docs/smoke-test-23.md    (should return at least 1 line)
      grep "已存入" docs/smoke-test-23.md                 (should return at least 1 line)
  </action>
  <verify>
    <automated>python -c "
import re, sys
content = open('docs/smoke-test-23.md', encoding='utf-8').read()
checks = [
    ('D-14.1 criterion', 'ModuleNotFoundError' in content),
    ('D-14.2 criterion', '已存入' in content),
    ('D-14.3 criterion', '知识库配置页' in content),
    ('D-14.4 criterion', 'RAG context' in content and 'few-shot' in content),
    ('log path reference', 'backend.log' in content),
    ('pass/fail table', 'PASS / FAIL' in content),
]
failures = [name for name, ok in checks if not ok]
if failures: sys.exit('Missing: ' + str(failures))
print('All criteria present')
"</automated>
  </verify>
  <acceptance_criteria>
    - docs/smoke-test-23.md exists and is UTF-8 encoded
    - File contains the string "ModuleNotFoundError" (D-14.1 check)
    - File contains the string "已存入" (D-14.2 check)
    - File contains the string "知识库配置页" (D-14.3 check)
    - File contains both "RAG context" and "few-shot" (D-14.4 check)
    - File contains "backend.log" (correct log path reference)
    - File contains a pass/fail summary table with all four criteria
    - File contains a preconditions section with the "无 Python 运行时" check
    - Verify script above exits with code 0 and prints "All criteria present"
  </acceptance_criteria>
  <done>docs/smoke-test-23.md written with complete checklist covering all four D-14 pass criteria; any human can execute it against the packaged installer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute smoke test on packaged installer</name>
  <read_first>
    - docs/smoke-test-23.md (the checklist just created in Task 1)
    - .planning/phases/CTRX-23-pyinstaller/23-CONTEXT.md (D-13, D-14, D-16 — release gate requirements)
  </read_first>
  <what-built>
    Plans 23-01 through 23-03 have:
    - Installed lancedb/sentence-transformers/torch into .venv
    - Downloaded bge-m3 weights to electron/resources/models/bge-m3/
    - Extended hiddenimports in ctrx_backend.spec
    - Added model env var injection to backendChildEnv()
    - Added models/** to package.json extraResources

    To build the installer, run:
      scripts/build.ps1 -Version 1.4.0   (or appropriate version flag)

    This executes: PyInstaller → copy backend to electron/resources/ → electron-builder → NSIS installer.
  </what-built>
  <how-to-verify>
    Before executing the checklist:

    Step 1 — Build the installer:
      PowerShell: .\scripts\build.ps1 -Version 1.4.0
      (If -Version is not supported, check scripts/build.ps1 for the correct flag or hardcode version)

    Step 2 — Locate the installer at dist/CTRX-Setup-1.4.0.exe (or dist/win-unpacked/)

    Step 3 — Install on a Windows machine (the current dev machine is acceptable if no Python
    is in the system PATH during the test — or use a separate test machine)

    Step 4 — Execute docs/smoke-test-23.md step by step:
      - Steps A through D as written
      - Fill in the Evidence Collected table
      - Record PASS or FAIL for each of the four D-14 criteria

    Log file location: %APPDATA%\CTRX\logs\backend.log
    (On Windows, %APPDATA% is typically C:\Users\<username>\AppData\Roaming)

    Expected outcomes (all four must be PASS):
      D-14.1: Log shows no ModuleNotFoundError; shows embedding model load line
      D-14.2: PathB entry submission shows「已存入 N 条」ElMessage success toast
      D-14.3: KB config page shows the newly entered record after refresh
      D-14.4: Re-processing a performance-fee contract shows "RAG context" or "few-shot" in log
  </how-to-verify>
  <resume-signal>
    Type "smoke test PASS" if all four D-14 criteria passed.
    Type "smoke test FAIL: [describe which criterion failed and what you observed]" if any criterion failed.

    If FAIL: do NOT proceed to /gsd:complete-phase. Diagnose the failure, fix the root cause
    (likely in 23-02 or 23-03), re-build, and re-run the checklist.
  </resume-signal>
  <acceptance_criteria>
    - All four D-14 criteria are marked PASS in the Evidence Collected table in docs/smoke-test-23.md
    - The Evidence Collected section has at least the log snippet for D-14.1 and the log line for D-14.4
    - No ModuleNotFoundError appears anywhere in the backend.log from the packaged run
    - The "RAG context" or "few-shot" keyword is visible in the actual log (not just the checklist document)
  </acceptance_criteria>
  <done>All four D-14 pass criteria confirmed on packaged installer; v1.4 KB full-chain smoke test passed; release gate cleared (D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packaged installer → test machine | The installer is the artifact under test; smoke test verifies no supply-chain corruption produced a bad bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | Packaged installer (CTRX-Setup-1.4.0.exe) | accept | Smoke test itself is the integrity check. For production distribution, consider adding a SHA-256 checksum to the release page. Low risk for internal desktop tooling. |
| T-23-09 | Information Disclosure | Backend log at %APPDATA%\CTRX\logs\backend.log | accept | Log is local to the user's machine; no PII in KB embedding pipeline logs. LLM API key is NOT logged (existing protection). |
| T-23-SC | Tampering | docs/smoke-test-23.md content | accept | Documentation file; no supply chain concern. |
</threat_model>

<verification>
After Task 1:

  # Verify checklist completeness
  python -c "
  content = open('docs/smoke-test-23.md', encoding='utf-8').read()
  criteria = ['ModuleNotFoundError', '已存入', '知识库配置页', 'RAG context', 'few-shot', 'backend.log']
  for c in criteria:
      assert c in content, f'Missing: {c}'
  print('All criteria present in smoke-test-23.md')
  "

After Task 2 (human):

  # Confirm evidence is recorded in the file
  grep "PASS\|FAIL" docs/smoke-test-23.md
</verification>

<success_criteria>
- docs/smoke-test-23.md exists with complete KB chain checklist
- All four D-14 pass criteria are present as checkable items
- Human executes checklist on packaged installer and all four criteria pass
- Phase 23 release gate cleared — v1.4 ready to ship
</success_criteria>

<output>
Create .planning/phases/CTRX-23-pyinstaller/23-04-SUMMARY.md when done
</output>
