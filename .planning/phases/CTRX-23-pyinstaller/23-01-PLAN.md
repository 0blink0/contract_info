---
phase: "23"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - electron/resources/models/bge-m3/
autonomous: false
requirements:
  - KB-PKG-01
  - KB-PKG-02

must_haves:
  truths:
    - "lancedb, pyarrow, sentence-transformers, torch are installed in .venv and importable"
    - "bge-m3 model directory exists at electron/resources/models/bge-m3/ with config.json, tokenizer.json, and model weights"
    - "SentenceTransformer(path, local_files_only=True) loads the model from electron/resources/models/bge-m3/ without network access"
    - "electron/resources/models/ is excluded from git via .gitignore"
  artifacts:
    - path: "electron/resources/models/bge-m3/config.json"
      provides: "Sentinel confirming bge-m3 directory is in SentenceTransformer flat-directory format"
    - path: ".gitignore"
      provides: "Exclusion rule for model weights directory"
  key_links:
    - from: "electron/resources/models/bge-m3/"
      to: "kb_service.py SentenceTransformer(str(model_path), local_files_only=True)"
      via: "CTRX_MODELS_DIR env var (injected in Wave 2 plan)"
      pattern: "config\\.json.*tokenizer\\.json"
---

<objective>
Install KB dependencies into .venv and populate the bge-m3 model weight directory at
electron/resources/models/bge-m3/. This Wave 0 plan is a hard prerequisite — no
hiddenimports audit, no Electron env injection, and no build verification is possible
until both blockers are resolved.

Purpose: Unblock all subsequent Phase 23 work. lancedb, pyarrow, sentence-transformers,
and torch are absent from .venv (VERIFIED: pip list). electron/resources/models/ does
not exist (VERIFIED: filesystem scan).

Output:
- .venv with lancedb>=0.33.0, sentence-transformers>=5.5.1, pyarrow, torch installed
- electron/resources/models/bge-m3/ populated in SentenceTransformer flat-directory format
- .gitignore updated to exclude the ~2.5 GB model directory
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CTRX-23-pyinstaller/23-CONTEXT.md
@.planning/phases/CTRX-23-pyinstaller/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install KB dependencies (CPU-only torch)</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (verify lancedb>=0.33.0 and sentence-transformers>=5.5.1 lines are present)
    - .planning/phases/CTRX-23-pyinstaller/23-RESEARCH.md (Blocker 1 section, Pitfall 3 on torch CPU-only)
  </read_first>
  <action>
    Install dependencies into the project's .venv. Follow these steps in order:

    Step 1 — Install CPU-only torch first to prevent pip from pulling CUDA wheels (Pitfall 3:
    CUDA torch adds 1-3 GB unnecessarily):
      .venv/Scripts/pip install torch --index-url https://download.pytorch.org/whl/cpu

    Step 2 — Install lancedb and sentence-transformers (pyarrow installs as a transitive dep):
      .venv/Scripts/pip install lancedb>=0.33.0 sentence-transformers>=5.5.1

    Step 3 — Verify all four packages are installed:
      .venv/Scripts/pip show lancedb pyarrow sentence-transformers torch

    No changes to requirements.txt are needed — the file already declares lancedb>=0.33.0
    and sentence-transformers>=5.5.1 on lines 14-15. Do NOT add torch or pyarrow entries
    (they are transitive dependencies).

    If pip is not found at .venv/Scripts/pip, try .venv/Scripts/pip.exe or
    python -m pip with the venv activated.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "import lancedb; import pyarrow; import sentence_transformers; import torch; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/pip show lancedb` outputs a line starting with `Name: lancedb`
    - `.venv/Scripts/pip show pyarrow` outputs a line starting with `Name: pyarrow`
    - `.venv/Scripts/pip show sentence-transformers` outputs a line starting with `Name: sentence-transformers`
    - `.venv/Scripts/pip show torch` outputs `Location:` path containing `cpu` or the wheel is the CPU variant (no `+cu` in version string)
    - The verify command above prints `OK` with exit code 0
  </acceptance_criteria>
  <done>All four packages importable from .venv without errors; torch is CPU-only variant.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Download bge-m3 and save to electron/resources/models/bge-m3/</name>
  <read_first>
    - .planning/phases/CTRX-23-pyinstaller/23-RESEARCH.md (Blocker 2 section, Pitfall 1 on cache vs flat-directory format)
    - .planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md (electron/resources/models/bge-m3 section)
    - backend/app/services/kb_service.py (lines around `SentenceTransformer(str(model_path), local_files_only=True)`)
  </read_first>
  <what-built>
    Task 1 has installed sentence-transformers into .venv. The model download (~2.5 GB) must
    be performed manually because it requires network access and substantial disk I/O that
    Claude cannot execute through the shell in a reliable interactive way.
  </what-built>
  <how-to-verify>
    Run this Python script from the project root (with .venv activated):

      python -c "
      from sentence_transformers import SentenceTransformer
      import shutil, os
      from pathlib import Path

      # Step 1: Download and save in flat-directory format (NOT hub cache format)
      target = Path('electron/resources/models/bge-m3')
      if not target.exists():
          print('Downloading bge-m3 (~2.5 GB)...')
          model = SentenceTransformer('BAAI/bge-m3')
          target.parent.mkdir(parents=True, exist_ok=True)
          model.save(str(target))
          print(f'Saved to {target}')
      else:
          print('Directory already exists, skipping download')

      # Step 2: Verify flat-directory format
      assert (target / 'config.json').exists(), 'config.json missing'
      assert (target / 'tokenizer.json').exists() or (target / 'tokenizer_config.json').exists(), 'tokenizer missing'

      # Step 3: Verify local reload works
      model2 = SentenceTransformer(str(target), local_files_only=True)
      vec = model2.encode(['test'])
      assert vec.shape[1] > 0, 'encode failed'
      print('Reload OK, vector shape:', vec.shape)
      "

    Expected output:
      Downloading bge-m3 (~2.5 GB)... (first run)
      Saved to electron/resources/models/bge-m3
      Reload OK, vector shape: (1, 1024)

    If the directory already exists from a previous attempt, it will print "Directory already
    exists" and proceed to reload verification.
  </how-to-verify>
  <resume-signal>
    After the script completes successfully, type "model downloaded" to continue.
    If you get an OSError about config.json on reload, the directory format is wrong —
    delete electron/resources/models/bge-m3/ and re-run with model.save() as shown above.
  </resume-signal>
  <acceptance_criteria>
    - electron/resources/models/bge-m3/config.json exists
    - electron/resources/models/bge-m3/tokenizer.json or tokenizer_config.json exists
    - electron/resources/models/bge-m3/ contains at least one .bin or .safetensors file
    - The reload script above prints "Reload OK" with exit code 0
    - The vector shape dimension is 1024 (bge-m3 embedding dimension)
  </acceptance_criteria>
  <done>bge-m3 weights are at electron/resources/models/bge-m3/ in flat SentenceTransformer local-path format; local_files_only=True reload works.</done>
</task>

<task type="auto">
  <name>Task 3: Add electron/resources/models/ to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (read current content before appending — verify the trailing entry and line endings)
    - .planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md (.gitignore section showing current lines 1-17)
  </read_first>
  <action>
    Append one line to .gitignore after the existing `dist/` line:

      electron/resources/models/

    Follow the trailing-slash pattern already used by `.venv/`, `__pycache__/`, `frontend/node_modules/`,
    `frontend/dist/`, `node_modules/`, `dist/` in the current file.

    Do NOT remove or reorder any existing entries.

    After appending, verify the line was added correctly:
      git check-ignore -v electron/resources/models/bge-m3/config.json

    This command should output a line containing `electron/resources/models/`.
  </action>
  <verify>
    <automated>git check-ignore -v electron/resources/models/bge-m3/config.json</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `electron/resources/models/` (exact, trailing slash, no leading space)
    - `git check-ignore -v electron/resources/models/bge-m3/config.json` prints a line referencing `.gitignore` with the `electron/resources/models/` pattern
    - `git status` does NOT show electron/resources/models/ or its contents as untracked files
  </acceptance_criteria>
  <done>Model weights directory is excluded from git tracking; git status confirms no model files appear as untracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → electron/resources/models/ | bge-m3 weights downloaded from HuggingFace Hub (BAAI/bge-m3); supply chain trust |
| .venv install | pip packages pulled from PyPI; supply chain trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | pip install (torch, lancedb, sentence-transformers) | mitigate | slopcheck post-install; packages are [ASSUMED] from audit table in RESEARCH.md — all four are established production libraries (PyTorch, Apache Arrow, UKPLab, LanceDB). Human checkpoint in Task 2 provides opportunity to abort if unexpected network behavior observed. |
| T-23-02 | Tampering | bge-m3 download from BAAI/bge-m3 on HuggingFace Hub | accept | Model is from BAAI (Beijing Academy of AI), well-known in Chinese NLP. Low-value target for supply chain attack. Record model version hash post-download for auditability. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | Package Legitimacy Audit completed in RESEARCH.md — all packages [ASSUMED OK] with no [SLOP] verdicts. Blocking human checkpoint in Task 2 allows abort if suspicious behavior observed. |
</threat_model>

<verification>
Run after all three tasks complete:

  # Verify model reload in isolation (no internet)
  .venv/Scripts/python -c "
  from sentence_transformers import SentenceTransformer
  from pathlib import Path
  model = SentenceTransformer(str(Path('electron/resources/models/bge-m3')), local_files_only=True)
  v = model.encode(['业绩报酬提取方式'])
  print('shape:', v.shape)
  assert v.shape == (1, 1024)
  print('PASS')
  "

  # Verify git exclusion
  git check-ignore -v electron/resources/models/bge-m3/config.json

  # Verify all packages installed
  .venv/Scripts/pip show lancedb pyarrow sentence-transformers torch
</verification>

<success_criteria>
- lancedb, pyarrow, sentence-transformers, torch are importable from .venv
- electron/resources/models/bge-m3/ exists with config.json and model weights
- SentenceTransformer reload with local_files_only=True returns vectors of shape (N, 1024)
- git status does not show model directory as untracked
- Wave 1 plans (23-02-PLAN.md, 23-03-PLAN.md) are now unblocked
</success_criteria>

<output>
Create .planning/phases/CTRX-23-pyinstaller/23-01-SUMMARY.md when done
</output>
