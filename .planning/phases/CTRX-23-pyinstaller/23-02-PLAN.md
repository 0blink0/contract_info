---
phase: "23"
plan: "02"
type: execute
wave: 1
depends_on:
  - "23-01"
files_modified:
  - ctrx_backend.spec
  - docs/packaging/hiddenimports-changelog.md
autonomous: true
requirements:
  - KB-PKG-01

must_haves:
  truths:
    - "ctrx_backend.spec windows_hidden list includes lancedb, pyarrow, sentence_transformers, torch, tokenizers, transformers, and huggingface_hub entries"
    - "docs/packaging/hiddenimports-changelog.md has a Phase 23 entry dated 2026-06-02 describing the added packages"
    - "Both changes land in the same git commit so check_hiddenimports_diff.py gate passes"
    - "pyinstaller build completes without ModuleNotFoundError for the new packages"
  artifacts:
    - path: "ctrx_backend.spec"
      provides: "windows_hidden list extended with KB embedding pipeline hiddenimports"
      contains: "lancedb"
    - path: "docs/packaging/hiddenimports-changelog.md"
      provides: "Changelog entry for Phase 23 hiddenimports additions"
      contains: "Phase 23"
  key_links:
    - from: "ctrx_backend.spec windows_hidden"
      to: "PyInstaller Analysis hiddenimports"
      via: "hiddenimports.extend(windows_hidden) at spec line 42"
      pattern: "hiddenimports\\.extend\\(windows_hidden\\)"
    - from: "docs/packaging/hiddenimports-changelog.md"
      to: "scripts/check_hiddenimports_diff.py --changelog"
      via: "CI gate: backend-packaging-gate.yml"
      pattern: "2026-06-02"
---

<objective>
Extend ctrx_backend.spec with the lancedb/pyarrow/sentence-transformers/torch hiddenimports
needed for the KB embedding pipeline, and append the matching changelog entry to
docs/packaging/hiddenimports-changelog.md. Both changes MUST be committed together
(D-03 gate).

Purpose: KB-PKG-01 — LanceDB and sentence-transformers packages must be reachable inside
the PyInstaller bundle. lancedb uses a Rust extension (.pyd) and pyarrow uses C extensions
that PyInstaller static analysis misses. Explicit hiddenimports are the correct fix
(per pyinstaller.org/en/stable/when-things-go-wrong.html).

Output:
- ctrx_backend.spec: windows_hidden list with ~15 new entries
- docs/packaging/hiddenimports-changelog.md: Phase 23 section appended
- Single commit containing both file changes (D-03 same-commit gate)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/CTRX-23-pyinstaller/23-CONTEXT.md
@.planning/phases/CTRX-23-pyinstaller/23-RESEARCH.md
@.planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md
@.planning/phases/CTRX-23-pyinstaller/23-01-SUMMARY.md

<interfaces>
From ctrx_backend.spec (exact current content):

  windows_hidden = [
      "colorama",
  ]

  # Optional dependency chain: include only when installed.
  if find_spec("openai") is not None:
      hiddenimports.extend(["openai", "openai._client"])

From docs/packaging/hiddenimports-changelog.md (exact current content):

  # hiddenimports changelog

  ## 2026-05-28

  - Added initial `ctrx_backend.spec` hiddenimports baseline.
  - Split hiddenimports by `common/windows/linux`.
  - Locked excludes with explicit `psycopg2` removal.

CI gate command (from .github/workflows/backend-packaging-gate.yml):

  python scripts/check_hiddenimports_diff.py \
    --base origin/master \
    --spec ctrx_backend.spec \
    --changelog docs/packaging/hiddenimports-changelog.md

Gate logic (from scripts/check_hiddenimports_diff.py):
  if hidden_changed and not changelog_changed: return exit code 1
  Watched tokens: common_hidden, windows_hidden, linux_hidden, hiddenimports
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run pyinstaller --debug=imports to discover actual hiddenimports gaps</name>
  <files>ctrx_backend.spec</files>
  <read_first>
    - ctrx_backend.spec (full file — understand current windows_hidden structure before modifying)
    - .planning/phases/CTRX-23-pyinstaller/23-RESEARCH.md (Pattern 1: hiddenimports strategy, Open Questions #3)
    - .planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md (ctrx_backend.spec section with target extension list)
  </read_first>
  <action>
    Step 1 — Run a test build with debug imports enabled to discover actual gaps:
      .venv/Scripts/pyinstaller ctrx_backend.spec --noconfirm --clean --log-level DEBUG 2>&1 | Select-String "ModuleNotFoundError|WARNING.*not found|ERROR" | Out-File /tmp/pyinstaller-debug.txt
    (or pipe to a file for review)

    Step 2 — Review the output for ModuleNotFoundError and missing-module warnings
    related to lancedb, pyarrow, sentence_transformers, torch, tokenizers, transformers,
    huggingface_hub.

    Step 3 — Update windows_hidden in ctrx_backend.spec by appending the following entries
    after the "colorama" line. Start with the research-provided baseline list and trim or
    extend based on Step 2 findings:

      windows_hidden = [
          "colorama",
          # --- Phase 23: lancedb / pyarrow / sentence-transformers (KB-PKG-01) ---
          "lancedb",
          "lancedb.remote",
          "lancedb.embeddings",
          "pyarrow",
          "pyarrow.vendored",
          "pyarrow.vendored.version",
          "sentence_transformers",
          "sentence_transformers.models",
          "sentence_transformers.util",
          "torch",
          "torch.utils",
          "torch.utils.data",
          "tokenizers",
          "transformers",
          "transformers.models",
          "transformers.models.bert",
          "transformers.models.xlm_roberta",
          "huggingface_hub",
          "huggingface_hub.file_download",
      ]

    Rules per D-01/D-02:
    - Append to windows_hidden ONLY (not common_hidden) — Windows-only scope for Phase 23
    - Do not remove any existing entries from common_hidden or windows_hidden
    - Do not modify linux_hidden
    - If Step 2 reveals additional modules not in the list above, add them
    - If Step 2 confirms some entries in the list above are unnecessary, keep them anyway
      (extra entries are harmless; missing entries cause runtime failures)

    DO NOT use find_spec guard for these packages — they are required, not optional.
    The openai find_spec guard is only for the optional openai integration.

    Step 4 — Run a clean build to verify no ModuleNotFoundError:
      .venv/Scripts/pyinstaller ctrx_backend.spec --noconfirm --clean --log-level WARN

    Note: The full build may take several minutes. A successful build writes dist/ctrx-backend/.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "import lancedb, pyarrow, sentence_transformers, torch, tokenizers, transformers, huggingface_hub; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ctrx_backend.spec windows_hidden list contains at minimum: "lancedb", "pyarrow", "sentence_transformers", "torch", "tokenizers", "transformers", "huggingface_hub"
    - The existing "colorama" entry in windows_hidden is still present (no regression)
    - common_hidden and linux_hidden are unchanged from before this task
    - pyinstaller build completes with exit code 0 (no ModuleNotFoundError in output)
    - dist/ctrx-backend/ctrx-backend.exe exists after build
  </acceptance_criteria>
  <done>windows_hidden is extended; pyinstaller build succeeds; dist/ctrx-backend/ contains the backend executable.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 23 changelog entry and commit both files together</name>
  <files>docs/packaging/hiddenimports-changelog.md</files>
  <read_first>
    - docs/packaging/hiddenimports-changelog.md (full file — copy the ## YYYY-MM-DD heading format from Phase 12 entry)
    - scripts/check_hiddenimports_diff.py (lines around the hidden_changed / changelog_changed logic to confirm gate behavior)
    - .planning/phases/CTRX-23-pyinstaller/23-PATTERNS.md (hiddenimports-changelog.md section with Phase 23 append pattern)
  </read_first>
  <action>
    Append the following section to docs/packaging/hiddenimports-changelog.md after the
    existing 2026-05-28 entry. Preserve the existing content exactly — only append:

      ## 2026-06-02

      - Phase 23 (KB-PKG-01): Appended lancedb/pyarrow/sentence-transformers/torch/tokenizers/
        transformers/huggingface_hub entries to `windows_hidden` for KB embedding pipeline
        packaging.
      - Rationale: lancedb uses a Rust extension (`lancedb._lib`) and pyarrow uses C extensions;
        PyInstaller static analysis cannot resolve all dynamic imports. sentence-transformers
        requires torch, tokenizers, transformers, and huggingface_hub at runtime.
      - Platform scope: Windows only (D-02); Linux deferred to next milestone.

    Then verify the CI gate passes BEFORE committing by running:
      python scripts/check_hiddenimports_diff.py \
        --base origin/master \
        --spec ctrx_backend.spec \
        --changelog docs/packaging/hiddenimports-changelog.md

    This must exit with code 0 (no output or "OK" message). If it exits with code 1,
    the changelog entry is missing or the spec file was not actually changed — fix before
    committing.

    Then commit both files in a single commit (D-03 same-commit requirement):
      git add ctrx_backend.spec docs/packaging/hiddenimports-changelog.md
      git commit -m "feat(23-02): extend windows_hidden for lancedb/sentence-transformers KB pipeline

    Append lancedb, pyarrow, sentence-transformers, torch, tokenizers, transformers,
    huggingface_hub entries to windows_hidden in ctrx_backend.spec.

    Changelog updated in same commit per D-03 gate requirement (KB-PKG-01)."

    Do NOT commit any other files in this commit (keep the gate clean).
  </action>
  <verify>
    <automated>python scripts/check_hiddenimports_diff.py --base HEAD~1 --spec ctrx_backend.spec --changelog docs/packaging/hiddenimports-changelog.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/packaging/hiddenimports-changelog.md contains a "## 2026-06-02" section with "Phase 23" text
    - The gate command `python scripts/check_hiddenimports_diff.py --base origin/master --spec ctrx_backend.spec --changelog docs/packaging/hiddenimports-changelog.md` exits with code 0
    - `git log --oneline -1` shows the commit message containing "23-02"
    - `git show --name-only HEAD` lists both ctrx_backend.spec AND docs/packaging/hiddenimports-changelog.md (same commit)
    - No other files appear in that commit
  </acceptance_criteria>
  <done>Changelog updated; both spec and changelog committed in the same git commit; CI gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .venv packages → PyInstaller bundle | hiddenimports controls which installed packages are bundled; incorrect entries cause runtime failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | ctrx_backend.spec windows_hidden | mitigate | D-03 gate enforces spec+changelog co-commit; check_hiddenimports_diff.py exit code 1 blocks CI if mismatched |
| T-23-04 | Denial of Service | PyInstaller build including torch (~1.5 GB CPU-only) | accept | Large bundle is expected; CPU-only torch (Wave 0 Task 1) bounds the size. No mitigation needed beyond size documentation. |
| T-23-SC | Tampering | pip packages in .venv (already installed in Wave 0) | accept | Packages verified in Wave 0; no new installs in this plan. |
</threat_model>

<verification>
After both tasks complete:

  # Gate check (must exit 0)
  python scripts/check_hiddenimports_diff.py \
    --base origin/master \
    --spec ctrx_backend.spec \
    --changelog docs/packaging/hiddenimports-changelog.md

  # Spec contains required entries
  python -c "
  import ast, sys
  src = open('ctrx_backend.spec').read()
  required = ['lancedb', 'pyarrow', 'sentence_transformers', 'torch', 'tokenizers', 'transformers', 'huggingface_hub']
  missing = [r for r in required if r not in src]
  if missing: sys.exit(f'Missing: {missing}')
  print('All required entries present')
  "

  # Commit integrity
  git show --name-only HEAD
</verification>

<success_criteria>
- windows_hidden in ctrx_backend.spec has all KB embedding pipeline hiddenimports
- docs/packaging/hiddenimports-changelog.md has Phase 23 entry
- Both in the same commit — gate exits 0
- PyInstaller build produces dist/ctrx-backend/ctrx-backend.exe without ModuleNotFoundError
</success_criteria>

<output>
Create .planning/phases/CTRX-23-pyinstaller/23-02-SUMMARY.md when done
</output>
