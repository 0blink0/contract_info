# v1.3 里程碑收口：规划外体验增强

**里程碑:** v1.3 多文件并行与详情页重构  
**归属:** 与 Phases 15–19 同一里程碑交付（2026-05-29），非独立 Phase 20  
**说明:** 下列项在 Phase 17–19 执行后根据 UAT 反馈补齐，计入 v1.3 发布范围。

---

## 桌面启动

| 项 | 说明 | 提交 |
|----|------|------|
| Electron 启动页 | 替换单行「正在启动后端」为品牌化卡片 + 进度动效（`electron/splash.html`） | `7b2497c` |

---

## 详情区导航与信息架构

| 项 | 说明 | 提交 |
|----|------|------|
| 子页返回层级 | 五表/字段 B 页「← 返回文件详情」；仅 Hub 显示「← 返回文件列表」 | `183b748` |
| Hub 与子页分工 | 文件名、状态、进度条、批量下载、删除 **仅 Hub**；子页仅保留本表一行标题 + 编辑/摘录 | `3ab4d45` |
| 表页下载按钮 | 单表「下载」从表页移除，统一在 Hub 操作 | `3ab4d45` |

---

## 分表 API 与旧后端兼容

| 项 | 说明 | 提交 |
|----|------|------|
| 分表 preview 404 回退 | 旧打包后端无 `GET /preview/{section}` 时，前端回退全量 `GET /preview` 再切片 | `4864c56` |
| verification 空数据回退 | verification 404/空行时，用 sectional preview 补核对行 | `ab3dfd9` |

**运维提示:** 桌面版需重打 PyInstaller 后端（`package_backend.ps1`）才能获得完整 verification API；仅更新前端时依赖上述回退。

---

## 摘录核对 UI（Phase 17 增强）

| 项 | 说明 | 提交 |
|----|------|------|
| 右侧阅读区 | 左右分栏：左表摘要 + 右栏分段阅读长摘录（替代表格 tooltip） | `e617668` |
| 移除页码列 | docx 解析层暂无 `page` 元数据，暂隐藏「摘录页码」列 | `e617668` |
| 规则抓取原文 | `source=rule` 时 excerpt 展示整段章节正文（非仅短 snippet） | `b4b29e0` |
| 阅读区加大 | 右栏约 55% 宽、最高约 72vh | `b4b29e0` |

---

## 仍属里程碑外 / 后续

| 项 | 状态 |
|----|------|
| docx/PDF 真实页码 | 未做；核对表预留能力，UI 已不展示空页码列 |
| 字段 B 摘录右栏 | 与五表同组件可复用，本里程碑未改 PathBDetail |
| PATHB-EX-01/02 | 仍 deferred post-v1.3 |

---

*Document created: 2026-05-29 — closes v1.3 UAT follow-ups into milestone record*
