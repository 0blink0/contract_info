---
phase: CTRX-12-pyinstaller
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scripts/smoke_phase12.ps1
  - docs/packaging/smoke-checklist-template.md
  - docs/packaging/phase12-smoke-evidence.md
autonomous: true
requirements:
  - PKG-03
must_haves:
  truths:
    - "clean Windows VM 可完成上传->抽取->下载主链路"
    - "Linux clean 启动日志无 ModuleNotFoundError"
    - "烟测失败必须返回非零退出并阻断发布"
  artifacts:
    - path: "scripts/smoke_phase12.ps1"
      provides: "主链路烟测执行与发布阻断"
    - path: "docs/packaging/phase12-smoke-evidence.md"
      provides: "结构化证据沉淀（命令/日志/截图/结果）"
  key_links:
    - from: "scripts/smoke_phase12.ps1"
      to: "backend API flow"
      via: "health/upload/extract/download checks"
      pattern: "health|upload|extract|download"
---

<objective>
在 12-01 打包基础上完成 Phase 12 的验收闭环：通过 clean VM 烟测证明二进制可在无 Python 环境下跑通主链路，并形成可审计证据。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/CTRX-12-pyinstaller/12-CONTEXT.md
@.planning/phases/CTRX-12-pyinstaller/12-RESEARCH.md
@.planning/phases/CTRX-12-pyinstaller/12-01-PLAN.md
@electron/resources/
</context>

<tasks>
<task type="auto">
  <name>Task 1: 构建 Windows clean VM 主链路烟测脚本与阻断机制</name>
  <files>scripts/smoke_phase12.ps1, docs/packaging/smoke-checklist-template.md, docs/packaging/phase12-smoke-evidence.md</files>
  <action>实现 `scripts/smoke_phase12.ps1`：启动 backend 二进制后执行 health->upload->extract->download 全链路检查；校验 `CTRX_DATA_DIR` 隔离；任一步失败立即返回非零退出码。补充 smoke checklist 模板和证据文档模板（命令、日志、截图、下载产物哈希、执行人、时间戳）。</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/smoke_phase12.ps1 -BackendExePath "electron/resources/ctrx-backend-win-x64-v1.2.0/ctrx-backend.exe" -DataDir "$env:TEMP\ctrx-smoke" -GoldenDocx "example/_golden_contract.docx"</automated>
  </verify>
  <done>Windows clean VM 烟测可一键执行并输出 PASS/FAIL；失败时发布阻断生效。</done>
</task>

<task type="auto">
  <name>Task 2: 补齐 Linux clean 启动验收证据</name>
  <files>docs/packaging/phase12-smoke-evidence.md</files>
  <action>在验收文档中增加 Linux 启动检查记录：运行 packaged backend，采集启动日志并断言无 `ModuleNotFoundError`、无 PostgreSQL 连接尝试。该检查作为 Success Criteria #1 的 Linux 证据。</action>
  <verify>
    <automated>bash -lc 'CTRX_DATA_DIR=/tmp/ctrx-smoke-linux "electron/resources/ctrx-backend-linux-x64-v1.2.0/ctrx-backend" --help > /tmp/ctrx-linux-start.log 2>&1 || true; ! rg "ModuleNotFoundError|postgresql|psycopg2" /tmp/ctrx-linux-start.log'</automated>
  </verify>
  <done>Linux clean 启动证据纳入 phase12 smoke 文档，满足跨平台可启动验收口径。</done>
</task>
</tasks>

<verification>
- Windows clean VM 主链路：`powershell -ExecutionPolicy Bypass -File scripts/smoke_phase12.ps1 ...`
- Linux clean 启动：`bash -lc '... ! rg "ModuleNotFoundError|postgresql|psycopg2" ...'`
- 证据产出检查：`rg "PASS|FAIL|截图|日志|hash" docs/packaging/phase12-smoke-evidence.md`
</verification>

<success_criteria>
1. clean Windows VM（无 Python）可完整跑通上传->抽取->下载。
2. Linux clean 启动日志无 `ModuleNotFoundError`。
3. `CTRX_DATA_DIR` 隔离可验证，安装目录无运行时写入。
4. 任意烟测失败即非零退出并阻断发布。
</success_criteria>

<output>
After completion, create `.planning/phases/CTRX-12-pyinstaller/12-02-SUMMARY.md`
</output>
