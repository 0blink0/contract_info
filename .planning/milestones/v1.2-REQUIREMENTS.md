# Archived Requirements: CTRX v1.2 桌面化交付

**Archive Date:** 2026-05-29
**Milestone:** v1.2 桌面化交付
**Status:** ✅ SHIPPED 2026-05-29

> This file is an archive of the v1.2 requirements as of milestone close.
> For current/next milestone requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: CTRX v1.2 桌面化交付

**Defined:** 2026-05-28
**Core Value:** 上传 docx → 可导入 Excel + 路径 B 手录辅助 + 可解释校验（本地桌面，无服务器依赖）

## v1.2 Requirements

### DB — SQLite 迁移

- [x] **DB-01**: 系统以方言无关类型运行 SQLite，7 个 Alembic 迁移和 ORM 模型中 `postgresql.JSONB` 替换为 `JSON`、`postgresql.UUID` 替换为 `Uuid`，所有 JSON 列可正常序列化/反序列化
  - **Outcome:** Validated — Phase 11-02 replaced types across 4 migration files + ORM model; JSON roundtrip test passes
- [x] **DB-02**: 数据目录（uploads/、exports/）解析为 `CTRX_DATA_DIR` 环境变量所指路径，不依赖 `__file__` 相对路径，在 PyInstaller `_MEIPASS` 和开发模式下均正确
  - **Outcome:** Validated — Phase 11-03 added `data_dir()`/`uploads_dir()` helpers in config.py; 6 consumer files updated
- [x] **DB-03**: SQLite 会话开启 WAL 模式，Uvicorn 线程池并发读写不产生数据库锁超时
  - **Outcome:** Validated — Phase 11-04 added WAL event listener to session.py
- [x] **DB-04**: `desktop_main.py` 在 uvicorn 启动前以程序化方式执行 `alembic upgrade head`，首次运行自动建表，升级运行自动迁移
  - **Outcome:** Validated — Phase 11-04 created desktop_main.py; programmatic Alembic + idempotency test passes

### PKG — PyInstaller 打包

- [x] **PKG-01**: 新增 `desktop_main.py` 作为 Python 打包入口：读取 `CTRX_DATA_DIR` / `CTRX_PORT` 环境变量 → 初始化路径 → 清除 `get_settings` lru_cache → 程序化执行 Alembic → 启动 uvicorn；支持 `sys.frozen` 路径分支
  - **Outcome:** Validated — Phase 12-01 hardened desktop_main.py with fail-fast resource assertions and sys.frozen branch
- [x] **PKG-02**: 完整 PyInstaller spec（`--onedir`），hiddenimports 覆盖 uvicorn 内部模块、`sqlalchemy.dialects.sqlite`、`pydantic_settings`；datas 包含 `dicts/`、`alembic/`；excludes `psycopg2`；二进制产物放入 `electron/resources/`
  - **Outcome:** Validated — Phase 12-01 created ctrx_backend.spec + hiddenimports CI gate + package_backend.ps1/sh
- [x] **PKG-03**: 在无 Python 环境的干净 Windows VM 烟雾测试通过：上传合同 → 跑通抽取 → 下载 xlsx 全部可用
  - **Outcome:** Validated (Windows) / Deferred (Linux) — Phase 12-02 Windows main-chain smoke passed; Linux clean-VM verify user-approved deferred

### ELEC — Electron 壳与 IPC

- [x] **ELEC-01**: Electron 主进程管理 Python 子进程完整生命周期：spawn PyInstaller 二进制 → 健康轮询 `GET /health`（300 ms 间隔、30 s 超时）→ 健康通过后显示主窗口（启动期显示加载隔屏）→ 崩溃自动重试最多 3 次 → 应用退出时先 SIGTERM 再 SIGKILL（5 s 后）
  - **Outcome:** Validated — Phase 13-01 lifecycle state machine (7/7 tests pass)
- [x] **ELEC-02**: Electron `contextBridge` 通过 `ipcMain.handle` 暴露三个通道：`save-settings`（写 userData/config.json）、`load-settings`（读）、`get-port`（返回子进程监听端口）；使用 electron-store v10；配置项：`llmBaseUrl`、`llmApiKey`、`llmModel`
  - **Outcome:** Validated — Phase 13-01 IPC channels + electron-store (7/7 IPC tests pass; API key redacted in logs)
- [x] **ELEC-03**: 首次启动（或 `llmBaseUrl`/`llmApiKey` 为空时）显示 3 步引导向导（Welcome → LLM 凭证 → 连接测试），向导未完成时封闭主界面入口；完成后路由跳转到主界面
  - **Outcome:** Validated — Phase 13-02 OnboardingWizardView + router beforeEach guard (wizard-gate test passes)
- [x] **ELEC-04**: 应用内 Settings 页面可随时修改 LLM 配置；保存后重启 Python 子进程使新 Key/BaseURL 生效；Settings 与向导共用 `LlmConfigForm.vue` 组件
  - **Outcome:** Validated — Phase 13-02 SettingsView + restart transaction + config rollback (7/7 settings-restart tests pass)

### BUILD — 构建流水线

- [x] **BUILD-01**: electron-builder 配置产出 Windows NSIS 安装包（`.exe`）和 Linux AppImage，`extraResources` 引用 PyInstaller `--onedir` 产物目录，`asarUnpack` 排除大型二进制
  - **Outcome:** Validated — Phase 14-01/03 electron-builder 26.x inline config; CTRX-Setup-1.2.0.exe (133MB) produced
- [x] **BUILD-02**: 提供 4 步构建脚本（PowerShell `.ps1` 和 Bash `.sh` 各一份）：① PyInstaller 打包 Python → ② Vite 构建 Vue SPA → ③ TypeScript 编译 Electron 主进程 → ④ electron-builder 生成安装包
  - **Outcome:** Validated — Phase 14-02 build.ps1 + build.sh with semver validation and fail-fast
- [x] **BUILD-03**: 构建流水线同时产出 Linux `.deb` 安装包，可通过 `dpkg -i` 安装
  - **Outcome:** Validated — Phase 14-01 deb target configured in electron-builder; build.sh produces AppImage + deb

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| DB-01 | Phase 11 | ✅ Complete | JSON types replaced, roundtrip test passes |
| DB-02 | Phase 11 | ✅ Complete | CTRX_DATA_DIR isolation verified |
| DB-03 | Phase 11 | ✅ Complete | WAL listener in session.py |
| DB-04 | Phase 11 | ✅ Complete | Programmatic alembic upgrade on startup |
| PKG-01 | Phase 12 | ✅ Complete | desktop_main.py with fail-fast + sys.frozen |
| PKG-02 | Phase 12 | ✅ Complete | ctrx_backend.spec + hiddenimports CI gate |
| PKG-03 | Phase 12 | ⚠️ Complete (Windows); Linux deferred | Windows smoke passed; Linux clean-VM deferred |
| ELEC-01 | Phase 13 | ✅ Complete | Lifecycle state machine, 7/7 tests |
| ELEC-02 | Phase 13 | ✅ Complete | 3-channel IPC, electron-store, key redaction |
| ELEC-03 | Phase 13 | ✅ Complete | 3-step wizard with route guard |
| ELEC-04 | Phase 13 | ✅ Complete | Settings + restart transaction + rollback |
| BUILD-01 | Phase 14 | ✅ Complete | NSIS 133MB installer produced |
| BUILD-02 | Phase 14 | ✅ Complete | build.ps1 + build.sh dispatchers |
| BUILD-03 | Phase 14 | ✅ Complete | deb target configured, build.sh dispatches |

**Known gaps at close:**
- PKG-03 Linux clean-VM verification: explicitly deferred by user. Windows path is the primary delivery target; Linux verify can be run in future CI environment.

---
*Archive created: 2026-05-29 at v1.2 milestone close*
